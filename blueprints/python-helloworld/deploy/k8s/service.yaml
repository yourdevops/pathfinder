apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
  labels:
    app: {{ service_name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ service_port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ service_name }}
