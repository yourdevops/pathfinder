---
phase: 06.4-ci-step-identity-and-change-tracking
plan: 02
subsystem: ci-steps
tags: [django, tasks, git, slug, sha, archival, change-detection]

# Dependency graph
requires:
  - phase: 06.4-01
    provides: CIStep model with slug, status, file_path, last_change_type fields; clone_repo_full function
provides:
  - Rewritten scan_steps_repository with per-file SHA, slug derivation, collision detection, change classification, archival
  - cleanup_archived_steps task for safe removal of unreferenced archived steps
  - get_compatible_steps filters out archived steps
  - discover_steps returns file_path alongside directory_path
affects: [06.4-03]

# Tech tracking
tech-stack:
  added: []
  patterns: [per-file SHA via git log, slug-based identity derivation, change classification (interface vs metadata), soft-delete archival with cleanup]

key-files:
  created: []
  modified:
    - core/tasks.py
    - core/ci_steps.py
    - core/ci_manifest.py

key-decisions:
  - "last_change_type stored as empty string (not None) when no change detected, consistent with 06.4-01 CharField convention"
  - "Reset last_change_type for all active steps before each scan to avoid stale change markers"

patterns-established:
  - "Slug collision detection: cross-repo slug+engine uniqueness checked before step creation/update"
  - "Per-file SHA via repo_obj.git.log for granular change tracking instead of repo HEAD"
  - "Change classification: _classify_change returns interface/metadata/None based on field comparison"

# Metrics
duration: 3min
completed: 2026-02-16
---

# Phase 06.4 Plan 02: Scan Logic Rewrite with Per-file SHA, Slug Identity, and Archival Summary

**Rewritten scan_steps_repository with per-file commit SHA tracking, slug-based identity derivation, cross-repo collision detection, interface/metadata change classification, and soft-delete archival replacing hard-delete**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-16T17:11:13Z
- **Completed:** 2026-02-16T17:14:06Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- scan_steps_repository fully rewritten: uses clone_repo_full, computes per-file SHA via git log, derives slugs from x-pathfinder.name with directory fallback, detects cross-repo slug collisions, classifies changes as interface or metadata, archives removed steps instead of deleting
- cleanup_archived_steps task safely removes archived steps with zero CIWorkflowStep references (respects on_delete=PROTECT FK)
- get_compatible_steps filters status="active" so archived steps never appear in workflow composer step picker
- discover_steps now returns file_path (relative path to engine file) alongside directory_path

## Task Commits

Each task was committed atomically:

1. **Task 1: Update discover_steps and rewrite scan_steps_repository** - `93fe556` (feat)
2. **Task 2: Filter archived steps in get_compatible_steps** - `91b053a` (feat)

## Files Created/Modified
- `core/tasks.py` - Added _derive_step_slug, _classify_change helpers; rewrote scan_steps_repository loop; added cleanup_archived_steps task
- `core/ci_steps.py` - Added file_path key to discover_steps results
- `core/ci_manifest.py` - Added status="active" filter to get_compatible_steps queryset

## Decisions Made
- Used empty string (not None) for last_change_type when no change detected, consistent with Plan 01 decision on CharField blank=True convention
- Reset last_change_type for all active steps before each scan to ensure clean change markers per scan cycle

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Scan logic complete with full change tracking, ready for Plan 03 (tests or remaining tasks)
- All model fields populated: slug, status, file_path, last_change_type, commit_sha
- Archival and cleanup flow operational

---
*Phase: 06.4-ci-step-identity-and-change-tracking*
*Completed: 2026-02-16*
