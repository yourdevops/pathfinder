---
phase: 06.4-ci-step-identity-and-change-tracking
plan: 01
subsystem: database
tags: [django, models, migrations, git, clone]

# Dependency graph
requires:
  - phase: 05.3-ci-steps-redesign
    provides: CIStep model and core/git_utils.py foundation
provides:
  - CIStep model with slug, status, file_path, last_change_type fields
  - UniqueConstraint on (engine, slug) for catalog-wide uniqueness
  - clone_repo_full function for full single-branch clone
affects: [06.4-02, 06.4-03]

# Tech tracking
tech-stack:
  added: []
  patterns: [soft-delete via status field, slug-based identity, per-file SHA via full clone]

key-files:
  created:
    - core/migrations/0023_cistep_file_path_cistep_last_change_type_cistep_slug_and_more.py
  modified:
    - core/models.py
    - core/git_utils.py

key-decisions:
  - "last_change_type uses blank=True (empty string) instead of null=True per Django CharField convention and ruff DJ001 lint rule"

patterns-established:
  - "CIStep status field pattern: active/archived choices for soft-delete instead of hard-delete"
  - "clone_repo_full alongside clone_repo_shallow: separate entry points for full vs shallow clone needs"

# Metrics
duration: 3min
completed: 2026-02-16
---

# Phase 06.4 Plan 01: CIStep Model Fields and Full Clone Utility Summary

**CIStep model extended with slug, status, file_path, last_change_type fields and UniqueConstraint; clone_repo_full function added to git_utils.py**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-16T17:05:51Z
- **Completed:** 2026-02-16T17:08:25Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- CIStep model has 4 new fields: slug (URL-safe identity), status (active/archived soft-delete), file_path (repo-relative path), last_change_type (interface/metadata change tracking)
- UniqueConstraint on (engine, slug) ensures catalog-wide uniqueness of step slugs per CI engine
- clone_repo_full function provides full single-branch clone for per-file SHA computation via git log
- Migration 0023 applied cleanly to empty CI tables

## Task Commits

Each task was committed atomically:

1. **Task 1: Add slug, status, file_path, and last_change_type fields to CIStep model** - `a1f81fe` (feat)
2. **Task 2: Add clone_repo_full function to git_utils.py** - `1c0e36f` (feat)

## Files Created/Modified
- `core/models.py` - Added 4 new fields, STATUS_CHOICES, CHANGE_TYPE_CHOICES, and UniqueConstraint to CIStep model
- `core/git_utils.py` - Added clone_repo_full function for full single-branch clone
- `core/migrations/0023_cistep_file_path_cistep_last_change_type_cistep_slug_and_more.py` - Database migration for new fields and constraint

## Decisions Made
- Used `blank=True` (empty string sentinel) instead of `null=True` for `last_change_type` CharField, per Django convention and ruff DJ001 lint rule. Empty string represents "no change detected" instead of null.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed null=True from last_change_type CharField**
- **Found during:** Task 1 (CIStep model fields)
- **Issue:** Plan specified `null=True` on `last_change_type` CharField, but ruff DJ001 lint rule (enforced by pre-commit hook) rejects `null=True` on string-based fields
- **Fix:** Removed `null=True`, kept `blank=True` with empty string as the "no change" sentinel
- **Files modified:** core/models.py
- **Verification:** ruff check passes, Django check passes, migration applies cleanly
- **Committed in:** a1f81fe (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Minor semantic change (empty string vs null for "no change"). No scope creep. Functionally equivalent.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- CIStep model fields ready for scan logic rewrite in Plan 02
- clone_repo_full ready for per-file SHA computation in Plan 02
- UniqueConstraint on (engine, slug) ready for slug assignment in Plan 02

---
*Phase: 06.4-ci-step-identity-and-change-tracking*
*Completed: 2026-02-16*
