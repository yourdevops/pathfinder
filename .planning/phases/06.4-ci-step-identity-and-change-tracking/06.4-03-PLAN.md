---
phase: 06.4-ci-step-identity-and-change-tracking
plan: 03
type: execute
wave: 2
depends_on: ["06.4-01"]
files_modified:
  - core/views/ci_workflows.py
  - core/templates/core/ci_workflows/workflow_detail.html
  - core/templates/core/ci_workflows/step_detail.html
  - core/templates/core/ci_workflows/repo_detail.html
  - core/templates/core/ci_workflows/_steps_table.html
autonomous: true

must_haves:
  truths:
    - "Steps catalog table shows archived badge for archived steps"
    - "Step detail page shows archived status banner and slug info"
    - "Workflow detail steps tab shows warning badges for steps with interface changes"
    - "Workflow detail steps tab shows archived badge for archived steps"
    - "Repo detail page shows archived badge on archived steps"
    - "_filter_steps excludes archived steps from the catalog by default"
  artifacts:
    - path: "core/views/ci_workflows.py"
      provides: "Updated _filter_steps to exclude archived, updated WorkflowDetailView to annotate step change warnings"
      contains: "has_step_warnings"
    - path: "core/templates/core/ci_workflows/workflow_detail.html"
      provides: "Step update and archived badges in Steps tab"
      contains: "last_change_type"
    - path: "core/templates/core/ci_workflows/step_detail.html"
      provides: "Archived banner and slug display"
      contains: "archived"
    - path: "core/templates/core/ci_workflows/repo_detail.html"
      provides: "Archived badge on step cards"
      contains: "archived"
    - path: "core/templates/core/ci_workflows/_steps_table.html"
      provides: "Archived badge in catalog table rows"
      contains: "archived"
  key_links:
    - from: "core/views/ci_workflows.py"
      to: "core/models.py"
      via: "CIStep.status and CIStep.last_change_type fields"
      pattern: "status.*archived|last_change_type"
    - from: "core/templates/core/ci_workflows/workflow_detail.html"
      to: "core/views/ci_workflows.py"
      via: "has_step_warnings and step.last_change_type context"
      pattern: "has_step_warnings|last_change_type"
---

<objective>
Update views and templates to surface archived step status and step change warnings in the UI.

Purpose: Users see meaningful feedback when steps are archived or have interface changes. Workflow authors know when their workflows reference outdated or archived steps and can take corrective action.

Output: Updated views with archived step filtering in catalog, change warning computation in workflow detail. Updated templates with badges for archived/changed steps.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.4-ci-step-identity-and-change-tracking/06.4-RESEARCH.md
@.planning/phases/06.4-ci-step-identity-and-change-tracking/06.4-01-SUMMARY.md
@core/views/ci_workflows.py
@core/templates/core/ci_workflows/workflow_detail.html
@core/templates/core/ci_workflows/step_detail.html
@core/templates/core/ci_workflows/repo_detail.html
@core/templates/core/ci_workflows/_steps_table.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update views to filter archived steps and compute change warnings</name>
  <files>core/views/ci_workflows.py</files>
  <action>
**Part A: Filter archived steps in _filter_steps (around line 189)**

In the `_filter_steps` function, add a filter to exclude archived steps by default. Change the initial queryset from:
```python
steps = (
    CIStep.objects.all()
    .select_related("repository")
    ...
)
```
To:
```python
steps = (
    CIStep.objects.filter(status="active")
    .select_related("repository")
    ...
)
```

This ensures the Steps Catalog page only shows active steps. Archived steps are hidden from discovery.

**Part B: Add step change warnings in WorkflowDetailView (around line 646)**

In the `WorkflowDetailView.get()` method, after loading `workflow_steps`, compute whether any steps have changes or are archived. Add these to the context:

After `workflow_steps = workflow.workflow_steps.select_related("step").order_by("order")`:

```python
# Check for step warnings (archived or interface changes)
has_step_warnings = False
for ws in workflow_steps:
    if ws.step.status == "archived" or ws.step.last_change_type == "interface":
        has_step_warnings = True
        break
```

Add `has_step_warnings` to the render context dict:
```python
"has_step_warnings": has_step_warnings,
```

**Part C: Update StepDetailView (around line 273) to pass slug and status to template**

In `StepDetailView.get()`, add the step's slug to the context (it is already available via `step.slug` from the model). No extra queries needed -- the step object already has `slug` and `status` fields. The template will access them directly via `{{ step.slug }}` and `{{ step.status }}`.

No code change needed for StepDetailView itself -- the step model instance already carries these fields. The template changes in Task 2 handle the display.

**Part D: Update StepsRepoDetailView (around line 90) to include archived steps separately**

In `StepsRepoDetailView.get()`, the current code loads steps and groups by phase. Archived steps should be shown in a separate section. After the existing step grouping logic, add:

```python
archived_steps = repo.steps.filter(status="archived").order_by("name")
```

Add to context: `"archived_steps": archived_steps,`

Also update `total_steps` to count only active:
```python
active_steps = repo.steps.filter(status="active")
```
Replace `steps = repo.steps.all().order_by("phase", "name")` with `steps = active_steps.order_by("phase", "name")`.

Keep using `steps` for the phase grouping (now only active), and pass `archived_steps` as separate context.

IMPORTANT: This project uses Alpine.js CSP build. Do NOT use inline scripts, arrow functions, or multi-expression directives. Simple single-expression directives are fine. All UI changes are in static Tailwind classes and Django template tags -- no JavaScript needed for badges.
  </action>
  <verify>
Run `uv run python manage.py check` -- must pass.
Run `uv run python -c "from core.views.ci_workflows import WorkflowDetailView, _filter_steps, StepsRepoDetailView; print('imports OK')"` -- must succeed.
  </verify>
  <done>_filter_steps excludes archived steps. WorkflowDetailView computes has_step_warnings. StepsRepoDetailView separates active and archived steps.</done>
</task>

<task type="auto">
  <name>Task 2: Update templates with archived and change warning badges</name>
  <files>
    core/templates/core/ci_workflows/workflow_detail.html
    core/templates/core/ci_workflows/step_detail.html
    core/templates/core/ci_workflows/repo_detail.html
    core/templates/core/ci_workflows/_steps_table.html
  </files>
  <action>
**Template 1: workflow_detail.html -- Steps tab step warnings**

In the Steps tab section (around line 134-171), add badges for archived and changed steps in each workflow step row. Inside the `{% for ws in workflow_steps %}` loop, after the step name `<span>` element:

After `<span class="text-dark-text font-medium">{{ ws.step.name }}</span>`, add:
```html
{% if ws.step.status == "archived" %}
<span class="ml-2 inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-red-500/20 text-red-400">
    Archived
</span>
{% endif %}
{% if ws.step.last_change_type == "interface" %}
<span class="ml-2 inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-amber-500/20 text-amber-300">
    Step Updated
</span>
{% elif ws.step.last_change_type == "metadata" %}
<span class="ml-2 inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-blue-500/20 text-blue-300">
    Metadata Updated
</span>
{% endif %}
```

Also add a warning banner above the steps list if `has_step_warnings` is true. Before the `{% if workflow_steps %}` check, add:
```html
{% if has_step_warnings %}
<div class="bg-amber-500/10 border border-amber-500/30 rounded-lg p-4 mb-4 flex items-center gap-3">
    <svg class="w-5 h-5 text-amber-400 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z" />
    </svg>
    <div>
        <p class="text-amber-300 text-sm font-medium">Some steps in this workflow have been updated or archived.</p>
        <p class="text-amber-300/70 text-xs mt-0.5">Edit the workflow to review and update affected steps.</p>
    </div>
</div>
{% endif %}
```

**Template 2: step_detail.html -- Archived banner and slug display**

After the header section (after the phase badge, around line 32), add an archived banner:
```html
{% if step.status == "archived" %}
<div class="bg-red-500/10 border border-red-500/30 rounded-lg p-4 mb-6 flex items-center gap-3">
    <svg class="w-5 h-5 text-red-400 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4" />
    </svg>
    <div>
        <p class="text-red-400 text-sm font-medium">This step has been archived.</p>
        <p class="text-red-400/70 text-xs mt-0.5">It was removed from its steps repository. Workflows using this step should be updated.</p>
    </div>
</div>
{% endif %}
```

In the General info card (the `<dl>` section), add a slug row after the Directory row:
```html
{% if step.slug %}
<div>
    <dt class="text-sm text-dark-muted">Slug</dt>
    <dd class="text-dark-text font-mono text-sm">{{ step.slug }}</dd>
</div>
{% endif %}
```

Also add a status row after slug:
```html
<div>
    <dt class="text-sm text-dark-muted">Status</dt>
    <dd>
        {% if step.status == "archived" %}
        <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-red-500/20 text-red-400">Archived</span>
        {% else %}
        <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-green-500/20 text-green-400">Active</span>
        {% endif %}
    </dd>
</div>
```

If `step.file_path` is populated, show it instead of the hardcoded `ci-steps/` prefix for the Directory field:
Replace:
```html
<dd class="text-dark-text font-mono text-sm">ci-steps/{{ step.directory_name }}/</dd>
```
With:
```html
<dd class="text-dark-text font-mono text-sm">{% if step.file_path %}{{ step.file_path }}{% else %}ci-steps/{{ step.directory_name }}/{% endif %}</dd>
```

Also update the source_url construction in StepDetailView.get() to use file_path when available:
In `core/views/ci_workflows.py` StepDetailView (around line 285-289), update:
```python
source_url = ""
if step.commit_sha and step.repository:
    base_url = step.repository.git_url.rstrip("/")
    if base_url.endswith(".git"):
        base_url = base_url[:-4]
    if step.file_path:
        source_url = f"{base_url}/blob/{step.commit_sha}/{step.file_path}"
    else:
        source_url = f"{base_url}/blob/{step.commit_sha}/ci-steps/{step.directory_name}/{engine_file}"
```

**Template 3: repo_detail.html -- Archived steps section**

After the existing "Imported Steps by Phase" section (around line 153, before the `{% if can_delete %}` block), add an archived steps section:
```html
{% if archived_steps %}
<div class="mt-6">
    <h2 class="text-lg font-semibold text-dark-muted mb-3">Archived Steps ({{ archived_steps|length }})</h2>
    <div class="space-y-2">
        {% for step in archived_steps %}
        <a href="{% url 'ci_workflows:step_detail' step_uuid=step.uuid %}"
           class="flex items-center justify-between bg-dark-surface border border-dark-border/50 rounded-lg p-4 hover:border-dark-accent/50 transition-colors block opacity-60">
            <div class="flex items-center gap-2">
                <h4 class="font-medium text-dark-muted">{{ step.name }}</h4>
                <span class="inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-red-500/20 text-red-400">Archived</span>
            </div>
            {% if step.description %}
            <p class="text-sm text-dark-muted">{{ step.description|truncatechars:60 }}</p>
            {% endif %}
        </a>
        {% endfor %}
    </div>
</div>
{% endif %}
```

Also add an "Archived" badge inline on step cards in the main steps-by-phase section. In the existing step cards loop (around line 127-143), after `<h4 class="font-medium text-dark-text">{{ step.name }}</h4>`, add:
```html
{% if step.status == "archived" %}
<span class="ml-2 inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-red-500/20 text-red-400">Archived</span>
{% endif %}
```

(Note: After the view change in Task 1, the main section only shows active steps, so this badge would only appear if we ever show mixed. Since we separated active from archived, this badge is a safety net.)

**Template 4: _steps_table.html -- Archived badge in catalog rows**

In the first `<td>` cell (name column, around line 9), after the step name span, add:
```html
{% if step.status == "archived" %}
<span class="ml-2 inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-red-500/20 text-red-400">Archived</span>
{% endif %}
```

Since _filter_steps now excludes archived steps, this badge is a safety net. It would show if the filter is ever changed to include archived steps (e.g., for an admin "show all" toggle in the future).
  </action>
  <verify>
Run `uv run python manage.py check` -- must pass.
Visually inspect: Run `make build && make run`, navigate to a workflow detail page, steps catalog, and step detail page. Verify no template rendering errors. (If no steps exist, the templates should still render cleanly with empty state.)
  </verify>
  <done>Workflow detail shows step update/archived warnings. Step detail shows archived banner and slug. Repo detail separates active and archived steps. Steps catalog table has archived badge support.</done>
</task>

</tasks>

<verification>
- `uv run python manage.py check` passes
- `make build` succeeds
- _filter_steps excludes archived steps (status="active" filter)
- WorkflowDetailView passes has_step_warnings to context
- workflow_detail.html shows warning banner and per-step badges
- step_detail.html shows archived banner, slug, and status
- repo_detail.html shows archived steps in separate section
- _steps_table.html has archived badge support
- No inline scripts added (CSP compatible)
</verification>

<success_criteria>
- Archived steps hidden from steps catalog
- Workflow detail shows warning when steps have interface changes
- Workflow detail shows archived badge on archived steps
- Step detail page displays slug, status, and file_path
- Repo detail page separates active and archived steps
- All templates render without errors (including empty state)
</success_criteria>

<output>
After completion, create `.planning/phases/06.4-ci-step-identity-and-change-tracking/06.4-03-SUMMARY.md`
</output>
