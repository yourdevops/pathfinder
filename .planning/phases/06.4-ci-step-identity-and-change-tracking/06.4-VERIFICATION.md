---
phase: 06.4-ci-step-identity-and-change-tracking
verified: 2026-02-16T17:23:57Z
status: passed
score: 8/8 success criteria verified
---

# Phase 06.4: CI Step Identity and Change Tracking Verification Report

**Phase Goal:** Steps have proper identity (globally unique slug per engine), per-file versioning, SHA-based change detection, and soft-delete via archival instead of hard delete

**Verified:** 2026-02-16T17:23:57Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria from ROADMAP.md)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CIStep has slug field with UniqueConstraint on (engine, slug) across entire catalog | ✓ VERIFIED | Model has `slug` field + `UniqueConstraint(fields=["engine", "slug"], name="unique_step_slug_per_engine")` in core/models.py:630-665 |
| 2 | scan_steps_repository computes per-file commit SHA via git log (not repo HEAD) | ✓ VERIFIED | Line 347 in core/tasks.py: `per_file_sha = repo_obj.git.log("-1", "--format=%H", "--", file_path).strip()` |
| 3 | Unchanged steps (same SHA) are skipped during scan | ✓ VERIFIED | Lines 374-380 in core/tasks.py: SHA comparison with early return on match, stats["unchanged"] incremented |
| 4 | Changed steps are classified as interface or metadata change | ✓ VERIFIED | `_classify_change()` helper (lines 228-252) returns "interface" or "metadata" based on field comparison; applied at line 391 |
| 5 | Steps removed from repository are archived, not deleted | ✓ VERIFIED | Lines 433-438 in core/tasks.py: soft-delete via `update(status="archived")` instead of `.delete()` |
| 6 | Archived steps are filtered out of workflow composer step picker | ✓ VERIFIED | Line 91 in core/ci_manifest.py: `get_compatible_steps` filters `status="active"` |
| 7 | Workflow detail shows warning badges for steps with interface changes or archived status | ✓ VERIFIED | Lines 659-663 compute `has_step_warnings`; templates show badges at workflow_detail.html:163-175 |
| 8 | cleanup_archived_steps task safely removes unreferenced archived steps | ✓ VERIFIED | Lines 472-483 in core/tasks.py: excludes steps with CIWorkflowStep references before deleting |

**Score:** 8/8 truths verified

### Required Artifacts (from 06.4-03-PLAN must_haves)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/views/ci_workflows.py` | Updated _filter_steps to exclude archived, WorkflowDetailView computes has_step_warnings | ✓ VERIFIED | Line 202: `filter(status="active")`; Lines 659-663: has_step_warnings computation; Line 718: passed to context |
| `core/templates/core/ci_workflows/workflow_detail.html` | Step update and archived badges in Steps tab | ✓ VERIFIED | Lines 163-175: archived/interface/metadata badges per step; Lines 138-145: warning banner when has_step_warnings |
| `core/templates/core/ci_workflows/step_detail.html` | Archived banner and slug display | ✓ VERIFIED | Lines 34-42: archived banner; Lines 65-70: slug display; Lines 72-77: status badge |
| `core/templates/core/ci_workflows/repo_detail.html` | Archived badge on step cards | ✓ VERIFIED | Lines 133-135: archived badge in main section; Lines 160-180: separate archived steps section |
| `core/templates/core/ci_workflows/_steps_table.html` | Archived badge in catalog table rows | ✓ VERIFIED | Lines 10-12: archived badge in name column |

**Additional Core Artifacts (from 06.4-01 and 06.4-02):**

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/models.py` (CIStep) | slug, status, file_path, last_change_type fields with UniqueConstraint | ✓ VERIFIED | Lines 630-646: all 4 fields present; Lines 661-666: UniqueConstraint on (engine, slug) |
| `core/git_utils.py` | clone_repo_full function | ✓ VERIFIED | Lines 166-198: full single-branch clone for per-file SHA |
| `core/tasks.py` | Rewritten scan_steps_repository with per-file SHA, slug derivation, change classification, archival | ✓ VERIFIED | Lines 256-456: full implementation with all required logic |
| `core/tasks.py` | cleanup_archived_steps task | ✓ VERIFIED | Lines 472-483: safe deletion of unreferenced archived steps |
| `core/ci_steps.py` | discover_steps returns file_path | ✓ VERIFIED | Line 53: file_path in returned dict |
| `core/ci_manifest.py` | get_compatible_steps filters archived steps | ✓ VERIFIED | Line 91: `filter(status="active")` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| core/views/ci_workflows.py | core/models.py | CIStep.status and CIStep.last_change_type fields | ✓ WIRED | _filter_steps uses `status="active"` (line 202); WorkflowDetailView checks `ws.step.status` and `ws.step.last_change_type` (lines 661-662) |
| core/templates/core/ci_workflows/workflow_detail.html | core/views/ci_workflows.py | has_step_warnings and step.last_change_type context | ✓ WIRED | Template accesses `has_step_warnings` (line 137) and `ws.step.last_change_type` (lines 168, 172); context provides both (line 718) |
| scan_steps_repository | clone_repo_full | Full clone for per-file SHA | ✓ WIRED | Lines 298-302 call clone_repo_full; line 347 uses repo_obj.git.log for per-file SHA |
| scan_steps_repository | _derive_step_slug | Slug derivation | ✓ WIRED | Line 352 calls `_derive_step_slug(step_info["name"], dir_name)` |
| scan_steps_repository | _classify_change | Change type detection | ✓ WIRED | Line 391 calls `_classify_change(existing_same_repo, new_fields)` |
| get_compatible_steps | CIStep.status | Archived step filtering | ✓ WIRED | Line 91 filters `status="active"` |

### Requirements Coverage

No requirements explicitly mapped to phase 06.4 in REQUIREMENTS.md. Phase addresses GAP-01, GAP-02, GAP-03, GAP-04 from docs/ci-workflows/REMEDIATION.md.

### Anti-Patterns Found

**None.** No TODOs, FIXMEs, placeholders, or stub implementations found in modified files. Only legitimate HTML input placeholders detected.

### Commits Verified

All commits from SUMMARYs exist and match the documented work:

**Plan 01 (Model and Clone):**
- `a1f81fe` - CIStep model fields (slug, status, file_path, last_change_type)
- `1c0e36f` - clone_repo_full function

**Plan 02 (Scan Logic):**
- `93fe556` - Rewrite scan_steps_repository with per-file SHA, slug, archival
- `91b053a` - Filter archived steps in get_compatible_steps

**Plan 03 (Views and Templates):**
- `7fe5de9` - Update views to filter archived steps and compute change warnings
- `d0dae8f` - Add archived and change warning badges to templates

### Django Health Check

```
$ uv run python manage.py check
System check identified no issues (0 silenced).
```

✓ No Django errors or warnings

## Summary

**All 8 success criteria verified.** Phase 06.4 goal fully achieved.

### What Works:

1. **Step Identity:** Slug field with catalog-wide uniqueness per engine enforced by UniqueConstraint
2. **Per-file Versioning:** git log computes commit SHA per step file (not repo HEAD)
3. **SHA-based Change Detection:** Unchanged steps skipped; changed steps classified as interface or metadata changes
4. **Soft-delete Archival:** Steps removed from repo are archived (not deleted); cleanup task safely removes unreferenced archived steps
5. **UI Filtering:** Archived steps excluded from steps catalog and workflow composer step picker
6. **UI Warnings:** Workflow detail shows warning banner and per-step badges for archived/changed steps
7. **Step Detail:** Shows archived banner, slug, status, and file_path
8. **Repo Detail:** Separates active and archived steps into distinct sections

### Test Coverage:

- All must_haves from 06.4-03-PLAN verified (templates and views)
- All artifacts from 06.4-01-PLAN verified (model fields and clone utility)
- All artifacts from 06.4-02-PLAN verified (scan logic, cleanup task, filtering)
- All key links verified (models ↔ views ↔ templates)
- No anti-patterns detected
- Django check passes

### Gaps:

**None.** All required functionality implemented and wired correctly.

---

_Verified: 2026-02-16T17:23:57Z_
_Verifier: Claude (gsd-verifier)_
