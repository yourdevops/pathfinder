---
phase: 07-implement-unified-environment-variables-management
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - core/utils.py
  - core/models.py
  - core/tests/test_env_vars.py
autonomous: true
requirements:
  - DPLY-01
  - DPLY-02
  - DPLY-04

must_haves:
  truths:
    - "resolve_env_vars returns system PTF_* vars for any context"
    - "resolve_env_vars merges project, service, and environment vars in cascade order"
    - "Locked variables cannot be overridden by downstream levels"
    - "Description is inherited from upstream unless downstream provides its own"
    - "check_deployment_gate detects empty values and reports which level to fix"
    - "SERVICE_NAME references replaced with PTF_SERVICE throughout codebase"
  artifacts:
    - path: "core/utils.py"
      provides: "resolve_env_vars() and check_deployment_gate() functions"
      contains: "def resolve_env_vars"
    - path: "core/tests/test_env_vars.py"
      provides: "Test suite for cascade resolution and deployment gate"
      contains: "class TestResolveEnvVars"
  key_links:
    - from: "core/utils.py"
      to: "core/models.py"
      via: "resolve_env_vars reads env_vars JSONField from Project, Service, Environment"
      pattern: "project\\.env_vars"
---

<objective>
Implement the cascade resolution utility function and deployment gate check, with comprehensive tests. Migrate variable shape to include `description` field and rename SERVICE_NAME to PTF_SERVICE.

Purpose: The resolution function is the foundation for all env var display and deployment readiness checking. TDD ensures correctness of cascade logic (lock enforcement, description inheritance, system-injected vars) before building UI.
Output: `resolve_env_vars()` and `check_deployment_gate()` in `core/utils.py`, test suite, updated variable shape.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/env-vars.md
@core/models.py
@core/utils.py
@core/views/services.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — resolve_env_vars and check_deployment_gate</name>
  <files>core/utils.py, core/tests/test_env_vars.py</files>
  <action>
**RED phase:** Create `core/tests/test_env_vars.py` with test cases covering:

1. `TestResolveEnvVars`:
   - `test_project_only_context` — resolve with just a project: returns PTF_PROJECT (system, locked) + project vars. Verify source="system" and source="project".
   - `test_project_environment_context` — resolve with project + environment: returns PTF_PROJECT + PTF_ENVIRONMENT + merged project + environment vars. Environment overrides project value.
   - `test_full_cascade` — resolve with project + service + environment: returns all 3 PTF_* vars + merged cascade. Verify correct override order.
   - `test_locked_var_prevents_override` — project locks a var, service tries to override: resolved value is the project's locked value, not service's.
   - `test_locked_var_at_service_level` — service locks a var, environment tries to override: resolved value is service's locked value.
   - `test_system_vars_always_locked` — PTF_* vars cannot be overridden by any level.
   - `test_description_inheritance` — project sets description, service overrides value but no description: resolved var has project's description. Service overrides both value and description: resolved var has service's description.
   - `test_empty_value_not_lockable` — a variable with empty value should have lock=False regardless of what was stored (enforced in resolution or CRUD).
   - `test_results_sorted_by_key` — output is alphabetically sorted by key.

2. `TestCheckDeploymentGate`:
   - `test_all_values_present_passes` — returns (True, []).
   - `test_empty_value_blocks` — returns (False, list of empty vars) with correct source info.
   - `test_system_vars_excluded_from_gate` — system PTF_* vars are never flagged as empty (they always have values).

Use Django's `TestCase`. Create mock Project/Service/Environment objects using the ORM (create actual test instances with env_vars JSONField populated).

**GREEN phase:** Implement in `core/utils.py`:

```python
def resolve_env_vars(project, service=None, environment=None):
```

Follow the cascade logic from `docs/env-vars.md`:
1. Inject PTF_PROJECT always. Inject PTF_SERVICE if service provided. Inject PTF_ENVIRONMENT if environment provided. All system vars: source="system", locked_by="system", lock=True.
2. Layer project vars: source="project", locked_by="project" if lock=True.
3. Layer service vars (if provided): skip if key is locked upstream. Inherit description from upstream if downstream description is empty. source="service".
4. Layer environment vars (if provided): skip if key is locked upstream. Inherit description. source="environment", locked_by=None (environment is terminal).
5. Return sorted list of dicts: `{key, value, lock, description, source, locked_by}`.

```python
def check_deployment_gate(resolved_vars):
```

Filter resolved vars for empty values (excluding system vars). Return tuple: `(is_ready: bool, empty_vars: list)`.

**REFACTOR:** Clean up if needed. Ensure no duplication.

Run tests: `uv run python manage.py test core.tests.test_env_vars`
  </action>
  <verify>
`uv run python manage.py test core.tests.test_env_vars` — all tests pass.
  </verify>
  <done>resolve_env_vars correctly implements the full cascade with lock enforcement, description inheritance, and system-injected PTF_* variables. check_deployment_gate correctly identifies empty values. All 12+ test cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate variable shape and rename SERVICE_NAME to PTF_SERVICE</name>
  <files>core/models.py, core/views/services.py, core/templates/core/services/wizard/step_configuration.html</files>
  <action>
1. **Variable shape migration** — no database migration needed since env_vars is a JSONField storing a list of dicts. Existing data with `{key, value, lock}` shape is backwards-compatible; missing `description` field defaults to `""` in the resolution function (already handled by `.get("description", "")`).

2. **Update `Service.get_merged_env_vars()`** in `core/models.py`:
   - Add a deprecation comment: `# DEPRECATED: Use core.utils.resolve_env_vars() instead. Kept for backwards compat during migration.`
   - Update it to pass through to `resolve_env_vars(self.project, service=self)` so existing callers still work during transition.

3. **Rename SERVICE_NAME to PTF_SERVICE** in `core/views/services.py`:
   - In `ServiceCreateWizard.get_form_initial()` (around line 183): change `"key": "SERVICE_NAME"` to `"key": "PTF_SERVICE"`.
   - In `ServiceCreateWizard.done()` (around line 329-331): change `SERVICE_NAME` references to `PTF_SERVICE`.
   - Update the `get_merged_env_vars` call sites in `ServiceDetailView` and `ServiceSettingsView` to use `resolve_env_vars()` from `core/utils.py` instead.

4. **Update wizard template** `core/templates/core/services/wizard/step_configuration.html`:
   - Change any `SERVICE_NAME` text to `PTF_SERVICE`.

5. Run `uv run python manage.py check` to verify no Django errors.
  </action>
  <verify>
`uv run python manage.py check` passes. `grep -r "SERVICE_NAME" core/` returns zero results (only PTF_SERVICE). `uv run python manage.py test core.tests.test_env_vars` still passes.
  </verify>
  <done>Variable shape supports description field. SERVICE_NAME fully replaced with PTF_SERVICE. Service.get_merged_env_vars() delegates to resolve_env_vars(). Django check passes.</done>
</task>

</tasks>

<verification>
1. `uv run python manage.py test core.tests.test_env_vars` — all tests pass
2. `uv run python manage.py check` — no Django errors
3. `grep -r "SERVICE_NAME" core/` — returns no results
4. `resolve_env_vars()` function exists in `core/utils.py` and handles all documented cascade scenarios
</verification>

<success_criteria>
- resolve_env_vars() correctly resolves cascade for all 4 contexts (project-only, project+env, project+service, full cascade)
- Lock enforcement prevents downstream override
- System PTF_* variables are injected and always locked
- Description inheritance works per spec
- check_deployment_gate() detects empty values
- SERVICE_NAME renamed to PTF_SERVICE everywhere
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-implement-unified-environment-variables-management/07-01-SUMMARY.md`
</output>
