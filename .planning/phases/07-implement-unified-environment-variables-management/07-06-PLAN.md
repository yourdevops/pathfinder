---
phase: 07-implement-unified-environment-variables-management
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - core/utils.py
  - core/tests/test_env_vars.py
  - core/templates/core/env_vars/_env_var_row.html
  - core/templates/core/services/wizard/step_configuration.html
autonomous: true
gap_closure: true
requirements:
  - DPLY-04

must_haves:
  truths:
    - "resolve_env_vars() returns variables sorted by source hierarchy (System > Project > Service > Environment) then alphabetically within each group"
    - "No visible source badge on env var rows — source information appears only in the hover tooltip"
  artifacts:
    - path: "core/utils.py"
      provides: "Cascade resolution with source-priority sort"
      contains: "source_priority"
    - path: "core/templates/core/env_vars/_env_var_row.html"
      provides: "Display row without visible source badge"
    - path: "core/tests/test_env_vars.py"
      provides: "Test verifying sort order by source priority"
  key_links:
    - from: "core/utils.py"
      to: "core/templates/core/env_vars/_env_var_container.html"
      via: "resolved_vars passed to template already in correct order"
      pattern: "source_priority"
---

<objective>
Fix two presentation gaps from human verification: (1) sort order and (2) visible source badges.

Purpose: Variables must display in cascade hierarchy order (System > Project > Service > Environment) and source information must appear only in the tooltip, not as a visible colored badge.
Output: Corrected sort logic in resolve_env_vars() and badge-free row template.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-implement-unified-environment-variables-management/07-01-SUMMARY.md
@.planning/phases/07-implement-unified-environment-variables-management/07-02-SUMMARY.md
@core/utils.py
@core/templates/core/env_vars/_env_var_row.html
@core/templates/core/services/wizard/step_configuration.html
@core/tests/test_env_vars.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix resolve_env_vars sort order and update tests</name>
  <files>core/utils.py, core/tests/test_env_vars.py</files>
  <action>
In `core/utils.py`, change the sort on line 186 from:

```python
return sorted(merged.values(), key=lambda v: v["key"])
```

to sort by (source_priority, key):

```python
source_priority = {"system": 0, "project": 1, "service": 2, "environment": 3}
return sorted(merged.values(), key=lambda v: (source_priority.get(v["source"], 99), v["key"]))
```

This ensures System PTF_* variables always appear first, followed by Project vars, then Service, then Environment, with alphabetical ordering within each source group.

In `core/tests/test_env_vars.py`, add a test case that verifies the sort order:
- Create a project with vars B_VAR and A_VAR
- Call resolve_env_vars(project)
- Assert the result order is: PTF_PROJECT (system), then A_VAR (project), then B_VAR (project)
- The system var must come before project vars regardless of alphabetical key ordering

Also verify any existing tests still pass — the sort change should not break existing assertions since system vars were already alphabetically before project vars in most cases, but confirm.
  </action>
  <verify>
Run `uv run python manage.py test core.tests.test_env_vars` — all tests pass including the new sort order test.
  </verify>
  <done>resolve_env_vars() returns variables sorted by (source_priority, key) where system=0, project=1, service=2, environment=3. Test confirms system vars always precede project vars.</done>
</task>

<task type="auto">
  <name>Task 2: Remove visible source badges from env var rows</name>
  <files>core/templates/core/env_vars/_env_var_row.html, core/templates/core/services/wizard/step_configuration.html</files>
  <action>
In `core/templates/core/env_vars/_env_var_row.html`, remove the entire source badge block (lines 97-104):

```html
{# Source badge #}
<span class="flex-shrink-0 px-2 py-0.5 text-xs rounded
    {% if var.source == 'system' %}bg-purple-500/20 text-purple-400
    {% elif var.source == 'project' %}bg-blue-500/20 text-blue-400
    {% elif var.source == 'service' %}bg-teal-500/20 text-teal-400
    {% else %}bg-green-500/20 text-green-400{% endif %}">
    {{ var.source|title }}
</span>
```

Delete these 7 lines entirely. The source information is already present in the tooltip on hover (line 83: `From: {{ var.source }}`), which is the correct behavior per user decision.

In `core/templates/core/services/wizard/step_configuration.html`, remove the "System" badge from the PTF_* vars section (line 85):

```html
<span class="ml-auto text-xs px-2 py-0.5 rounded bg-purple-500/20 text-purple-400">System</span>
```

Delete this line. The wizard PTF vars are already visually distinct (read-only, locked icon, muted styling), and the user rejected visible source badges everywhere.
  </action>
  <verify>
Run `uv run python manage.py check` to verify templates have no syntax errors. Visually confirm (or grep) that no `var.source|title` badge span exists in the env var templates.
  </verify>
  <done>No visible source badges appear on any env var rows. Source information is accessible only via the hover tooltip on the variable key.</done>
</task>

</tasks>

<verification>
1. `uv run python manage.py test core.tests.test_env_vars` — all tests pass
2. `uv run python manage.py check` — no errors
3. Grep `_env_var_row.html` for `source|title` — no matches (badge removed)
4. Grep `step_configuration.html` for `bg-purple-500/20` — no matches (wizard badge removed)
</verification>

<success_criteria>
- Variables in resolved list are ordered System > Project > Service > Environment, then alphabetically within each group
- No visible source badge anywhere in the env var UI
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-implement-unified-environment-variables-management/07-06-SUMMARY.md`
</output>
