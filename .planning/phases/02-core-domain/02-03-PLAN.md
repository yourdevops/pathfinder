---
phase: 02-core-domain
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - core/views/projects.py
  - core/templates/core/projects/detail.html
  - core/templates/core/projects/_services_tab.html
  - core/templates/core/projects/_environments_tab.html
  - core/templates/core/projects/_members_tab.html
  - core/templates/core/projects/_settings_tab.html
  - core/templates/core/components/nav_project.html
  - core/context_processors.py
  - core/urls.py
  - core/permissions.py
autonomous: true

must_haves:
  truths:
    - "Project detail page shows project name and description in header"
    - "Four tabs visible: Services, Environments, Members, Settings"
    - "Clicking tab loads content via HTMX without full page reload"
    - "URL changes when switching tabs (hx-push-url)"
    - "When viewing project, sidebar shows project-scoped navigation with back button"
    - "Viewers can see project but not access Settings tab"
  artifacts:
    - path: "core/views/projects.py"
      provides: "ProjectDetailView with tab handling"
      contains: "class ProjectDetailView"
    - path: "core/templates/core/projects/detail.html"
      provides: "Project detail with tab navigation"
      contains: "hx-get"
    - path: "core/templates/core/components/nav_project.html"
      provides: "Project-scoped sidebar navigation"
      contains: "Back to Projects"
    - path: "core/permissions.py"
      provides: "Project permission helpers and mixins"
      contains: "get_user_project_role"
  key_links:
    - from: "detail.html tabs"
      to: "ProjectDetailView"
      via: "hx-get with tab param"
      pattern: "hx-get.*tab="
    - from: "nav_project.html"
      to: "projects:list"
      via: "back button link"
      pattern: "url 'projects:list'"
---

<objective>
Implement project detail page with HTMX tabs and context-replacing sidebar navigation

Purpose: Create the primary project workspace view with tabbed interface (Services, Environments, Members, Settings) and AWS Console-style navigation that replaces main nav when viewing a project.

Output: Functional project detail with tab navigation, project-scoped sidebar
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-domain/02-CONTEXT.md
@.planning/phases/02-core-domain/02-RESEARCH.md
@.planning/phases/02-core-domain/02-02-SUMMARY.md
@core/views/projects.py
@core/templates/core/components/nav.html
@core/context_processors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project permission helpers and mixins</name>
  <files>core/permissions.py, core/views/projects.py</files>
  <action>
**Create core/permissions.py:**

```python
"""Project-level permission helpers and view mixins."""
from django.shortcuts import redirect, get_object_or_404
from django.contrib import messages
from core.models import Project, ProjectMembership, GroupMembership


def has_system_role(user, role):
    """Check if user has a system role via group membership."""
    if not user.is_authenticated:
        return False
    return GroupMembership.objects.filter(
        user=user,
        group__status='active',
        group__system_roles__contains=role
    ).exists()


def get_user_project_role(user, project):
    """
    Return highest project role for user, or None if no access.

    Role priority: owner > contributor > viewer
    Admin and Operator system roles get owner-level access.
    """
    ROLE_PRIORITY = {'owner': 3, 'contributor': 2, 'viewer': 1}

    # System admins and operators get owner access
    if has_system_role(user, 'admin') or has_system_role(user, 'operator'):
        return 'owner'

    # Get all memberships through user's groups
    user_group_ids = GroupMembership.objects.filter(
        user=user,
        group__status='active'
    ).values_list('group_id', flat=True)

    memberships = ProjectMembership.objects.filter(
        project=project,
        group_id__in=user_group_ids
    )

    if not memberships.exists():
        return None

    # Return highest role
    roles = [m.project_role for m in memberships]
    return max(roles, key=lambda r: ROLE_PRIORITY.get(r, 0))


def can_access_project(user, project, required_role='viewer'):
    """Check if user has at least the required role."""
    ROLE_HIERARCHY = ['viewer', 'contributor', 'owner']
    user_role = get_user_project_role(user, project)

    if not user_role:
        return False

    return ROLE_HIERARCHY.index(user_role) >= ROLE_HIERARCHY.index(required_role)


class ProjectPermissionMixin:
    """Base mixin for project-scoped views."""
    required_role = 'viewer'  # Override in subclass

    def dispatch(self, request, *args, **kwargs):
        self.project = get_object_or_404(Project, uuid=kwargs.get('project_uuid'))
        self.user_project_role = get_user_project_role(request.user, self.project)

        if not self.user_project_role:
            messages.error(request, 'You do not have access to this project.')
            return redirect('projects:list')

        ROLE_HIERARCHY = ['viewer', 'contributor', 'owner']
        if ROLE_HIERARCHY.index(self.user_project_role) < ROLE_HIERARCHY.index(self.required_role):
            messages.error(request, 'You do not have permission for this action.')
            return redirect('projects:detail', project_uuid=self.project.uuid)

        return super().dispatch(request, *args, **kwargs)

    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['project'] = self.project
        context['user_project_role'] = self.user_project_role
        return context


class ProjectViewerMixin(ProjectPermissionMixin):
    required_role = 'viewer'


class ProjectContributorMixin(ProjectPermissionMixin):
    required_role = 'contributor'


class ProjectOwnerMixin(ProjectPermissionMixin):
    required_role = 'owner'
```

Update core/views/projects.py imports to use these helpers.
  </action>
  <verify>python -c "from core.permissions import get_user_project_role, ProjectViewerMixin; print('OK')"</verify>
  <done>Permission helpers created for project role checking</done>
</task>

<task type="auto">
  <name>Task 2: Implement project detail view with HTMX tabs</name>
  <files>core/views/projects.py, core/urls.py, core/templates/core/projects/detail.html, core/templates/core/projects/_services_tab.html, core/templates/core/projects/_environments_tab.html, core/templates/core/projects/_members_tab.html, core/templates/core/projects/_settings_tab.html</files>
  <action>
**Add to core/views/projects.py:**

```python
from django.views.decorators.vary import vary_on_headers
from django.utils.decorators import method_decorator

@method_decorator(vary_on_headers("HX-Request"), name='dispatch')
class ProjectDetailView(LoginRequiredMixin, ProjectViewerMixin, TemplateView):
    """Project detail with HTMX tab navigation."""

    def get_template_names(self):
        tab = self.request.GET.get('tab', 'services')
        if self.request.htmx:
            return [f'core/projects/_{tab}_tab.html']
        return ['core/projects/detail.html']

    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['active_tab'] = self.request.GET.get('tab', 'services')

        # Tab-specific data
        tab = context['active_tab']
        if tab == 'environments':
            context['environments'] = self.project.environments.filter(
                status='active'
            ).order_by('order', 'name')
        elif tab == 'members':
            context['memberships'] = self.project.memberships.select_related('group').order_by('project_role')
        # services tab will be empty until Phase 5
        # settings tab shows project settings form

        return context
```

**Add to core/urls.py projects_patterns:**
```python
path('<uuid:project_uuid>/', ProjectDetailView.as_view(), name='detail'),
```

**Create core/templates/core/projects/detail.html:**
```html
{% extends "base.html" %}

{% block title %}{{ project.name }} - Pathfinder{% endblock %}

{% block content %}
<div class="p-6">
    <!-- Project Header -->
    <div class="mb-6">
        <h1 class="text-2xl font-bold text-dark-text">{{ project.name }}</h1>
        {% if project.description %}
        <p class="text-dark-muted mt-1">{{ project.description }}</p>
        {% endif %}
    </div>

    <!-- Tab Navigation -->
    <nav class="flex space-x-1 border-b border-dark-border mb-6">
        <button hx-get="{% url 'projects:detail' project.uuid %}?tab=services"
                hx-target="#tab-content"
                hx-swap="innerHTML"
                hx-push-url="true"
                class="px-4 py-2 text-sm font-medium rounded-t-lg transition-colors
                       {% if active_tab == 'services' %}bg-dark-surface text-dark-text border-b-2 border-dark-accent{% else %}text-dark-muted hover:text-dark-text hover:bg-dark-surface/50{% endif %}">
            Services
        </button>
        <button hx-get="{% url 'projects:detail' project.uuid %}?tab=environments"
                hx-target="#tab-content"
                hx-swap="innerHTML"
                hx-push-url="true"
                class="px-4 py-2 text-sm font-medium rounded-t-lg transition-colors
                       {% if active_tab == 'environments' %}bg-dark-surface text-dark-text border-b-2 border-dark-accent{% else %}text-dark-muted hover:text-dark-text hover:bg-dark-surface/50{% endif %}">
            Environments
        </button>
        <button hx-get="{% url 'projects:detail' project.uuid %}?tab=members"
                hx-target="#tab-content"
                hx-swap="innerHTML"
                hx-push-url="true"
                class="px-4 py-2 text-sm font-medium rounded-t-lg transition-colors
                       {% if active_tab == 'members' %}bg-dark-surface text-dark-text border-b-2 border-dark-accent{% else %}text-dark-muted hover:text-dark-text hover:bg-dark-surface/50{% endif %}">
            Members
        </button>
        {% if user_project_role == 'owner' %}
        <button hx-get="{% url 'projects:detail' project.uuid %}?tab=settings"
                hx-target="#tab-content"
                hx-swap="innerHTML"
                hx-push-url="true"
                class="px-4 py-2 text-sm font-medium rounded-t-lg transition-colors
                       {% if active_tab == 'settings' %}bg-dark-surface text-dark-text border-b-2 border-dark-accent{% else %}text-dark-muted hover:text-dark-text hover:bg-dark-surface/50{% endif %}">
            Settings
        </button>
        {% endif %}
    </nav>

    <!-- Tab Content -->
    <div id="tab-content">
        {% include "core/projects/_"|add:active_tab|add:"_tab.html" %}
    </div>
</div>
{% endblock %}
```

**Create tab partial templates:**

_services_tab.html:
```html
<div class="text-center py-12">
    <svg class="w-16 h-16 mx-auto text-dark-muted mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
    </svg>
    <h3 class="text-lg font-medium text-dark-text mb-2">No services yet</h3>
    <p class="text-dark-muted mb-4">Services will appear here once you create them in Phase 5.</p>
</div>
```

_environments_tab.html:
```html
<div>
    <div class="flex justify-between items-center mb-4">
        <h3 class="text-lg font-medium text-dark-text">Environments</h3>
        {% if user_project_role == 'owner' %}
        <a href="{% url 'projects:environment_create' project.uuid %}"
           class="px-4 py-2 bg-dark-accent hover:bg-dark-accent/80 text-white rounded-lg transition-colors text-sm">
            Add Environment
        </a>
        {% endif %}
    </div>

    {% if environments %}
    <div class="space-y-2">
        {% for env in environments %}
        <div class="flex items-center justify-between p-4 bg-dark-surface border border-dark-border rounded-lg
                    {% if env.is_production %}border-l-4 border-l-amber-500{% endif %}">
            <div class="flex items-center gap-3">
                <span class="font-medium text-dark-text">{{ env.name }}</span>
                {% if env.is_default %}
                <span class="px-2 py-0.5 text-xs bg-dark-accent/20 text-dark-accent rounded">Default</span>
                {% endif %}
                {% if env.is_production %}
                <span class="px-2 py-0.5 text-xs bg-amber-500/20 text-amber-400 rounded">Production</span>
                {% endif %}
            </div>
            {% if user_project_role == 'owner' %}
            <a href="{% url 'projects:environment_detail' project.uuid env.uuid %}"
               class="text-dark-muted hover:text-dark-text transition-colors">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
            </a>
            {% endif %}
        </div>
        {% endfor %}
    </div>
    {% else %}
    <div class="text-center py-8 bg-dark-surface border border-dark-border rounded-lg">
        <p class="text-dark-muted mb-4">No environments configured yet.</p>
        {% if user_project_role == 'owner' %}
        <a href="{% url 'projects:environment_create' project.uuid %}"
           class="text-dark-accent hover:underline">Add your first environment</a>
        {% endif %}
    </div>
    {% endif %}
</div>
```

_members_tab.html:
```html
<div>
    <div class="flex justify-between items-center mb-4">
        <h3 class="text-lg font-medium text-dark-text">Project Members</h3>
        {% if user_project_role == 'owner' %}
        <button hx-get="{% url 'projects:add_member_modal' project.uuid %}"
                hx-target="body"
                hx-swap="beforeend"
                class="px-4 py-2 bg-dark-accent hover:bg-dark-accent/80 text-white rounded-lg transition-colors text-sm">
            Add Group
        </button>
        {% endif %}
    </div>

    {% regroup memberships by project_role as role_groups %}
    {% for role_group in role_groups %}
    <div class="mb-6">
        <h4 class="text-sm font-semibold text-dark-muted uppercase mb-2">
            {{ role_group.grouper|title }}s
        </h4>
        <div class="space-y-2">
            {% for membership in role_group.list %}
            <div class="flex items-center justify-between p-3 bg-dark-surface border border-dark-border rounded-lg">
                <span class="text-dark-text">{{ membership.group.name }}</span>
                {% if user_project_role == 'owner' %}
                <button hx-delete="{% url 'projects:remove_member' project.uuid membership.group.uuid %}"
                        hx-confirm="Remove {{ membership.group.name }} from this project?"
                        hx-target="#tab-content"
                        hx-swap="innerHTML"
                        class="text-dark-muted hover:text-red-400 transition-colors">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
                {% endif %}
            </div>
            {% endfor %}
        </div>
    </div>
    {% empty %}
    <div class="text-center py-8 bg-dark-surface border border-dark-border rounded-lg">
        <p class="text-dark-muted">No groups assigned to this project.</p>
        {% if user_project_role == 'owner' %}
        <p class="text-dark-muted text-sm mt-2">Add groups to grant access.</p>
        {% endif %}
    </div>
    {% endfor %}
</div>
```

_settings_tab.html:
```html
<div class="max-w-2xl">
    <h3 class="text-lg font-medium text-dark-text mb-4">Project Settings</h3>

    <!-- Basic Info Section -->
    <div class="bg-dark-surface border border-dark-border rounded-lg p-6 mb-6">
        <h4 class="text-sm font-semibold text-dark-muted uppercase mb-4">Basic Information</h4>
        <form hx-post="{% url 'projects:update' project.uuid %}"
              hx-target="#tab-content"
              hx-swap="innerHTML">
            {% csrf_token %}
            <div class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-dark-text mb-1">Name</label>
                    <input type="text" name="name" value="{{ project.name }}" readonly
                           class="w-full px-3 py-2 bg-dark-bg border border-dark-border rounded-lg text-dark-muted cursor-not-allowed">
                    <p class="text-xs text-dark-muted mt-1">Project names cannot be changed after creation.</p>
                </div>
                <div>
                    <label class="block text-sm font-medium text-dark-text mb-1">Description</label>
                    <textarea name="description" rows="3"
                              class="w-full px-3 py-2 bg-dark-bg border border-dark-border rounded-lg text-dark-text focus:outline-none focus:ring-2 focus:ring-dark-accent">{{ project.description }}</textarea>
                </div>
                <div class="flex justify-end">
                    <button type="submit"
                            class="px-4 py-2 bg-dark-accent hover:bg-dark-accent/80 text-white rounded-lg transition-colors">
                        Save Changes
                    </button>
                </div>
            </div>
        </form>
    </div>

    <!-- Environment Variables Section (placeholder - will be implemented in Plan 04) -->
    <div class="bg-dark-surface border border-dark-border rounded-lg p-6 mb-6">
        <h4 class="text-sm font-semibold text-dark-muted uppercase mb-4">Environment Variables</h4>
        <p class="text-dark-muted text-sm">Project-level environment variables will be configured here.</p>
    </div>

    <!-- Danger Zone -->
    <div class="bg-dark-surface border border-red-500/30 rounded-lg p-6">
        <h4 class="text-sm font-semibold text-red-400 uppercase mb-4">Danger Zone</h4>
        <div class="flex items-center justify-between">
            <div>
                <p class="text-dark-text font-medium">Archive this project</p>
                <p class="text-dark-muted text-sm">Archived projects are hidden but can be restored.</p>
            </div>
            <button hx-post="{% url 'projects:archive' project.uuid %}"
                    hx-confirm="Are you sure you want to archive {{ project.name }}?"
                    class="px-4 py-2 border border-red-500/50 text-red-400 hover:bg-red-500/10 rounded-lg transition-colors">
                Archive Project
            </button>
        </div>
    </div>
</div>
```

Add placeholder URL patterns for environment and member management (will be implemented fully in Plan 04):
```python
# Add to projects_patterns
path('<uuid:project_uuid>/environments/create/', EnvironmentCreateView.as_view(), name='environment_create'),
path('<uuid:project_uuid>/environments/<uuid:env_uuid>/', EnvironmentDetailView.as_view(), name='environment_detail'),
path('<uuid:project_uuid>/members/add/', AddMemberModalView.as_view(), name='add_member_modal'),
path('<uuid:project_uuid>/members/<uuid:group_uuid>/remove/', RemoveMemberView.as_view(), name='remove_member'),
path('<uuid:project_uuid>/update/', ProjectUpdateView.as_view(), name='update'),
path('<uuid:project_uuid>/archive/', ProjectArchiveView.as_view(), name='archive'),
```

Create stub views for these (basic implementations, will be enhanced in Plan 04).
  </action>
  <verify>Navigate to /projects/{uuid}/, tabs switch content via HTMX, URL updates with tab parameter</verify>
  <done>Project detail page with HTMX tabs functional</done>
</task>

<task type="auto">
  <name>Task 3: Implement context-replacing project sidebar</name>
  <files>core/templates/core/components/nav_project.html, core/context_processors.py, theme/templates/base.html</files>
  <action>
**Update core/context_processors.py navigation_context:**

```python
def navigation_context(request):
    """Provide navigation context including active project."""
    context = {
        'in_project_context': False,
        'current_project': None,
    }

    # Check if we're in a project-scoped URL
    if hasattr(request, 'resolver_match') and request.resolver_match:
        if 'project_uuid' in request.resolver_match.kwargs:
            from core.models import Project
            try:
                project = Project.objects.get(
                    uuid=request.resolver_match.kwargs['project_uuid']
                )
                context['in_project_context'] = True
                context['current_project'] = project
            except Project.DoesNotExist:
                pass

    return context
```

**Create core/templates/core/components/nav_project.html:**
```html
<!-- Project-scoped Sidebar Navigation -->
<aside class="fixed left-0 top-0 h-full w-64 bg-dark-surface border-r border-dark-border flex flex-col">
    <!-- Back to main nav -->
    <div class="p-4 border-b border-dark-border">
        <a href="{% url 'projects:list' %}" class="flex items-center text-dark-muted hover:text-dark-text transition-colors mb-3">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
            </svg>
            Back to Projects
        </a>
        <h2 class="text-lg font-bold text-dark-text truncate" title="{{ current_project.name }}">
            {{ current_project.name }}
        </h2>
        {% if current_project.description %}
        <p class="text-sm text-dark-muted truncate mt-1" title="{{ current_project.description }}">
            {{ current_project.description }}
        </p>
        {% endif %}
    </div>

    <!-- Project Navigation -->
    <nav class="flex-1 p-4 space-y-1">
        <a href="{% url 'projects:detail' current_project.uuid %}?tab=services"
           class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors
                  {% if request.GET.tab == 'services' or not request.GET.tab %}bg-dark-border{% endif %}">
            <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
            </svg>
            Services
        </a>
        <a href="{% url 'projects:detail' current_project.uuid %}?tab=environments"
           class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors
                  {% if request.GET.tab == 'environments' %}bg-dark-border{% endif %}">
            <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2m-2-4h.01M17 16h.01" />
            </svg>
            Environments
        </a>
        <a href="{% url 'projects:detail' current_project.uuid %}?tab=members"
           class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors
                  {% if request.GET.tab == 'members' %}bg-dark-border{% endif %}">
            <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
            Members
        </a>
        {% if user_project_role == 'owner' or is_admin %}
        <a href="{% url 'projects:detail' current_project.uuid %}?tab=settings"
           class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors
                  {% if request.GET.tab == 'settings' %}bg-dark-border{% endif %}">
            <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
            Settings
        </a>
        {% endif %}
    </nav>

    <!-- User Section (same as main nav) -->
    <div class="p-4 border-t border-dark-border">
        <div class="flex items-center justify-between">
            <div class="flex items-center">
                <div class="w-8 h-8 rounded-full bg-dark-accent flex items-center justify-center text-white font-medium">
                    {{ user.username|slice:":1"|upper }}
                </div>
                <div class="ml-3">
                    <p class="text-sm font-medium text-dark-text">{{ user.username }}</p>
                    <p class="text-xs text-dark-muted">{{ user.email }}</p>
                </div>
            </div>
            <a href="{% url 'auth:logout' %}" class="text-dark-muted hover:text-dark-text transition-colors" title="Logout">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                </svg>
            </a>
        </div>
    </div>
</aside>
```

**Update theme/templates/base.html:**
Replace the nav include with conditional logic:

```html
{% if user.is_authenticated %}
    {% if in_project_context and current_project %}
        {% include "core/components/nav_project.html" %}
    {% else %}
        {% include "core/components/nav.html" %}
    {% endif %}
{% endif %}
```

Ensure navigation_context is added to context processors in settings.py.
  </action>
  <verify>Navigate to project detail, sidebar shows project nav with back button; click back returns to main nav</verify>
  <done>Context-replacing sidebar navigation working for project context</done>
</task>

</tasks>

<verification>
1. Navigate to /projects/{uuid}/ - project detail loads with header and tabs
2. Click each tab - content swaps via HTMX without full page reload
3. URL bar updates when switching tabs (e.g., ?tab=environments)
4. Sidebar changes to project-scoped navigation with back button
5. Click "Back to Projects" - returns to project list with main navigation
6. Settings tab only visible to project owners and admins
7. Permission checks work (viewers cannot access settings)
</verification>

<success_criteria>
- Project detail shows name, description, and four tabs
- Tabs load content dynamically via HTMX
- URL updates when switching tabs (browser back/forward works)
- Context-replacing sidebar shows when viewing project
- Back button returns to main navigation and project list
- Permission helpers correctly determine user's project role
- Settings tab restricted to owners/admins
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-domain/02-03-SUMMARY.md`
</output>
