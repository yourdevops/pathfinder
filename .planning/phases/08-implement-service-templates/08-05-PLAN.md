---
phase: 08-implement-service-templates
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified: []
autonomous: false
requirements:
  - BPRT-01
  - BPRT-02
  - BPRT-03
  - BPRT-04
  - BPRT-05
  - BPRT-06

must_haves:
  truths:
    - "All template features verified end-to-end by human"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete template system: registration, sync, wizard integration, scaffolding, and deregistration.

Purpose: Confirm all features work correctly in the browser before marking the phase complete.
Output: Phase verified or issues identified for gap closure.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete template system</name>
  <what-built>
Complete template registration system with sync, wizard integration, and scaffolding:
- Template list page (table layout) at /templates/
- Template registration form at /templates/register/
- Template detail page with metadata sections and version list
- Manual sync button on detail page
- Template deregistration with deletion guard
- Templates sidebar navigation section
- Wizard template picker dropdown + version dropdown on repository step
- Env var pre-population from template on configuration step
- Template reference on review step
- Service record stores template FK and version string
- scaffold_repository rewritten for template-aware scaffolding
  </what-built>
  <how-to-verify>
1. **Start the app**: `make build && make run`
2. **Navigate to Templates**: Click "Templates" in sidebar — verify expandable section exists between CI Workflows and Integrations
3. **Template list**: Visit /templates/ — verify empty state with "Register Template" button
4. **Register a template**: Click Register, select SCM connection, enter git URL of a template repo with pathfinder.yaml. Submit.
   - Verify: redirects to detail page, shows template name from manifest, description, runtimes, required_vars
   - Verify: version list shows semver git tags sorted newest first
   - Verify: sync status shows "Synced"
5. **Template list revisited**: Go back to /templates/ — verify template appears in table with correct columns (name, description, runtimes, version count, sync status, last synced)
6. **Manual sync**: On detail page, click "Sync Now" — verify sync triggers and status updates
7. **Service creation wizard**: Create a new service. On step 2 (Repository):
   - Select "Create new repository"
   - Verify template dropdown appears (only for new repo mode)
   - Select a template — verify version dropdown appears below
   - Select a version
   - Proceed to step 3 (Configuration)
   - Verify: environment variables from template's required_vars are pre-populated (keys present, values empty)
   - Verify: no visual indicator that vars came from template
   - Proceed to review step
   - Verify: review shows template name and version
   - (Optional: complete wizard to test scaffolding if SCM connection is available)
8. **Deregistration guard**: If a service was created from the template, try to deregister it — verify blocked with error message
9. **Deregistration success**: Register a second template, then deregister it immediately (no services) — verify deletion succeeds
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all features work correctly in the browser.
</verification>

<success_criteria>
Human approves all template features: registration, list, detail, sync, wizard picker, env var seeding, review display, deregistration guard.
</success_criteria>

<output>
After completion, create `.planning/phases/08-implement-service-templates/08-05-SUMMARY.md`
</output>
