---
phase: 06.3-security-compliance-design-secrets-slsa-l3-sox-rbac
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security/rbac.md
autonomous: true

must_haves:
  truths:
    - "docs/security/rbac.md replaces the coarse owner/contributor/viewer model with granular CRUD permissions on every resource type"
    - "Document defines 8 predefined role bundles across two tiers: system (4) and project (4)"
    - "Production deployment approval workflow enforces four-eyes principle with approved_by != requested_by constraint"
    - "Two-tier scoping resolution order is defined: system role > project role > implicit authenticated user. Production behavior driven by Environment.is_production flag"
    - "Emergency override mechanism allows platform-admin to bypass approval with mandatory justification and audit trail"
    - "Default approval expiry is 4 hours, configurable per environment"
  artifacts:
    - path: "docs/security/rbac.md"
      provides: "Granular permission model, role bundles, two-tier scoping, approval workflow, emergency override, migration mapping"
      contains: "DeploymentApproval"
  key_links:
    - from: "docs/security/rbac.md"
      to: "docs/rbac.md"
      via: "supersedes note explaining migration from old model"
    - from: "docs/security/rbac.md"
      to: "docs/deployments/deployment-lifecycle.md"
      via: "cross-reference for pending_approval state addition to deployment lifecycle"
    - from: "docs/security/rbac.md"
      to: "docs/security/secrets.md"
      via: "cross-reference for secret-specific permissions (secrets-admin system role)"
---

<objective>
Create the SOX-compliant RBAC design documentation with granular permissions, role bundles, two-tier scoping, and production approval workflow.

Purpose: Replace the current coarse owner/contributor/viewer permission model with granular CRUD permissions, predefined role bundles, two-tier scoping (system + project), and an explicit approval workflow for production deployments enforcing the four-eyes principle. Production behavior driven by `Environment.is_production` flag. This addresses the "authorization model insufficient for regulated industries" finding from the deployment review.

Output: One documentation file -- docs/security/rbac.md.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.3-security-compliance-design-secrets-slsa-l3-sox-rbac/06.3-CONTEXT.md
@.planning/phases/06.3-security-compliance-design-secrets-slsa-l3-sox-rbac/06.3-RESEARCH.md

# Current RBAC doc (to be superseded)
@docs/rbac.md

# Deployment docs (approval workflow extends lifecycle)
@docs/deployments/deployment-lifecycle.md
@docs/deployments/promotion.md
@docs/deployments/review.md

# Other docs with access control tables that reference current roles
@docs/services.md
@docs/environments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/security/rbac.md -- Permission Model and Role Bundles</name>
  <files>docs/security/rbac.md</files>
  <action>
Create `docs/security/rbac.md` defining the complete SOX-compliant RBAC redesign. This is the largest document in the security domain -- it covers the permission model, role bundles, scoping, and approval workflow.

**Opening paragraph:** Pathfinder's access control model uses granular CRUD permissions organized into predefined role bundles across two scoping tiers: system-wide and project-level. This replaces the original owner/contributor/viewer model (docs/rbac.md) with fine-grained control suitable for regulated industries. Production-specific behavior (approval workflow, attestation verification) is triggered by `Environment.is_production`, not by per-environment role assignments. The model enforces segregation of duties (SOX compliance) through an explicit approval workflow for production deployments.

**Section: Permission Model**
Define the atomic permission unit:
```
Permission = (resource_type, action)
```

Resource types table:
| Resource Type | Description | Example Actions |
|---------------|-------------|-----------------|
| `service` | Services within a project | create, read, update, delete |
| `deployment` | Deployment operations | create, read, deploy, approve |
| `ci_workflow` | CI Workflow definitions | create, read, update, delete |
| `secret` | Secret values | create, read (names only), update, delete |
| `connection` | Integration connections | create, read, update, delete |
| `environment` | Project environments | create, read, update, delete |
| `project` | Projects | create, read, update, delete |
| `user` | User accounts | create, read, update, delete |
| `group` | Groups and memberships | create, read, update, delete |
| `audit_log` | Audit trail | read, export |
| `steps_repo` | CI Steps repositories | create, read, update, delete |

Standard actions: `create`, `read`, `update`, `delete`. Special actions: `deploy` (trigger deployment), `approve` (approve production deployment), `export` (generate reports).

Note: Permissions are data-driven (defined in Django settings/fixtures), not hardcoded in view logic. This enables future Config-as-Code without code changes.

**Section: Predefined Role Bundles**

System roles (platform-wide):

| Role | Description | Key Permissions |
|------|-------------|-----------------|
| `platform-admin` | Full system access | All permissions on all resource types |
| `platform-operator` | Manage integrations and infrastructure | CRUD on connection, steps_repo, environment; read on all other types |
| `security-auditor` | Read-only compliance view | Read on all resource types; read + export on audit_log |
| `secrets-admin` | Manage secrets across projects | CRUD on secret (system-wide scope); read on project, environment |

Project roles (single project scope):

| Role | Description | Key Permissions |
|------|-------------|-----------------|
| `project-admin` | Full project control | All CRUD within project scope; manage members (CRUD on group membership within project) |
| `release-manager` | Deploy to all environments including production | Deploy + approve on deployment (all environments); read on all project resources |
| `developer` | Build and deploy non-production | CRUD on service, ci_workflow; deploy on deployment (non-prod only); create + update on secret; read on all project resources |
| `viewer` | Read-only project access | Read on all project resource types |

**Section: Two-Tier Scoping**
Define the resolution order (highest priority first):

1. **System role** (highest priority) -- overrides all. If user has `platform-admin`, they can do anything regardless of project roles. If user has `security-auditor`, they can read all resources system-wide.
2. **Project role** -- base project access. Assigned via GroupMembership -> ProjectMembership. Highest project role wins when user is in multiple groups with different roles (same as current model).
3. **Implicit authenticated user** -- baseline read access to plugins list, own project memberships.

**Production behavior:** When `Environment.is_production` is true, deployments require approval from a user with `release-manager` or `project-admin` role (who is not the requester). No per-environment role assignments -- the `is_production` flag drives the behavior.

Include an example scenario:
```
User: alice
  System roles: (none)
  Groups: [team-a-devs, senior-devs]

Group: team-a-devs
  Project "frontend": developer

Group: senior-devs
  Project "frontend": project-admin
  Project "backend": developer

Alice's effective permissions:
  frontend: project-admin (highest of developer, project-admin)
    -> can approve production deployments (project-admin has approve permission)
  backend: developer
    -> can deploy to non-prod only; cannot approve production deployments
```

**Section: Production Approval Workflow**
Define the four-eyes principle enforcement:

**Approval Model:**
```
DeploymentApproval:
  - deployment: FK Deployment (OneToOne)
  - requested_by: FK User
  - requested_at: datetime
  - approved_by: FK User (nullable -- null until approved)
  - approved_at: datetime (nullable)
  - rejected_by: FK User (nullable)
  - rejected_at: datetime (nullable)
  - expires_at: datetime
  - status: enum (pending, approved, rejected, expired)
  - reason: text (required for rejection)
  - emergency_override: bool (default false)
  - override_justification: text (required if emergency_override is true)
```

**Constraint:** `approved_by != requested_by` (enforced at model validation level). This is the core of the four-eyes principle -- the person requesting deployment cannot approve their own request.

**Workflow steps:**
1. User with `deploy` permission requests deployment to a production environment (any environment where `is_production = true`)
2. Pathfinder creates Deployment with status `pending_approval` and DeploymentApproval with status `pending`
3. Approval expiry set to `now + 4 hours` (default, configurable per environment)
4. Users with `release-manager` or `project-admin` project role receive notification
5. Approver (must be different user) reviews deployment details: artifact, build verification status, env var snapshot, secret refs
6. **Approve:** DeploymentApproval status -> `approved`, Deployment status -> `pending` (enters normal deployment flow)
7. **Reject:** DeploymentApproval status -> `rejected`, Deployment status -> `cancelled`, reason recorded
8. **Expire:** Background task checks pending approvals past `expires_at`, transitions to `expired`, Deployment -> `cancelled`

**Non-production environments:** No approval required. Deployment goes directly to `pending` status. This is the current behavior and remains unchanged.

**Configurable per environment:** The approval requirement is tied to `Environment.is_production`. Environments can opt in to approval even if not marked as production (future enhancement -- not in initial design).

**Section: Emergency Override**
Define the bypass mechanism for urgent situations:
- Only users with `platform-admin` system role can trigger an emergency override
- Emergency override requires:
  1. Mandatory justification text field (minimum 20 characters)
  2. `emergency_override = true` on DeploymentApproval record
  3. Audit log entry at CRITICAL level with justification
  4. Visual indicator in deployment history: red "Emergency Override" badge
  5. Weekly compliance report includes all emergency overrides for review
- Self-approval IS allowed during emergency override (platform-admin can both request and approve) -- the justification and audit trail provide the compliance paper trail
- Emergency overrides are expected to be rare; frequency monitoring can detect misuse

**Section: Deployment Lifecycle Extension**
Explain how the approval workflow extends the deployment lifecycle (cross-reference docs/deployments/deployment-lifecycle.md):
- New status: `pending_approval` inserted before `pending` for production deployments
- Status flow for production: `pending_approval` -> `pending` -> `running` -> `health_check` -> `success`/`failed`
- Status flow for non-production (unchanged): `pending` -> `running` -> `health_check` -> `success`/`failed`
- The `triggered_by` field remains; `approved_by` is added via DeploymentApproval for full attribution

**Section: Migration from Current Model**
Define the mapping from old roles to new roles:
| Old Role | New Role | Notes |
|----------|----------|-------|
| System: `admin` | `platform-admin` | Full system access preserved |
| System: `operator` | `platform-operator` | Scope unchanged |
| System: `auditor` | `security-auditor` | Gains export permission |
| System: `user` (implicit) | Implicit authenticated | Baseline read unchanged |
| Project: `owner` | `project-admin` | Full project control preserved |
| Project: `contributor` | `developer` | Deploy permission now non-prod only |
| Project: `viewer` | `viewer` | Read-only unchanged |

Key change: Old `contributor` could not deploy to production (owner-only). New `developer` also cannot. The new `release-manager` role fills the gap -- deploy + approve across all environments. This is a new capability that did not exist in the old model.

Note: This document supersedes docs/rbac.md. The old document describes the original permission model which is replaced by this design. Implementation should update docs/rbac.md to redirect to docs/security/rbac.md.

**Section: Addressing "Authorization Insufficient" Finding**
Cross-reference docs/deployments/review.md finding:
- Review identified: current model allows a project owner to both write code and deploy to production, which fails SOX audits
- This design addresses it: four-eyes principle enforced via DeploymentApproval with `approved_by != requested_by` constraint
- The `release-manager` role separates deployment approval capability from development capability
- Emergency override provides the "break glass" mechanism without weakening the default controls

**Section: Access Control Summary**
Include a comprehensive permission matrix table for all role bundles (system + project), similar to the current Access Control Summary in docs/rbac.md but using the new granular permissions. This replaces the existing summary tables.

Target length: 160-230 lines. This is the largest document -- the permission model, 8 role definitions, approval workflow, and migration mapping require thorough specification. Use design-level model definitions, not Django code. Cross-reference deployment-lifecycle.md and docs/rbac.md.
  </action>
  <verify>
File exists at docs/security/rbac.md. Contains permission model with resource types and actions. Contains 8 predefined role bundles (4 system, 4 project). Contains two-tier scoping with resolution order. Contains production behavior driven by is_production flag. Contains DeploymentApproval model with four-eyes constraint. Contains emergency override mechanism. Contains approval workflow steps. Contains deployment lifecycle extension (pending_approval state). Contains migration mapping from old roles. Contains access control summary table. File is 150-280 lines.
  </verify>
  <done>
rbac.md defines the complete SOX-compliant RBAC redesign: granular CRUD permissions on all resource types, 8 predefined role bundles across two tiers (system + project), production behavior driven by is_production flag, approval workflow with four-eyes principle, emergency override mechanism, deployment lifecycle extension, and migration mapping from the current model.
  </done>
</task>

</tasks>

<verification>
1. docs/security/rbac.md exists with complete RBAC redesign
2. Permission model defines atomic (resource_type, action) pairs
3. All 11 resource types listed with appropriate actions
4. 8 role bundles defined across system (4) and project (4) tiers
5. Two-tier scoping resolution order clearly documented (system > project > implicit)
6. Production behavior driven by Environment.is_production flag, not per-environment roles
7. DeploymentApproval model with approved_by != requested_by constraint
8. Emergency override with mandatory justification and audit trail
9. Approval expiry defaults to 4 hours per research recommendation
10. Deployment lifecycle extended with pending_approval state
11. Migration mapping from old roles (admin->platform-admin, owner->project-admin, etc.)
12. "Authorization insufficient" finding from deployment review addressed
13. Supersedes note referencing docs/rbac.md
14. No Django code, URL patterns, or template names
</verification>

<success_criteria>
The RBAC design document fully specifies the SOX-compliant permission model: granular CRUD permissions replace the coarse owner/contributor/viewer model, predefined role bundles provide curated permission sets across two tiers, production behavior driven by is_production flag, and the approval workflow with four-eyes principle directly addresses the deployment review's authorization finding.
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-security-compliance-design-secrets-slsa-l3-sox-rbac/06.3-03-SUMMARY.md`
</output>
