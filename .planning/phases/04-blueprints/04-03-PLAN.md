---
phase: 04-blueprints
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - core/views/blueprints.py
  - core/templates/core/blueprints/list.html
  - core/templates/core/blueprints/detail.html
  - core/views/projects.py
  - core/templates/core/projects/detail.html
autonomous: true

must_haves:
  truths:
    - "Blueprint list can be filtered by tags and deploy plugin"
    - "Unavailable blueprints shown dimmed with tooltip"
    - "Toggle control shows/hides unavailable blueprints"
    - "Manual sync refreshes versions via HTMX"
    - "Project context shows blueprints filtered by project connections"
  artifacts:
    - path: "core/templates/core/blueprints/list.html"
      provides: "Filter controls and availability dimming"
      contains: "Show unavailable"
    - path: "core/templates/core/blueprints/detail.html"
      provides: "Manual sync with HTMX partial update"
      contains: "hx-post"
  key_links:
    - from: "core/views/blueprints.py"
      to: "core/models.py"
      via: "is_available_globally"
      pattern: "is_available_globally"
    - from: "core/templates/core/blueprints/list.html"
      to: "core/views/blueprints.py"
      via: "Alpine.js filtering"
      pattern: "x-show"
---

<objective>
Add filtering, availability display, and HTMX sync updates to blueprints UI.

Purpose: Complete the blueprint catalog experience per CONTEXT.md decisions - filtered views, dimmed unavailable items, and responsive sync status updates.

Output: Polished blueprint UI with all Phase 4 requirements satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blueprints/04-CONTEXT.md
@.planning/phases/04-blueprints/04-RESEARCH.md
@.planning/phases/04-blueprints/04-01-SUMMARY.md
@.planning/phases/04-blueprints/04-02-SUMMARY.md

# HTMX patterns from connections
@core/templates/core/connections/list.html
@core/templates/core/connections/_health_status.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add client-side filtering with Alpine.js</name>
  <files>core/templates/core/blueprints/list.html</files>
  <action>
Update list.html to add Alpine.js filtering per CONTEXT.md decisions:

**Filter controls section (above table):**
Add x-data with filter state:
```html
<div x-data="{
    tagFilter: '',
    pluginFilter: '',
    showUnavailable: false
}">
```

**Filter controls row:**
- Tag filter: Select dropdown with all_tags options + "All tags" default
- Deploy Plugin filter: Select dropdown with deploy_plugins options + "All plugins" default
- Show unavailable: Checkbox toggle (default unchecked per CONTEXT.md)

**Table rows filtering:**
Add data attributes to each row for filtering:
- data-tags="{{ item.blueprint.tags|join:',' }}"
- data-plugin="{{ item.blueprint.deploy_plugin }}"
- data-available="{{ item.is_available|yesno:'true,false' }}"

Add x-show directive to each row:
```html
x-show="
    (tagFilter === '' || $el.dataset.tags.includes(tagFilter)) &&
    (pluginFilter === '' || $el.dataset.plugin === pluginFilter) &&
    (showUnavailable || $el.dataset.available === 'true')
"
```

**Unavailable blueprints styling:**
- Apply class="opacity-50" when not item.is_available
- Name cell: wrap in span with title="{{ item.required_plugin }} connection required" tooltip
- Still clickable (links to detail page where they see setup hint)

**Empty state handling:**
Add x-show for empty state when no results match filters (use x-ref and check visible rows).
  </action>
  <verify>Manual test: Navigate to /blueprints/, toggle filters, verify rows show/hide correctly</verify>
  <done>Blueprint list has working tag, plugin, and availability filters</done>
</task>

<task type="auto">
  <name>Task 2: Add HTMX sync status updates</name>
  <files>
    core/templates/core/blueprints/detail.html
    core/templates/core/blueprints/_sync_status.html
    core/views/blueprints.py
  </files>
  <action>
**Update _sync_status.html partial:**
Create a standalone partial that can be swapped via HTMX:
```html
<div id="sync-status" class="flex items-center gap-4">
    <!-- Status badge -->
    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
        {% if blueprint.sync_status == 'synced' %}bg-green-500/20 text-green-400
        {% elif blueprint.sync_status == 'syncing' %}bg-yellow-500/20 text-yellow-400
        {% elif blueprint.sync_status == 'error' %}bg-red-500/20 text-red-400
        {% else %}bg-gray-500/20 text-gray-400{% endif %}">
        {{ blueprint.get_sync_status_display }}
    </span>

    <!-- Last synced -->
    {% if blueprint.last_synced_at %}
    <span class="text-sm text-dark-muted">
        Last synced {{ blueprint.last_synced_at|timesince }} ago
    </span>
    {% endif %}

    <!-- Sync button -->
    {% if can_manage and blueprint.sync_status != 'syncing' %}
    <button hx-post="{% url 'blueprints:sync' uuid=blueprint.uuid %}"
            hx-target="#sync-status"
            hx-swap="outerHTML"
            class="text-sm text-dark-accent hover:text-dark-accent/80">
        Sync Now
    </button>
    {% elif blueprint.sync_status == 'syncing' %}
    <span class="text-sm text-dark-muted">Syncing...</span>
    {% endif %}
</div>
```

**Update detail.html:**
- Include the sync status partial in the appropriate location
- Wrap the status section with id="sync-status"
- If sync_status == 'error', show expandable error section:
  ```html
  {% if blueprint.sync_status == 'error' and blueprint.sync_error %}
  <div x-data="{ expanded: false }" class="mt-4 p-4 bg-red-500/10 border border-red-500/30 rounded-lg">
      <button @click="expanded = !expanded" class="flex items-center text-red-400">
          <svg class="w-4 h-4 mr-2 transition-transform" :class="{ 'rotate-90': expanded }" ...>
          Sync Error Details
      </button>
      <pre x-show="expanded" x-transition class="mt-2 text-sm text-red-300 whitespace-pre-wrap">{{ blueprint.sync_error }}</pre>
  </div>
  {% endif %}
  ```

**Update BlueprintSyncView in views/blueprints.py:**
Ensure the sync view returns the partial with can_manage in context:
```python
if request.headers.get('HX-Request'):
    return render(request, 'core/blueprints/_sync_status.html', {
        'blueprint': blueprint,
        'can_manage': has_system_role(request.user, ['admin', 'operator']),
    })
```

**Add polling for syncing status (optional enhancement):**
If blueprint.sync_status == 'syncing', add hx-trigger="every 3s" to poll for updates until status changes.
  </action>
  <verify>Manual test: Click "Sync Now" on detail page, verify status updates without page reload</verify>
  <done>Manual sync triggers HTMX update of sync status section</done>
</task>

<task type="auto">
  <name>Task 3: Add blueprint availability banner on detail page</name>
  <files>core/templates/core/blueprints/detail.html</files>
  <action>
When a blueprint is not available (deploy_plugin has no matching connection), show a setup hint banner at the top of the detail page per CONTEXT.md:

```html
{% if not is_available and required_plugin %}
<div class="mb-6 p-4 bg-amber-500/10 border border-amber-500/30 rounded-lg">
    <div class="flex items-start">
        <svg class="w-5 h-5 text-amber-400 mt-0.5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
        </svg>
        <div>
            <h3 class="text-amber-400 font-medium">Blueprint Unavailable</h3>
            <p class="mt-1 text-sm text-dark-muted">
                This blueprint requires a <strong>{{ required_plugin }}</strong> connection.
                <a href="{% url 'connections:list' %}" class="text-dark-accent hover:underline">
                    Add a connection
                </a>
                to use this blueprint.
            </p>
        </div>
    </div>
</div>
{% endif %}
```

Also ensure the "Use Blueprint" button (for Phase 5) or any action buttons are disabled/hidden when blueprint is unavailable.
  </action>
  <verify>Manual test: View detail of blueprint with no matching deploy connection, verify banner appears</verify>
  <done>Unavailable blueprints show setup hint banner on detail page</done>
</task>

</tasks>

<verification>
Complete Phase 4 verification checklist:

**BPRT-01: Operator can register blueprint from git URL with preview**
- [ ] Navigate to /blueprints/register/
- [ ] SCM Provider dropdown shows "None (public repository)" + GitHub connections
- [ ] Enter valid GitHub URL - preview panel shows manifest fields
- [ ] Register button disabled until preview succeeds
- [ ] Enter URL without ssp-template.yaml - shows error, button stays disabled
- [ ] Click Register Blueprint - creates blueprint and redirects to detail page

**BPRT-02: Blueprint syncs metadata from ssp-template.yaml**
- [ ] After sync completes, blueprint shows name from manifest
- [ ] Description, tags, ci.plugin, deploy.plugin populated
- [ ] manifest field contains full YAML content
- [ ] Blueprint.connection field matches selected SCM provider

**BPRT-03: Blueprint shows available git tags as versions**
- [ ] Detail page shows version dropdown
- [ ] Versions are sorted (latest first)
- [ ] Pre-releases hidden by default
- [ ] Toggle reveals pre-releases

**BPRT-04: Blueprint displays name, description, tags, ci.plugin, deploy.plugin**
- [ ] List shows all fields in table columns
- [ ] Detail page shows all metadata
- [ ] Detail page shows SCM connection name or "None (public)"

**BPRT-05: Operator can manually sync blueprint**
- [ ] Sync Now button visible for operators
- [ ] Click triggers background sync
- [ ] Status updates via HTMX without page reload

**BPRT-06: Blueprint availability filtered by connections**
- [ ] Blueprints without matching deploy connection appear dimmed
- [ ] Tooltip shows required plugin
- [ ] Toggle "Show unavailable" hides/shows them
- [ ] Detail page shows setup hint banner for unavailable

Run: python manage.py runserver
Test all flows manually as admin user.
</verification>

<success_criteria>
- All 6 BPRT requirements satisfied
- Filter controls work for tags and deploy plugin
- Show unavailable toggle works (default: hidden)
- Unavailable blueprints dimmed with tooltip on list
- Setup hint banner on detail page for unavailable blueprints
- HTMX sync updates status without page reload
- Sync errors shown in expandable section
</success_criteria>

<output>
After completion, create `.planning/phases/04-blueprints/04-03-SUMMARY.md`
</output>
