---
phase: 04-blueprints
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - core/views/blueprints.py
  - core/views/__init__.py
  - core/urls.py
  - core/templates/core/blueprints/list.html
  - core/templates/core/blueprints/register.html
  - core/templates/core/blueprints/detail.html
autonomous: true

must_haves:
  truths:
    - "User can view blueprint list with table layout"
    - "Operator can register a blueprint from git URL"
    - "Blueprint detail shows metadata and version dropdown"
    - "Registration triggers background sync"
  artifacts:
    - path: "core/views/blueprints.py"
      provides: "Blueprint views (list, detail, register)"
      exports: ["BlueprintListView", "BlueprintDetailView", "BlueprintRegisterView"]
    - path: "core/templates/core/blueprints/list.html"
      provides: "Table-style blueprint list"
      contains: "blueprints"
    - path: "core/templates/core/blueprints/detail.html"
      provides: "Blueprint detail with versions"
      contains: "blueprint"
  key_links:
    - from: "core/views/blueprints.py"
      to: "core/models.py"
      via: "Blueprint.objects"
      pattern: "Blueprint\\.objects"
    - from: "core/views/blueprints.py"
      to: "core/tasks.py"
      via: "sync_blueprint.enqueue"
      pattern: "sync_blueprint\\.enqueue"
    - from: "core/urls.py"
      to: "core/views/blueprints.py"
      via: "URL patterns"
      pattern: "BlueprintListView"
---

<objective>
Create blueprint views and templates for listing, registration, and detail pages.

Purpose: Replace placeholder blueprint views with functional implementation per CONTEXT.md decisions (table layout, single URL registration, version dropdown).

Output: Working blueprint UI that operators can use to register and view blueprints.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blueprints/04-CONTEXT.md
@.planning/phases/04-blueprints/04-RESEARCH.md
@.planning/phases/04-blueprints/04-01-SUMMARY.md

# Existing view patterns
@core/views/connections.py
@core/permissions.py
@core/urls.py
@core/templates/core/connections/list.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blueprint views module</name>
  <files>core/views/blueprints.py, core/views/__init__.py</files>
  <action>
Create core/views/blueprints.py with the following views:

**BlueprintListView(LoginRequiredMixin, View):**
- template_name = 'core/blueprints/list.html'
- GET: Query Blueprint.objects.exclude(sync_status='pending').select_related('connection')
- Collect all_tags (unique tags from all blueprints) and deploy_plugins for filter dropdowns
- Get available_plugins from active IntegrationConnections
- Build blueprint_data list with {'blueprint': bp, 'is_available': bp.is_available_globally(), 'required_plugin': bp.deploy_plugin if not available else None}
- Check can_manage = has_system_role(request.user, ['admin', 'operator'])
- Render with blueprints, all_tags, deploy_plugins, can_manage

**BlueprintRegisterView(OperatorRequiredMixin, View):**
- template_name = 'core/blueprints/register.html'
- GET: Check for active GitHub connections, render form with warning if none exist
- POST:
  - Get git_url from request.POST, strip whitespace
  - Validate not empty
  - Validate URL format with parse_github_url (from core.tasks)
  - Check for duplicate git_url
  - Create Blueprint with sync_status='pending', created_by=request.user.username
  - Enqueue sync_blueprint task
  - Redirect to blueprints:list with success message

**BlueprintDetailView(LoginRequiredMixin, View):**
- template_name = 'core/blueprints/detail.html'
- GET: get_object_or_404(Blueprint, uuid=uuid)
- Get show_prereleases from request.GET.get('show_prereleases') == 'true'
- Filter versions (exclude prereleases unless show_prereleases)
- Get selected_version from request.GET.get('version') or use blueprint.latest_version
- Check is_available = blueprint.is_available_globally()
- Check can_manage = has_system_role(request.user, ['admin', 'operator'])
- Render with blueprint, versions, current_version, show_prereleases, prerelease_count, is_available, required_plugin, can_manage

**BlueprintSyncView(OperatorRequiredMixin, View):**
- POST only: get_object_or_404(Blueprint, uuid=uuid)
- If sync_status != 'syncing': mark as syncing, enqueue sync_blueprint task
- If HTMX request: return partial _sync_status.html
- Otherwise: redirect to blueprints:detail

Update core/views/__init__.py to import and export:
- BlueprintListView (as BlueprintsListView for backwards compat with URL)
- BlueprintDetailView
- BlueprintRegisterView
- BlueprintSyncView

Remove the placeholder BlueprintsListView import from placeholders.py.
  </action>
  <verify>python manage.py check reports no errors</verify>
  <done>Blueprint views exist with list, detail, register, sync functionality</done>
</task>

<task type="auto">
  <name>Task 2: Update URL patterns</name>
  <files>core/urls.py</files>
  <action>
Update core/urls.py:

1. Update imports to use new blueprint views:
```python
from .views import (
    # ... existing imports ...
    BlueprintsListView, BlueprintDetailView, BlueprintRegisterView, BlueprintSyncView,
    # Remove BlueprintsListView from placeholders import if there
)
```

2. Update blueprints_patterns:
```python
blueprints_patterns = [
    path('', BlueprintsListView.as_view(), name='list'),
    path('register/', BlueprintRegisterView.as_view(), name='register'),
    path('<uuid:uuid>/', BlueprintDetailView.as_view(), name='detail'),
    path('<uuid:uuid>/sync/', BlueprintSyncView.as_view(), name='sync'),
]
```

Note: Keep the name 'list' for backwards compatibility with nav.html links.
  </action>
  <verify>python manage.py check && python -c "from django.urls import reverse; print(reverse('blueprints:list'), reverse('blueprints:register'))"</verify>
  <done>Blueprint URL patterns support list, register, detail, sync routes</done>
</task>

<task type="auto">
  <name>Task 3: Create blueprint templates</name>
  <files>
    core/templates/core/blueprints/list.html
    core/templates/core/blueprints/register.html
    core/templates/core/blueprints/detail.html
    core/templates/core/blueprints/_sync_status.html
  </files>
  <action>
Create core/templates/core/blueprints/ directory and templates.

**list.html** (table-style per CONTEXT.md):
- Extend base.html
- Page title: "Blueprints"
- Header with "Register Blueprint" button (if can_manage)
- Table with columns: Name, Description, Tags, Deploy Plugin, Versions, Last Synced, Status
- For each item in blueprints:
  - Apply opacity-50 class if not item.is_available
  - Name links to detail page (or dimmed span with tooltip if unavailable)
  - Tags as inline badges (text-xs bg-dark-border rounded px-2 py-0.5)
  - Deploy plugin name
  - Version count
  - Last synced (humanized) or "Never"
  - Sync status badge (synced=green, syncing=yellow, error=red, pending=gray)
- Empty state if no blueprints: "No blueprints registered. Register your first blueprint to get started."
- Include filter controls (stretch goal - can be simple text for now)

**register.html**:
- Extend base.html
- Page title: "Register Blueprint"
- If no_github_connection: show warning banner with link to create one
- Form with:
  - git_url text input (placeholder: "https://github.com/org/repo")
  - Submit button "Register Blueprint"
- Show error messages if any

**detail.html**:
- Extend base.html
- Page title: blueprint.name or "Blueprint"
- Back link to blueprints:list
- If not is_available: show info banner "Add a {{ required_plugin }} connection to use this blueprint"
- Metadata section:
  - Name, Description
  - Tags (badges)
  - CI Plugin, Deploy Plugin
  - Git URL (link)
  - Sync status with timestamp
  - Sync button (if can_manage and not syncing)
- Versions section:
  - Dropdown to select version (auto-select latest_version)
  - Toggle checkbox "Show pre-releases" (shows prerelease_count)
  - List of versions or "No versions found"
- If sync_status == 'error': show expandable error message

**_sync_status.html** (partial for HTMX):
- Sync status badge
- Last synced timestamp
- Sync button (if can_manage)

Use existing Tailwind classes from connections templates. Dark theme classes: bg-dark-bg, bg-dark-surface, text-dark-text, text-dark-muted, border-dark-border.
  </action>
  <verify>ls -la core/templates/core/blueprints/ shows all 4 files</verify>
  <done>Blueprint templates render list, register form, and detail with versions</done>
</task>

</tasks>

<verification>
1. Start dev server: `python manage.py runserver`
2. Login as admin
3. Navigate to /blueprints/ - should see empty list with "Register Blueprint" button
4. Click "Register Blueprint" - should see form
5. Test with invalid URL - should show error
6. Test with valid GitHub URL (e.g., a public repo) - should redirect to list
7. Blueprint should show "Syncing..." status
8. After worker processes task, status should change to "Synced" or "Error"
9. Click blueprint name to view detail page
10. Verify version dropdown shows available versions
</verification>

<success_criteria>
- /blueprints/ shows table-style list (empty initially)
- /blueprints/register/ shows single URL field form
- Registration creates blueprint and triggers sync
- /blueprints/<uuid>/ shows detail with metadata and versions
- Unavailable blueprints show dimmed with tooltip
- Sync button triggers background sync (if can_manage)
- Templates use dark theme classes consistently
</success_criteria>

<output>
After completion, create `.planning/phases/04-blueprints/04-02-SUMMARY.md`
</output>
