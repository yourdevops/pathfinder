---
phase: 03.1-unified-sidebar-navigation
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - core/templates/core/components/nav_project.html
autonomous: true

must_haves:
  truths:
    - "Project sidebar shows Details as first nav item (not Services)"
    - "Back button replaces DevSSP logo position when in project context"
    - "Back button navigates to Projects list"
    - "Project header shows project name prominently"
    - "Settings is accessible from Details page, not as separate nav item"
  artifacts:
    - path: "core/templates/core/components/nav_project.html"
      provides: "Refactored project-scoped sidebar"
      contains: "Back to Projects"
  key_links:
    - from: "core/templates/core/components/nav_project.html"
      to: "{% url 'projects:list' %}"
      via: "back button href"
      pattern: "projects:list"
---

<objective>
Refine the project sidebar navigation per the CONTEXT.md decisions: Details first (not Services), back button at logo position, and Settings not as separate nav item.

Purpose: Align project context navigation with the design decisions
Output: Refined project sidebar with corrected navigation structure
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-unified-sidebar-navigation/03.1-CONTEXT.md
@.planning/phases/03.1-unified-sidebar-navigation/03.1-01-SUMMARY.md

@core/templates/core/components/nav_project.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure project sidebar navigation</name>
  <files>core/templates/core/components/nav_project.html</files>
  <action>
Update nav_project.html with the following changes per CONTEXT.md:

1. **Back Button Position**: Move the back button to replace the DevSSP logo position (top-left corner):
   - The back button should be in the same visual position as "DevSSP" in the main nav
   - Text: "Back to Projects" with left arrow icon
   - Style: Same padding/sizing as the DevSSP header section
   - Navigation: {% url 'projects:list' %}

2. **Project Header**: Keep the project name section below the back button
   - Show project name prominently (text-lg font-bold)
   - Optional description (truncated, text-sm text-dark-muted)

3. **Navigation Order**: Reorder nav items to:
   - **Details** (first, default landing when entering project)
     - URL: {% url 'projects:detail' project_uuid=current_project.uuid %}
     - No tab parameter (or tab=details if needed)
     - Active when: no tab param or tab == 'details' or viewing project root
   - **Services**
     - URL: {% url 'projects:detail' project_uuid=current_project.uuid %}?tab=services
     - Active when: tab == 'services'
   - **Environments**
     - URL: {% url 'projects:detail' project_uuid=current_project.uuid %}?tab=environments
     - Active when: tab == 'environments'
   - **Members**
     - URL: {% url 'projects:detail' project_uuid=current_project.uuid %}?tab=members
     - Active when: tab == 'members'

4. **Remove Settings Nav Item**: Settings should NOT be a separate nav item
   - Settings are accessible from the Details page (via a button/link on the Settings tab)
   - The Settings tab still exists in the project detail view (accessed via URL ?tab=settings)
   - But it's not shown in the sidebar navigation
   - Remove the {% if current_project_role == 'owner' %} Settings link block

5. **Active State Logic**: Update the active highlighting:
   - Details: active when `not request.GET.tab or request.GET.tab == 'settings'`
     (Settings tab shows Details as active since settings is part of details context)
   - Services: active when `request.GET.tab == 'services'`
   - Environments: active when `request.GET.tab == 'environments'`
   - Members: active when `request.GET.tab == 'members'`
  </action>
  <verify>
Run: python manage.py check
Verify: No template errors
  </verify>
  <done>Project sidebar has Details first, no Settings nav item, back button at top</done>
</task>

</tasks>

<verification>
1. Navigate to a project - sidebar shows "Back to Projects" at top where DevSSP logo normally is
2. Click "Back to Projects" - returns to /projects/ list
3. Nav items are: Details (first), Services, Environments, Members
4. Settings is NOT shown as a nav item (even for owners)
5. Default landing (no tab param) shows Details as active
6. Each tab correctly highlights its nav item
</verification>

<success_criteria>
- Back button is at the top position (where DevSSP logo normally sits)
- Details is the first nav item and default active state
- Services, Environments, Members follow in that order
- Settings is NOT a separate nav item in the sidebar
- Active states work correctly for each tab
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-unified-sidebar-navigation/03.1-03-SUMMARY.md`
</output>
