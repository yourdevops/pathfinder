---
phase: 09-websockets-for-service-s-pages
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - core/consumers.py
  - core/templates/core/services/detail.html
  - core/templates/core/services/_builds_tab.html
  - core/templates/core/services/_ci_tab.html
  - core/templates/core/components/nav_service.html
  - theme/templates/base.html
autonomous: true
requirements:
  - SRVC-09

must_haves:
  truths:
    - "WebSocket connection opens when user navigates to service detail page"
    - "Connection persists across HTMX tab switches within the service"
    - "Dashboard sections update in real-time when build status changes"
    - "Builds list refreshes when any build changes status"
    - "Build detail status updates when build completes"
    - "CI tab manifest sync status updates in real-time"
    - "Sidebar shows green dot when WebSocket is connected"
    - "Page works fine without WebSocket -- manual refresh still works"
    - "Old HTMX polling is removed from builds tab"
    - "New or changed build rows get a brief highlight animation"
  artifacts:
    - path: "core/consumers.py"
      provides: "Full OOB rendering for all service sections"
      min_lines: 120
    - path: "core/templates/core/services/detail.html"
      provides: "WebSocket connection wrapper"
      contains: "ws-connect"
    - path: "core/templates/core/components/nav_service.html"
      provides: "Connection status indicator dot"
      contains: "wsStatus"
  key_links:
    - from: "core/templates/core/services/detail.html"
      to: "core/consumers.py"
      via: "ws-connect URL"
      pattern: "ws-connect.*ws/services"
    - from: "core/consumers.py"
      to: "core/templates/core/services/_stats_row.html"
      via: "render_to_string"
      pattern: "render_to_string.*_stats_row"
    - from: "core/consumers.py"
      to: "core/templates/core/services/_recent_builds.html"
      via: "render_to_string"
      pattern: "render_to_string.*_recent_builds"
    - from: "core/consumers.py"
      to: "core/templates/core/services/_ci_pipeline_card.html"
      via: "render_to_string"
      pattern: "render_to_string.*_ci_pipeline_card"
    - from: "core/consumers.py"
      to: "core/templates/core/services/_builds_tab.html"
      via: "render_to_string"
      pattern: "render_to_string.*_builds_tab"
---

<objective>
Wire the WebSocket connection into the service detail page, implement full OOB rendering in the consumer for all real-time sections, add the sidebar connection status indicator, remove old HTMX polling, and add build row highlight animation.

Purpose: Deliver the end-to-end real-time update experience -- server pushes rendered HTML partials over WebSocket, HTMX swaps them into the DOM via OOB.
Output: Fully functioning real-time updates for all service-context pages.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-websockets-for-service-s-pages/09-CONTEXT.md
@.planning/phases/09-websockets-for-service-s-pages/09-RESEARCH.md
@.planning/phases/09-websockets-for-service-s-pages/09-01-SUMMARY.md
@.planning/phases/09-websockets-for-service-s-pages/09-02-SUMMARY.md
@core/consumers.py
@core/routing.py
@core/templates/core/services/detail.html
@core/templates/core/services/_details_tab.html
@core/templates/core/services/_stats_row.html
@core/templates/core/services/_recent_builds.html
@core/templates/core/services/_ci_pipeline_card.html
@core/templates/core/services/_builds_tab.html
@core/templates/core/services/_ci_tab.html
@core/templates/core/components/nav_service.html
@theme/templates/base.html
@core/views/services.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire WebSocket connection and implement OOB rendering in consumer</name>
  <files>
    core/templates/core/services/detail.html
    core/consumers.py
    core/templates/core/services/_builds_tab.html
    core/templates/core/services/_ci_tab.html
  </files>
  <action>
    1. **Update `detail.html`** -- add WebSocket connection wrapper:
       Per locked decision: place `ws-connect` on the content wrapper OUTSIDE `#tab-content` so the connection persists across HTMX tab switches.
       ```html
       {% block content %}
       <div class="p-8" hx-ext="ws" ws-connect="/ws/services/{{ service.id }}/">
           <div id="tab-content">
               {% include tab_template %}
           </div>
       </div>
       {% endblock %}
       ```
       The htmx-ext-ws extension handles reconnection with exponential backoff automatically.

    2. **Update `core/consumers.py`** -- implement full `render_updates()`:
       The method must render all OOB-targetable sections and concatenate them into a single WS message. HTMX silently ignores OOB swaps for IDs not present in the current DOM, so it's safe to always send all sections.

       **render_updates(state) -- @sync_to_async:**
       Build template context from state dict, then render each section:

       a) **Dashboard sections** (visible on details tab):
          - `_stats_row.html` with id `dashboard-stats-row`
          - `_recent_builds.html` with id `dashboard-recent-builds`
          - `_ci_pipeline_card.html` with id `dashboard-ci-pipeline`
          - `_dashboard_empty.html` -- conditionally rendered when total_builds == 0

          For the empty state: when total_builds == 0, render `_dashboard_empty.html` and wrap it in `<div id="dashboard-stats-row" hx-swap-oob="true">` (replacing the stats row area with the empty state).

       b) **Builds tab** (visible on builds tab):
          - Render `_builds_tab.html` with full builds context (builds queryset, pagination, filters)
          - Wrap in `<div id="builds-tab-content" hx-swap-oob="true">`
          - This provides full list refresh when any build changes status per locked decision

       c) **CI tab sections** (visible on CI tab):
          - Add a wrapping div with `id="ci-tab-content"` around the content of `_ci_tab.html`
          - Consumer renders just the CI-relevant state changes (manifest sync status, workflow version info)
          - Wrap in `<div id="ci-manifest-status" hx-swap-oob="true">` targeting just the status badges section

       d) **Scaffold status badge** (visible on details tab header):
          - Render a `<span id="scaffold-badge" hx-swap-oob="true">` with current scaffold status
          - Only include when scaffold_status is pending/running/failed

       **Helper function `build_oob_html(template, context, target_id)`:**
       ```python
       def build_oob_html(template_name, context, target_id):
           html = render_to_string(template_name, context)
           return f'<div id="{target_id}" hx-swap-oob="true">{html}</div>'
       ```

       **Template context building:**
       The consumer must build the same context variables that `ServiceDetailView.get_context_data` provides for the details tab. Create a helper method `build_template_context(state)` that constructs:
       - `service`, `project` objects (fetch in get_current_state)
       - `total_builds`, `last_build`, `success_rate`, `avg_build_time_seconds`, `recent_builds`
       - `ci_workflow`, `ci_workflow_version`
       - `builds` (for builds tab -- paginated queryset, first page)
       - `has_running_builds`, `has_any_builds`
       - `show_empty_state`

       Note: For the builds tab OOB swap, send just the first page of builds with default sort. Users who have navigated to specific pages/filters will get a full refresh -- this is acceptable per the locked decision "full list refresh when any build changes status".

    3. **Update `_builds_tab.html`**:
       - Remove the HTMX polling: the `hx-get` + `hx-trigger="every 5s"` on the outer div should be removed. The div's `id="builds-tab-content"` is already the OOB target.
       - Keep all other functionality (sorting, filtering, pagination, search) -- these continue to work via HTMX GET requests.

    4. **Update `_ci_tab.html`**:
       - Add `id="ci-manifest-status"` to the manifest status + push actions section (the `<div class="flex items-center justify-between pt-4 border-t ...">`)
       - This allows the consumer to push manifest sync status updates via OOB swap

    5. **Add a CSS class for build row highlight animation** via Tailwind:
       - New/changed build rows need a brief highlight. Use the `ws-highlight` approach:
         In the consumer's builds tab rendering, add a `data-ws-updated="true"` attribute to changed rows.
         In base.html, add a small CSS animation:
         ```css
         /* In a <style> tag with nonce */
         [data-ws-updated] { animation: ws-highlight 2s ease-out; }
         @keyframes ws-highlight { from { background-color: rgba(59, 130, 246, 0.15); } to { background-color: transparent; } }
         ```
         OR simpler: the consumer can add a temporary Tailwind class that fades. Since this is a WS push, the simplest approach is to NOT track individual row changes and instead apply a subtle flash to the entire builds content area when it updates. Use the htmx `htmx:wsAfterMessage` event to add a brief class. Register an Alpine component or add to the base.html script:
         ```javascript
         document.addEventListener('htmx:wsAfterMessage', function(e) {
             var builds = document.getElementById('builds-tab-content');
             if (builds) {
                 builds.style.opacity = '0.7';
                 setTimeout(function() { builds.style.opacity = '1'; }, 150);
             }
         });
         ```
         This is a subtle visual cue that content has been refreshed, not an intrusive notification.
  </action>
  <verify>
    - `grep "ws-connect" core/templates/core/services/detail.html` shows the WebSocket URL
    - `grep "hx-swap-oob" core/consumers.py` shows OOB wrapping
    - `grep "render_to_string" core/consumers.py` shows template rendering
    - `grep -c "hx-trigger.*every" core/templates/core/services/_builds_tab.html` returns 0 (polling removed)
    - `uv run python manage.py check` passes
  </verify>
  <done>WebSocket connection established on service detail page, consumer renders OOB HTML for all service sections, old HTMX polling removed from builds tab and scaffold badge, builds get subtle refresh indicator</done>
</task>

<task type="auto">
  <name>Task 2: Add connection status indicator and fallback warning</name>
  <files>
    core/templates/core/components/nav_service.html
    theme/templates/base.html
  </files>
  <action>
    1. **Register `wsStatus` Alpine.js component in `base.html`**:
       Add to the `alpine:init` event listener block (alongside existing `dropdown`, `copyBtn`, etc.):
       ```javascript
       Alpine.data('wsStatus', function() {
           return {
               connected: false,
               init: function() {
                   var self = this;
                   document.addEventListener('htmx:wsOpen', function() {
                       self.connected = true;
                   });
                   document.addEventListener('htmx:wsClose', function() {
                       self.connected = false;
                   });
                   document.addEventListener('htmx:wsError', function() {
                       self.connected = false;
                   });
               }
           };
       });
       ```
       IMPORTANT: Use `function()` syntax (not arrow functions) per CSP Alpine build constraints.

    2. **Update `nav_service.html`** -- add connection status dot:
       In the Service Header section (the `<div class="p-4 border-b border-dark-border">` with the service name), add a small status dot next to the service name:

       Wrap the service header in `x-data="wsStatus()"`:
       ```html
       <div class="p-4 border-b border-dark-border" x-data="wsStatus()">
           <div class="flex items-center gap-2">
               <h2 class="text-lg font-bold text-dark-text truncate" title="{{ service.name }}">
                   {{ service.name }}
               </h2>
               <!-- WebSocket Connection Status -->
               <span class="w-2 h-2 rounded-full shrink-0 transition-colors duration-300"
                     :class="connected ? 'bg-green-400' : 'bg-gray-500'"
                     :title="connected ? 'Live updates active' : 'Live updates inactive'"></span>
               <!-- Status Badge -->
               <span class="px-2 py-0.5 text-xs rounded ...">
                   ...existing status badge...
               </span>
           </div>
           ...
       </div>
       ```

    3. **Add fallback warning** -- if WebSocket can't connect, show subtle warning:
       Per locked decision: if WS can't connect, show "Live updates unavailable" warning. Page works fine without it.

       In `detail.html`, add a small warning div that shows only when disconnected after initial attempt:
       This can be done in the ws-connect wrapper div. Add an Alpine component that listens for WS events:
       ```html
       <div class="p-8" hx-ext="ws" ws-connect="/ws/services/{{ service.id }}/"
            x-data="{ wsEverConnected: false, wsConnected: false }"
            @htmx:ws-open.window="wsEverConnected = true; wsConnected = true"
            @htmx:ws-close.window="wsConnected = false"
            @htmx:ws-error.window="wsConnected = false">
           <!-- Disconnected warning (only shows after initial connection was lost) -->
           <div x-show="wsEverConnected && !wsConnected" x-cloak
                class="mb-4 px-4 py-2 bg-amber-500/10 border border-amber-500/30 rounded-lg text-sm text-amber-300 flex items-center gap-2">
               <svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                   <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z" />
               </svg>
               Live updates temporarily unavailable. Reconnecting...
           </div>
           <div id="tab-content">
               {% include tab_template %}
           </div>
       </div>
       ```

       NOTE on CSP compatibility: The Alpine expressions above are single-expression assignments (`wsConnected = true`, etc.) and comparisons (`wsEverConnected && !wsConnected`) which work fine with Alpine CSP build. The `@htmx:ws-open.window` syntax listens for htmx events bubbled to window. However, verify that htmx-ext-ws events match these names. The actual event names from htmx-ext-ws 2.0 are: `htmx:wsOpen`, `htmx:wsClose`, `htmx:wsError`. In Alpine's `@` syntax, colons in event names use dots: `@htmx:ws-open.window` should be `@htmx:ws-open.camel.window` or use `x-on`. Check the actual htmx-ext-ws event names and adjust accordingly.

       SAFER APPROACH: Register the event listeners in the Alpine component's init method via `document.addEventListener()` rather than inline `@` directives, to avoid CSP event name parsing issues:
       ```javascript
       // In the wsStatus Alpine.data component (already registered in Task 2.1), add:
       // The component is already on nav_service.html. For detail.html, use a separate Alpine data:
       ```
       Actually, simplify: move the fallback warning logic into the existing `wsStatus` component. The nav_service.html sidebar already has `wsStatus`. For the disconnected banner in detail.html, use a separate `x-data` with simple boolean tracking set from JS event listeners. Since the htmx WS events bubble to document, the existing wsStatus component's listeners will work.

       Alternative (simpler, more reliable): Add a `<div id="ws-fallback-warning" class="hidden">` in detail.html, and toggle its visibility from the same event listeners in base.html's script block:
       ```javascript
       // In base.html, after the htmx:wsAfterMessage listener:
       var wsWarning = null;
       var wsEverConnected = false;
       document.addEventListener('htmx:wsOpen', function() {
           wsEverConnected = true;
           wsWarning = document.getElementById('ws-fallback-warning');
           if (wsWarning) wsWarning.classList.add('hidden');
       });
       document.addEventListener('htmx:wsClose', function() {
           if (wsEverConnected) {
               wsWarning = document.getElementById('ws-fallback-warning');
               if (wsWarning) wsWarning.classList.remove('hidden');
           }
       });
       ```
       This avoids CSP parsing issues entirely and works reliably.

       Use the vanilla JS approach for the fallback warning. Keep the Alpine wsStatus component for the sidebar dot (it's simpler and already works).
  </action>
  <verify>
    - `grep "wsStatus" theme/templates/base.html` shows the Alpine component registration
    - `grep "wsStatus" core/templates/core/components/nav_service.html` shows usage
    - `grep "ws-fallback-warning" core/templates/core/services/detail.html` shows the warning div
    - `uv run python manage.py check` passes
  </verify>
  <done>Sidebar shows green/gray dot for WebSocket connection status, disconnection warning appears after connection loss, page works without WebSocket via manual refresh</done>
</task>

</tasks>

<verification>
- WebSocket connection opens on service detail page load
- Navigating between tabs (details, builds, CI, environments, settings) does NOT disconnect the WebSocket
- Navigating away from the service closes the WebSocket
- When a build status changes, the dashboard stats row updates within 3 seconds
- When a build status changes, the builds list tab updates within 3 seconds
- Sidebar dot turns green when connected
- If WebSocket disconnects, dot turns gray and warning banner appears
- No HTMX polling (`hx-trigger="every Ns"`) remains in service templates
</verification>

<success_criteria>
All service-context pages receive real-time updates via WebSocket OOB swaps. Old HTMX polling is removed. Connection status is visible in the sidebar. Disconnection is handled gracefully with reconnection and fallback warning.
</success_criteria>

<output>
After completion, create `.planning/phases/09-websockets-for-service-s-pages/09-03-SUMMARY.md`
</output>
