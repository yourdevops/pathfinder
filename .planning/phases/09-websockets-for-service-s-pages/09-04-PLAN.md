---
phase: 09-websockets-for-service-s-pages
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified: []
autonomous: false
requirements:
  - SRVC-09

must_haves:
  truths:
    - "User can see real-time dashboard updates on service detail page"
    - "User can see builds list update when a build completes"
    - "User can see connection status dot in sidebar"
    - "Page works without WebSocket via manual refresh"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete WebSocket real-time update system end-to-end.

Purpose: Confirm that all real-time features work correctly from the user's perspective.
Output: Verified, working real-time update system.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-websockets-for-service-s-pages/09-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete WebSocket real-time update system</name>
  <files></files>
  <action>
    Present the verification checklist to the user. Start the application with `make run` (uses uvicorn now for WebSocket support).

    What was built:
    - Django Channels WebSocket consumer with database polling every 3 seconds
    - htmx-ext-ws extension for client-side OOB swap handling
    - Real-time updates for: dashboard stats, recent builds, CI pipeline card, builds list, CI tab manifest status
    - Conditional dashboard empty states (no workflow, no builds, has builds)
    - Sidebar connection status indicator (green/gray dot)
    - Disconnection warning banner with auto-reconnection
    - Dashboard polish: clickable commit SHAs, CI pipeline accent borders
    - Removed old HTMX polling from builds tab and scaffold badge

    Verification steps:

    1. Connection Test: Navigate to any service detail page. Green dot appears next to service name in sidebar. No errors in browser console.

    2. Dashboard Empty State: If no CI workflow -- shows "Set Up CI Pipeline" card. If workflow but no builds -- shows "No Builds Yet" card with Fetch Builds. If builds exist -- shows stats row, CI pipeline card, recent builds.

    3. Real-Time Dashboard Updates: Trigger a build change. Dashboard stats update within 3 seconds without page refresh.

    4. Real-Time Builds Tab: Click Builds in sidebar. WebSocket stays connected. Builds list refreshes automatically on status change.

    5. Tab Switching: Switch between all tabs. WebSocket connection persists (green dot stays green).

    6. CI Pipeline Accent Border: Green left border when healthy. Amber left border when needs attention.

    7. Clickable Commit SHAs: Commit SHAs are blue/clickable links opening commit/CI run URL.

    8. Disconnection Handling: Stop uvicorn -- dot turns gray, warning appears. Restart -- reconnects automatically.

    9. Navigation Away: Click Back to Project -- WebSocket closes cleanly, no console errors.
  </action>
  <verify>User confirms all 9 verification points pass by typing "approved"</verify>
  <done>Complete WebSocket real-time update system verified and working</done>
</task>

</tasks>

<verification>
All 9 verification points from the human-verify task must pass.
</verification>

<success_criteria>
Real-time WebSocket updates work correctly across all service-context pages. Dashboard empty states display properly. Connection status is visible. Fallback behavior is graceful.
</success_criteria>

<output>
After completion, create `.planning/phases/09-websockets-for-service-s-pages/09-04-SUMMARY.md`
</output>
