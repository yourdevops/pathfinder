---
phase: 06.6-sync-operations-and-logging
plan: 03
type: execute
wave: 2
depends_on: ["06.6-01"]
files_modified:
  - core/views/ci_workflows.py
  - core/templates/core/ci_workflows/repo_detail.html
  - core/templates/core/ci_workflows/_sync_history.html
  - core/templates/core/ci_workflows/_sync_detail.html
autonomous: true

must_haves:
  truths:
    - "Repo detail page shows sync history section with recent sync logs"
    - "Each sync log row shows status, trigger, SHA, timing, and step counts"
    - "Clicking a sync log expands to show per-step entries (added, updated, archived, skipped)"
    - "Branch protection status is displayed on the repo info card"
    - "Sync history is loadable via HTMX for dynamic updates"
  artifacts:
    - path: "core/templates/core/ci_workflows/repo_detail.html"
      provides: "Sync history section and protection status display"
      contains: "sync_logs"
    - path: "core/templates/core/ci_workflows/_sync_history.html"
      provides: "HTMX partial for sync history table"
      contains: "sync_log"
    - path: "core/templates/core/ci_workflows/_sync_detail.html"
      provides: "HTMX partial for expandable sync entry detail"
      contains: "entries"
    - path: "core/views/ci_workflows.py"
      provides: "Sync logs context in StepsRepoDetailView and new SyncDetailView"
      contains: "sync_logs"
  key_links:
    - from: "core/templates/core/ci_workflows/repo_detail.html"
      to: "core/views/ci_workflows.py"
      via: "sync_logs context variable and HTMX partial loading"
      pattern: "sync_logs"
    - from: "core/templates/core/ci_workflows/_sync_detail.html"
      to: "core/views/ci_workflows.py"
      via: "SyncDetailView returns entries for a sync log"
      pattern: "SyncDetailView"
---

<objective>
Add sync history UI to the steps repository detail page showing recent sync logs with expandable per-step detail, and display branch protection status.

Purpose: Operators can see when and how syncs ran, what changed per step, and whether branch protection is configured. This closes GAP-07 UI requirements.
Output: Updated repo detail template with sync history section, HTMX partials for sync table and detail expansion.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-sync-operations-and-logging/06.6-RESEARCH.md
@.planning/phases/06.6-sync-operations-and-logging/06.6-01-SUMMARY.md
@core/views/ci_workflows.py
@core/templates/core/ci_workflows/repo_detail.html
@core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sync logs to StepsRepoDetailView and create SyncDetailView</name>
  <files>core/views/ci_workflows.py, core/urls.py</files>
  <action>
**core/views/ci_workflows.py -- StepsRepoDetailView.get():**

Add sync_logs to the context. After the existing context building (around line 128, before the render call), add:

```python
# Sync history (most recent 20)
sync_logs = repo.sync_logs.prefetch_related("entries").order_by("-started_at")[:20]
```

Add `sync_logs` to the render context dict:
```python
"sync_logs": sync_logs,
```

**New SyncDetailView** -- Add after StepsRepoScanStatusView (or another convenient location):

```python
class SyncDetailView(LoginRequiredMixin, View):
    """HTMX partial for sync log detail with per-step entries."""

    def get(self, request, repo_name, sync_id):
        from core.models import StepsRepoSyncLog
        sync_log = get_object_or_404(
            StepsRepoSyncLog, id=sync_id, repository__name=repo_name
        )
        return render(
            request,
            "core/ci_workflows/_sync_detail.html",
            {"sync_log": sync_log, "entries": sync_log.entries.all()},
        )
```

**core/urls.py** -- Add the new view to ci_workflows_patterns. Import `SyncDetailView` from `.views.ci_workflows` at the top. Add URL:

```python
path(
    "repos/<dns:repo_name>/sync/<int:sync_id>/",
    SyncDetailView.as_view(),
    name="sync_detail",
),
```

Place this after the existing `repos/<dns:repo_name>/delete/` path and before the `steps/` paths.
  </action>
  <verify>
Run `uv run python manage.py check` -- must pass.
Run `uv run python manage.py shell -c "from django.urls import reverse; print(reverse('ci_workflows:sync_detail', kwargs={'repo_name': 'test', 'sync_id': 1}))"` -- prints the URL.
  </verify>
  <done>StepsRepoDetailView passes sync_logs context. SyncDetailView returns HTMX partial for a single sync log's entries. URL registered at repos/<repo_name>/sync/<sync_id>/.</done>
</task>

<task type="auto">
  <name>Task 2: Create sync history templates and update repo detail page</name>
  <files>core/templates/core/ci_workflows/repo_detail.html, core/templates/core/ci_workflows/_sync_history.html, core/templates/core/ci_workflows/_sync_detail.html</files>
  <action>
**core/templates/core/ci_workflows/repo_detail.html** -- Make two changes:

1. **Branch protection status** -- In the "Repository Info" card (the grid with Default Branch, Connection, Last Scanned, Created), add a fifth item AFTER the "Created" column. Change the grid from `grid-cols-2 md:grid-cols-4` to `grid-cols-2 md:grid-cols-5`:

```html
<div>
    <span class="text-dark-muted">Branch Protection</span>
    <p class="mt-1">
        {% if repo.protection_valid %}
        <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-green-500/20 text-green-400">Protected</span>
        {% else %}
        <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-amber-500/20 text-amber-400">Not Verified</span>
        {% endif %}
    </p>
</div>
```

2. **Sync History section** -- Add BEFORE the "Imported Steps" section (before the `<!-- Imported Steps by Phase -->` comment), insert:

```html
<!-- Sync History -->
{% if sync_logs %}
<div class="mb-6">
    <h2 class="text-lg font-semibold text-dark-text mb-3">Sync History</h2>
    {% include "core/ci_workflows/_sync_history.html" %}
</div>
{% endif %}
```

**core/templates/core/ci_workflows/_sync_history.html** -- Create new template:

```html
{% load humanize %}
<div class="bg-dark-surface border border-dark-border rounded-lg overflow-hidden">
    <table class="w-full text-sm">
        <thead>
            <tr class="border-b border-dark-border text-dark-muted text-left">
                <th class="px-4 py-3 font-medium">Status</th>
                <th class="px-4 py-3 font-medium">Trigger</th>
                <th class="px-4 py-3 font-medium">Commit</th>
                <th class="px-4 py-3 font-medium">Protection</th>
                <th class="px-4 py-3 font-medium">Changes</th>
                <th class="px-4 py-3 font-medium">When</th>
                <th class="px-4 py-3 font-medium"></th>
            </tr>
        </thead>
        <tbody class="divide-y divide-dark-border">
            {% for log in sync_logs %}
            <tr class="hover:bg-dark-border/20">
                <td class="px-4 py-3">
                    {% if log.status == "success" %}
                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-green-500/20 text-green-400">Success</span>
                    {% elif log.status == "partial" %}
                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-amber-500/20 text-amber-400">Partial</span>
                    {% elif log.status == "failed" %}
                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-red-500/20 text-red-400">Failed</span>
                    {% elif log.status == "skipped" %}
                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-500/20 text-gray-400">Skipped</span>
                    {% elif log.status == "running" %}
                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-blue-500/20 text-blue-400">Running</span>
                    {% endif %}
                </td>
                <td class="px-4 py-3 text-dark-text capitalize">{{ log.trigger }}</td>
                <td class="px-4 py-3">
                    {% if log.commit_sha %}
                    <code class="text-xs text-dark-muted bg-dark-bg px-1.5 py-0.5 rounded">{{ log.commit_sha|truncatechars:11 }}</code>
                    {% else %}
                    <span class="text-dark-muted">-</span>
                    {% endif %}
                </td>
                <td class="px-4 py-3">
                    {% if log.protection_valid %}
                    <span class="text-green-400 text-xs">OK</span>
                    {% else %}
                    <span class="text-amber-400 text-xs">Warn</span>
                    {% endif %}
                </td>
                <td class="px-4 py-3 text-dark-muted text-xs">
                    {% if log.status == "skipped" %}
                    <span>No changes</span>
                    {% else %}
                    {% if log.steps_added %}<span class="text-green-400">+{{ log.steps_added }}</span> {% endif %}
                    {% if log.steps_updated %}<span class="text-blue-400">~{{ log.steps_updated }}</span> {% endif %}
                    {% if log.steps_archived %}<span class="text-red-400">-{{ log.steps_archived }}</span> {% endif %}
                    {% if not log.steps_added and not log.steps_updated and not log.steps_archived %}<span>-</span>{% endif %}
                    {% endif %}
                </td>
                <td class="px-4 py-3 text-dark-muted text-xs">{{ log.started_at|naturaltime }}</td>
                <td class="px-4 py-3 text-right">
                    {% if log.status != "skipped" and log.status != "running" %}
                    <button
                        hx-get="{% url 'ci_workflows:sync_detail' repo_name=repo.name sync_id=log.id %}"
                        hx-target="#sync-detail-{{ log.id }}"
                        hx-swap="innerHTML"
                        class="text-dark-accent hover:text-dark-accent/80 text-xs">
                        Details
                    </button>
                    {% endif %}
                </td>
            </tr>
            <tr id="sync-detail-{{ log.id }}">
                {# HTMX detail will load here #}
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
```

Note: The `truncatechars:11` on commit_sha shows first 8 chars + "..." which matches the standard short SHA display (8 chars).

**core/templates/core/ci_workflows/_sync_detail.html** -- Create new template:

```html
<td colspan="7" class="px-4 py-3 bg-dark-bg/50">
    <div class="space-y-1">
        {% for entry in entries %}
        <div class="flex items-start gap-3 text-xs py-1">
            <span class="shrink-0 w-16">
                {% if entry.severity == "error" %}
                <span class="text-red-400 font-medium">ERROR</span>
                {% elif entry.severity == "warning" %}
                <span class="text-amber-400 font-medium">WARN</span>
                {% else %}
                <span class="text-dark-muted font-medium">INFO</span>
                {% endif %}
            </span>
            <span class="shrink-0 w-16">
                {% if entry.action == "added" %}
                <span class="text-green-400">Added</span>
                {% elif entry.action == "updated" %}
                <span class="text-blue-400">Updated</span>
                {% elif entry.action == "archived" %}
                <span class="text-red-400">Archived</span>
                {% elif entry.action == "skipped" %}
                <span class="text-gray-400">Skipped</span>
                {% endif %}
            </span>
            {% if entry.step_slug %}
            <code class="shrink-0 text-dark-text bg-dark-surface px-1.5 py-0.5 rounded">{{ entry.step_slug }}</code>
            {% endif %}
            <span class="text-dark-muted">{{ entry.message }}</span>
        </div>
        {% empty %}
        <p class="text-dark-muted text-xs py-2">No entries for this sync.</p>
        {% endfor %}
    </div>
</td>
```

After creating templates, run `make build` to rebuild static files (Tailwind may need new classes).
  </action>
  <verify>
Run `uv run python manage.py check` -- must pass.
Run `make build` -- completes without error.
Verify templates exist: `ls core/templates/core/ci_workflows/_sync_history.html core/templates/core/ci_workflows/_sync_detail.html`
  </verify>
  <done>Repo detail page shows branch protection status badge and sync history table. Sync logs show status, trigger, SHA, protection, change counts, and timestamp. Details button expands via HTMX to show per-step entries with severity, action, slug, and message.</done>
</task>

</tasks>

<verification>
1. `uv run python manage.py check` passes
2. `make build` completes
3. All three templates exist in core/templates/core/ci_workflows/
4. StepsRepoDetailView context includes sync_logs
5. SyncDetailView URL resolves correctly
6. Branch protection status displayed in repo info card
</verification>

<success_criteria>
- Repo detail page shows "Branch Protection" status (Protected/Not Verified badge)
- Sync history table shows recent sync logs with status, trigger, commit SHA, protection, change counts, timing
- Details button on sync logs loads per-step entries via HTMX
- Entry detail shows severity (ERROR/WARN/INFO), action (Added/Updated/Archived/Skipped), step slug, and message
- Running and skipped syncs do not show Details button
</success_criteria>

<output>
After completion, create `.planning/phases/06.6-sync-operations-and-logging/06.6-03-SUMMARY.md`
</output>
