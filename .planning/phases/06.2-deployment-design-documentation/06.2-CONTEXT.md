# Phase 6.2: Deployment Design Documentation - Context

**Gathered:** 2026-02-12
**Status:** Ready for planning

<domain>
## Phase Boundary

RFC-style design documentation for Deployments, organized similar to `docs/ci-workflows/`. Research-driven, abstract from implementation. Produces design docs that guide Phase 7 implementation — does NOT write code.

Existing material to build on:
- `docs/deployments.md` — high-level deployment design (methods, artifact matching, promotions)
- `docs/deployments/initial.md` — research brief (3 method tiers, resources, secrets, key questions)
- `docs/ci-workflows/` — the reference structure (README + 7 focused documents)

</domain>

<decisions>
## Implementation Decisions

### Promotion & Workflow

- **Auto-deploy to default environment**: Fully automatic — merge to main → build succeeds → deploy starts immediately, no human in the loop
- **Promotion approval model**: Role-based only — anyone with the right project role can promote (owner for production, contributor for non-production). No separate approval workflow.
- **Rollback strategy**: Re-deploy previous known-good artifact. Simple, artifact-focused. No git-revert model.
- **Promotion path ordering**: Recommended by environment priority but not enforced. First deployment MUST go to the project's default environment. Research industry standards for whether sequential enforcement is common or whether recommendation-only is the norm.
- **Failure handling**: Alert + manual action. Mark deployment as failed, notify user. User decides whether to rollback, fix-forward, or investigate. No auto-rollback.
- **Deployment status tracking**: Real-time updates — live status progression in UI (pending → running → health check → success/failed), similar to existing build tracking pattern.
- **Environment variable binding**: Snapshot at deploy time. Env vars frozen when deployment starts. Changing vars requires re-deploy. Predictable and auditable.
- **Concurrent deployments**: No locking needed across services. Services are independent — concurrent deploys to different services in same environment are fine. Same service+environment blocked (only one active deployment at a time).

### Claude's Discretion
- Document structure breakdown (how to split deployment domain into focused docs)
- Scope breadth across deployment methods (Docker, K8s, GitOps, CI-managed) — cover what's needed for the design to be complete
- Resource model depth (environment resources, secrets) — determine appropriate level of detail
- Specific document naming and organization within `docs/deployments/`

</decisions>

<specifics>
## Specific Ideas

- Reference structure is `docs/ci-workflows/` — README with overview + index table, then focused documents per topic
- The existing `docs/deployments.md` and `docs/deployments/initial.md` contain substantial material to incorporate/restructure
- CI workflows docs include a "What CI Workflows Do Not Do" section — deployments docs should have equivalent boundary-setting
- Build verification states table in CI workflows README shows how to create quick-reference tables for deployment states

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 06.2-deployment-design-documentation*
*Context gathered: 2026-02-12*
