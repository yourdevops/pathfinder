---
phase: 06.10-pluggable-webhook-routing-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/github/webhooks.py
  - plugins/github/urls.py
  - plugins/base.py
  - plugins/github/plugin.py
  - core/views/webhooks.py
  - core/views/__init__.py
  - core/urls.py
  - pathfinder/urls.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "GitHub webhook events (workflow_run, push) are handled by plugins/github/webhooks.py"
    - "Single webhook endpoint exists at /integrations/github/webhook/ dispatching by X-GitHub-Event header"
    - "Old /webhooks/build/ and /webhooks/steps-repo/ URLs no longer exist (404)"
    - "BasePlugin has get_webhook_url() returning empty string by default"
    - "GitHubPlugin.get_webhook_url() returns the full webhook URL using reverse()"
  artifacts:
    - path: "plugins/github/webhooks.py"
      provides: "GitHub webhook handler with signature verification and event dispatch"
      contains: "github_webhook"
    - path: "plugins/github/urls.py"
      provides: "Webhook URL pattern mounted under plugin namespace"
      contains: "webhook"
    - path: "plugins/base.py"
      provides: "get_webhook_url() method on BasePlugin"
      contains: "get_webhook_url"
  key_links:
    - from: "plugins/github/urls.py"
      to: "plugins/github/webhooks.py"
      via: "webhook URL pattern import"
      pattern: "webhooks\\.github_webhook"
    - from: "pathfinder/urls.py"
      to: "plugins/github/urls.py"
      via: "plugin URL autodiscovery loop"
      pattern: "integrations.*plugin_name"
---

<objective>
Move all GitHub-specific webhook handlers from core into the GitHub plugin, register a single webhook endpoint at /integrations/github/webhook/, and add get_webhook_url() to the plugin interface.

Purpose: Enforces Architecture Rule 1 (core must be plugin-agnostic) by removing all GitHub-specific webhook code from core. Establishes the pluggable webhook framework via the existing plugin URL autodiscovery pattern.

Output: plugins/github/webhooks.py (new), updated URL routing, deleted core/views/webhooks.py
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.10-pluggable-webhook-routing-framework/06.10-RESEARCH.md

@core/views/webhooks.py
@plugins/github/urls.py
@plugins/github/plugin.py
@plugins/base.py
@core/urls.py
@pathfinder/urls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugins/github/webhooks.py and register webhook URL</name>
  <files>
    plugins/github/webhooks.py
    plugins/github/urls.py
  </files>
  <action>
Create `plugins/github/webhooks.py` by moving ALL content from `core/views/webhooks.py`:
- Move `verify_github_signature()`, `identify_service_from_webhook()`, `extract_artifact_ref()` as-is
- Create a single `github_webhook()` view function decorated with `@csrf_exempt`
- `github_webhook()` checks `request.method != "POST"` and returns 200
- Reads `X-GitHub-Event` header to dispatch:
  - `"workflow_run"` -> call `_handle_workflow_run(request)` (the current `build_webhook` logic)
  - `"push"` -> call `_handle_push(request)` (the current `steps_repo_webhook` logic)
  - anything else -> return `HttpResponse(status=200)`
- `_handle_workflow_run(request)` contains the full body of current `build_webhook()` EXCEPT the method check (already done in dispatcher)
- `_handle_push(request)` contains the full body of current `steps_repo_webhook()` EXCEPT the method check and event_type check (already done in dispatcher)
- Keep all lazy imports inside handler functions (e.g., `from core.models import ...`) to avoid circular imports

Update `plugins/github/urls.py`:
- Add import: `from . import webhooks`
- Add URL pattern: `path("webhook/", webhooks.github_webhook, name="webhook")`
- This automatically mounts at `/integrations/github/webhook/` via the existing plugin URL autodiscovery loop in `pathfinder/urls.py`
  </action>
  <verify>
Run `uv run python manage.py check` -- no errors. Run `uv run python -c "from django.urls import reverse; print(reverse('github:webhook'))"` and confirm it prints `/integrations/github/webhook/`.
  </verify>
  <done>
`plugins/github/webhooks.py` exists with all webhook handler logic. `/integrations/github/webhook/` is a valid URL routed to `github_webhook` view.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add get_webhook_url() to plugin interface and clean up core</name>
  <files>
    plugins/base.py
    plugins/github/plugin.py
    core/views/webhooks.py
    core/urls.py
    pathfinder/urls.py
  </files>
  <action>
**plugins/base.py** -- Add `get_webhook_url()` method to `BasePlugin`:
```python
def get_webhook_url(self, external_url: str) -> str:
    """Return the full external webhook URL for this plugin.

    Plugins that receive webhooks override this to return their
    webhook endpoint URL. Core code calls this without knowing
    the plugin name, keeping core plugin-agnostic.

    Args:
        external_url: The site's external URL (e.g., 'https://pathfinder.example.com').

    Returns:
        Full webhook URL string, or empty string if plugin has no webhook.
    """
    return ""
```

**plugins/github/plugin.py** -- Override `get_webhook_url()` in `GitHubPlugin`:
```python
def get_webhook_url(self, external_url: str) -> str:
    """Return the full external webhook URL for this plugin."""
    from django.urls import reverse
    webhook_path = reverse("github:webhook")
    return f"{external_url.rstrip('/')}{webhook_path}"
```

**core/views/webhooks.py** -- DELETE this file entirely. All handler code now lives in `plugins/github/webhooks.py`. Per locked decision: clean break, no legacy code.

**core/urls.py** -- Remove the `webhooks` import from the `from .views import (...)` block (line 59). Remove the `webhooks_patterns` list (lines 441-444 defining `build_webhook` and `steps_repo_webhook` patterns).

**pathfinder/urls.py** -- Remove `webhooks_patterns` from the import block (line 39). Remove the `path("webhooks/", ...)` line (line 67).
  </action>
  <verify>
Run `uv run python manage.py check` -- no errors. Confirm `core/views/webhooks.py` does not exist. Grep the entire codebase for `/webhooks/build/` and `/webhooks/steps-repo/` literal strings -- should return zero results in Python files (only planning/docs files).
  </verify>
  <done>
`BasePlugin.get_webhook_url()` exists returning empty string by default. `GitHubPlugin.get_webhook_url()` returns the full webhook URL via `reverse()`. `core/views/webhooks.py` is deleted. No `/webhooks/` URL prefix exists in URL configuration. Django check passes.
  </done>
</task>

</tasks>

<verification>
1. `uv run python manage.py check` passes
2. `uv run python -c "from django.urls import reverse; print(reverse('github:webhook'))"` prints `/integrations/github/webhook/`
3. `core/views/webhooks.py` does not exist
4. No Python source files contain hardcoded `/webhooks/build/` or `/webhooks/steps-repo/` URL patterns (outside planning docs)
5. `uv run python -c "from plugins.github.plugin import GitHubPlugin; p = GitHubPlugin(); print(p.get_webhook_url('https://example.com'))"` prints `https://example.com/integrations/github/webhook/`
</verification>

<success_criteria>
- GitHub webhook handler exists at /integrations/github/webhook/ via plugin URL autodiscovery
- Single dispatch point routes by X-GitHub-Event header (workflow_run, push)
- Plugin interface has get_webhook_url() for plugin-agnostic URL resolution
- All GitHub-specific webhook code removed from core/
- Django check passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06.10-pluggable-webhook-routing-framework/06.10-01-SUMMARY.md`
</output>
