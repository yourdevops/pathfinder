---
phase: 06.5-workflow-and-build-model-hardening
verified: 2026-02-16T19:00:42Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 6.5: Workflow and Build Model Hardening Verification Report

**Phase Goal:** CIWorkflow has an explicit engine field set at creation, step ordering is validated before save, and an "archived" status allows graceful deprecation. Build model is engine-agnostic with a generic `ci_run_id` and has a distinct "revoked" verification status.

**Verified:** 2026-02-16T19:00:42Z
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CIWorkflow.engine is set at creation and used everywhere instead of step-derived engine | ✓ VERIFIED | `engine` CharField exists in CIWorkflow model (line 695), used in 6 locations in views, 2 in tasks, 2 in git_utils, 1 in services. Zero occurrences of `first_step.step.engine` in codebase. |
| 2 | Invalid step orders are rejected with a clear message in the workflow composer | ✓ VERIFIED | `_validate_step_order()` function exists (line 402), called in composer POST handler (line 556), returns descriptive error messages like "Step X requires Y runtime setup". |
| 3 | Archived workflows are hidden from new onboarding but remain functional for existing services | ✓ VERIFIED | Status choices include "archived" (line 708), `get_available_workflows_for_project()` filters by `status="published"` which excludes archived. UI shows archived badge and banner. |
| 4 | github_run_id is renamed to ci_run_id (engine-agnostic) | ✓ VERIFIED | Build model has `ci_run_id` field (line 877), used in tasks (lines 685), views (line 1108), templates (_build_row_expanded.html lines 8, 35-36). Zero occurrences of `github_run_id` in code (only in docs/planning). |
| 5 | Revoked versions produce "revoked" verification status with distinct UI badge | ✓ VERIFIED | Verification status includes "revoked" choice (line 898), verify_build task sets it (line 576), orange badge in _build_row.html (lines 49-55) with warning icon. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/models.py` | CIWorkflow.engine CharField + archived status choice | ✓ VERIFIED | Lines 695-699: engine field with default "github_actions". Line 708: status choices include "archived". Line 877: ci_run_id field. Lines 893-902: verification_status with "revoked". |
| `core/views/ci_workflows.py` | Step ordering validation + WorkflowArchiveView | ✓ VERIFIED | Lines 402-426: `_validate_step_order()` validates setup-before-use. Lines 828-840: WorkflowArchiveView toggles archived status. 6 locations use `workflow.engine`. |
| `core/urls.py` | Archive URL pattern | ✓ VERIFIED | Line 80: WorkflowArchiveView imported. Line 188-189: workflow_archive URL pattern registered. |
| `plugins/base.py` | map_run_status method on CICapableMixin | ✓ VERIFIED | Lines 157-162: Abstract method signature with docstring, raises NotImplementedError. |
| `plugins/github/plugin.py` | GitHubPlugin.map_run_status implementation | ✓ VERIFIED | Lines 191-202: Concrete implementation mapping GitHub status/conclusion to Build status (queued/in_progress→running, completed→mapped). |
| `core/templates/core/services/_build_row.html` | Revoked verification badge | ✓ VERIFIED | Lines 49-55: Orange badge with warning icon, title "Built with a revoked workflow version". |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| core/views/ci_workflows.py | core/models.py | workflow.engine replaces first_step.step.engine derivation | ✓ WIRED | 6 locations in views use `workflow.engine` (lines 519, 633, 725, 789, 898, 1010). Zero `first_step.step.engine` patterns found. |
| core/tasks.py | core/models.py | workflow.engine in verify_build and push_ci_manifest | ✓ WIRED | Line 537: `engine = workflow.engine` in verify_build. Line 752: `engine = service.ci_workflow.engine` in push_ci_manifest. |
| core/models.py | core/models.py | get_available_workflows_for_project excludes archived | ✓ WIRED | Lines 949-952: Both code paths filter `status="published"` which naturally excludes "archived". |
| core/tasks.py | plugins/github/plugin.py | plugin.map_run_status() call replacing Build.map_github_status() | ✓ WIRED | Line 670: `status = plugin.map_run_status(...)`. Zero `map_github_status` calls in code (only in docs). |
| core/tasks.py | core/models.py | ci_run_id field in update_or_create | ✓ WIRED | Line 685: `ci_run_id=run_id` in Build.objects.update_or_create(). |
| core/tasks.py | core/models.py | verification_status='revoked' for revoked versions | ✓ WIRED | Lines 575-576: `elif version_match.status == CIWorkflowVersion.Status.REVOKED: verification_status = "revoked"`. |

### Requirements Coverage

No requirements explicitly mapped to Phase 6.5 in REQUIREMENTS.md (this is remediation work addressing gaps GAP-08, GAP-09, GAP-10, GAP-15, GAP-16 from docs/ci-workflows/REMEDIATION.md).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| - | - | None found | - | - |

No TODO/FIXME/PLACEHOLDER comments. No stub implementations. No orphaned code. All implementations are substantive and wired.

### Migrations Verified

| Migration | Status | Details |
|-----------|--------|---------|
| 0025_add_engine_and_archived_to_ciworkflow.py | ✓ APPLIED | Adds engine field and expands status choices to include archived. |
| 0026_rename_github_run_id_build_ci_run_id_and_more.py | ✓ APPLIED | Renames github_run_id to ci_run_id and adds "revoked" verification status. |

Confirmed via `uv run python manage.py showmigrations core` - both migrations marked [X].

### Human Verification Required

#### 1. Step Ordering Validation Error Display

**Test:** Create a workflow with runtime-dependent step (e.g., python-build) before setup step (e.g., python-uv)
**Expected:** Error message appears above composer: "python-build requires python runtime setup. Add a setup step for python before this step."
**Why human:** Visual verification of error message display and workflow remains editable with current steps.

#### 2. Archive/Restore Workflow Toggle

**Test:**
1. Archive a workflow from detail page
2. Verify archived badge appears in workflow list with reduced opacity
3. Verify archived banner shows in detail page
4. Verify archived workflow is NOT in service creation wizard dropdown
5. Restore the workflow
6. Verify workflow reappears in service wizard

**Expected:**
- Archive button → Restore button
- Workflow list shows gray "Archived" badge and opacity-60 on card
- Detail page shows gray banner: "This workflow is archived..."
- Service wizard excludes archived workflows from dropdown
- Restore button → Archive button

**Why human:** Multi-page workflow requiring navigation and visual UI verification.

#### 3. Revoked Verification Badge Display

**Test:**
1. Create a build with a workflow version
2. Revoke that version
3. Run verify_build task for the build
4. Check build list

**Expected:** Build row shows orange "Revoked" badge with warning icon instead of red "Unauthorized" badge

**Why human:** Requires background task execution and visual badge verification.

#### 4. Engine Field Persistence Across Workflow Operations

**Test:**
1. Create workflow with engine "github_actions"
2. Add/remove steps
3. Fork workflow
4. Publish version
5. View manifest preview

**Expected:** Engine value persists across all operations, no runtime derivation from steps

**Why human:** Requires testing multiple workflow operations to ensure engine field flows correctly through all code paths.

## Summary

**All 5 success criteria verified. Phase 6.5 goal achieved.**

### Key Accomplishments

1. **CIWorkflow.engine field** - Eliminates fragile step-derived engine pattern across 10+ call sites (views, tasks, git_utils)
2. **Step ordering validation** - Enforces setup-before-use rule with clear, actionable error messages
3. **Archived workflow status** - Graceful deprecation without breaking existing services
4. **Engine-agnostic Build model** - `ci_run_id` replaces `github_run_id` across model, tasks, views, templates
5. **Revoked verification status** - Distinct orange badge for revoked versions (not lumped with unauthorized)
6. **Plugin-based status mapping** - `map_run_status()` on CICapableMixin enables engine-specific status mapping

### Implementation Quality

- **No stubs** - All functions have complete implementations
- **No TODOs** - No placeholder comments or deferred work
- **Complete wiring** - All components properly connected and imported
- **Migrations applied** - Both migrations confirmed in database
- **Zero regressions** - No old patterns remain in codebase
- **UI complete** - All badges, buttons, and banners implemented

### Design Pattern Changes

| Old Pattern | New Pattern | Impact |
|-------------|-------------|--------|
| `first_step.step.engine if first_step else "github_actions"` | `workflow.engine` | Direct field access, no runtime queries |
| Status filter `published/draft` | Status filter `published/draft/archived` | Graceful workflow deprecation |
| `Build.github_run_id` | `Build.ci_run_id` | Engine-agnostic naming |
| `Build.map_github_status()` | `plugin.map_run_status()` | Engine-specific logic in plugins |
| Revoked → "unauthorized" | Revoked → "revoked" | Distinct verification status |

---

_Verified: 2026-02-16T19:00:42Z_
_Verifier: Claude (gsd-verifier)_
