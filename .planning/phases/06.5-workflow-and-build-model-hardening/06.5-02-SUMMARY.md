---
phase: 06.5-workflow-and-build-model-hardening
plan: 02
subsystem: database, api, ui
tags: [django-models, ci-engine, build-verification, plugin-interface]

# Dependency graph
requires:
  - phase: 06.5-01
    provides: "CIWorkflow.engine field, step validation, archive status"
  - phase: 06.1
    provides: "CIWorkflowVersion model, Build verification_status, manifest verification"
provides:
  - "Engine-agnostic Build.ci_run_id field (replaces github_run_id)"
  - "Revoked verification status with distinct orange badge"
  - "CICapableMixin.map_run_status plugin interface method"
  - "GitHubPlugin.map_run_status implementation"
affects: [07-deployments, future-ci-engines]

# Tech tracking
tech-stack:
  added: []
  patterns: ["CI status mapping delegated to plugin interface"]

key-files:
  created:
    - "core/migrations/0026_rename_github_run_id_build_ci_run_id_and_more.py"
  modified:
    - "core/models.py"
    - "core/tasks.py"
    - "core/views/services.py"
    - "plugins/base.py"
    - "plugins/github/plugin.py"
    - "core/templates/core/services/_build_row.html"
    - "core/templates/core/services/_build_row_expanded.html"

key-decisions:
  - "Build.ci_run_id replaces github_run_id for engine-agnostic naming"
  - "Revoked versions produce distinct 'revoked' verification_status, not 'unauthorized'"
  - "map_run_status on CICapableMixin interface (not Build model) for engine-specific mapping"

patterns-established:
  - "CI status mapping via plugin.map_run_status(): each CI engine implements its own status/conclusion mapping"

# Metrics
duration: 3min
completed: 2026-02-16
---

# Phase 6.5 Plan 2: Build Model Hardening Summary

**Engine-agnostic Build model with ci_run_id field, revoked verification badge, and map_run_status plugin interface**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-16T18:53:08Z
- **Completed:** 2026-02-16T18:56:12Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- Renamed Build.github_run_id to ci_run_id across model, tasks, views, and templates
- Added "revoked" verification status with distinct orange badge in build UI
- Moved status mapping from Build.map_github_status to CICapableMixin.map_run_status plugin interface
- GitHubPlugin implements map_run_status with identical logic (queued/in_progress -> running, completed -> mapped conclusion)

## Task Commits

Each task was committed atomically:

1. **Task 1: Rename github_run_id to ci_run_id, add revoked verification choice, move map_github_status to plugin** - `13edbc4` (feat)
2. **Task 2: Update all references, revoked verification status, revoked badge in templates** - `65c2fc6` (feat)

## Files Created/Modified
- `core/models.py` - Renamed github_run_id to ci_run_id, added "revoked" to verification_status choices, removed map_github_status
- `plugins/base.py` - Added map_run_status abstract method to CICapableMixin
- `plugins/github/plugin.py` - Implemented map_run_status on GitHubPlugin
- `core/migrations/0026_rename_github_run_id_build_ci_run_id_and_more.py` - RenameField + AlterField migration
- `core/tasks.py` - Updated poll_build_details to use plugin.map_run_status and ci_run_id; verify_build now maps revoked versions to "revoked"
- `core/views/services.py` - Updated BuildLogsView to use build.ci_run_id
- `core/templates/core/services/_build_row.html` - Added orange "Revoked" verification badge, removed redundant revoked warning
- `core/templates/core/services/_build_row_expanded.html` - Replaced github_run_id with ci_run_id in build header and GitHub link

## Decisions Made
- Build.ci_run_id replaces github_run_id for engine-agnostic naming
- Revoked versions produce distinct "revoked" verification_status instead of being lumped with "unauthorized"
- map_run_status lives on CICapableMixin interface so each CI engine can implement its own status mapping

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-commit ruff-format hook reformatted plugins/base.py on first attempt; re-staged and committed successfully on retry.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Build model is fully engine-agnostic (ci_run_id, plugin-based status mapping)
- Revoked versions have clear visual distinction in the UI
- Phase 6.5 is complete; all workflow and build model hardening gaps addressed

---
*Phase: 06.5-workflow-and-build-model-hardening*
*Completed: 2026-02-16*
