---
phase: 06.7-version-lifecycle-automation
plan: 01
subsystem: database, ui
tags: [django-models, htmx, auto-update, version-retention, toggle]

# Dependency graph
requires:
  - phase: 06.1-ci-workflows-gap
    provides: CIWorkflowVersion model, Service ci_workflow_version FK, CI tab template
provides:
  - Service.auto_update_patch BooleanField for controlling patch auto-updates per service
  - SiteConfiguration.version_retention_days for cleanup policy
  - SiteConfiguration.last_cleanup_at for tracking cleanup runs
  - Auto-update toggle UI in service CI tab with HTMX inline update
affects: [06.7-02, 06.7-03, 06.7-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "HTMX toggle partial pattern with dedicated _auto_update_toggle.html template"

key-files:
  created:
    - core/migrations/0028_service_auto_update_patch_and_more.py
    - core/templates/core/services/_auto_update_toggle.html
  modified:
    - core/models.py
    - core/views/services.py
    - core/urls.py
    - core/templates/core/services/_ci_tab.html

key-decisions:
  - "HTMX toggle partial for auto-update instead of full page reload"
  - "Toggle rendered via include template for clean HTMX swap target"

patterns-established:
  - "HTMX toggle partial: dedicated template with id for outerHTML swap"

requirements-completed: [GAP-11, GAP-12]

# Metrics
duration: 4min
completed: 2026-02-16
---

# Phase 06.7 Plan 01: Model Fields and Auto-Update Toggle Summary

**Service auto_update_patch boolean with HTMX toggle on CI tab, plus SiteConfiguration retention fields for version cleanup policy**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-16T21:43:29Z
- **Completed:** 2026-02-16T21:47:33Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Added Service.auto_update_patch BooleanField (default=True) for per-service patch auto-update control
- Added SiteConfiguration.version_retention_days (default=365) and last_cleanup_at for version cleanup policy
- Built auto-update toggle in service CI tab with HTMX inline update and dedicated partial template

## Task Commits

Each task was committed atomically:

1. **Task 1: Add auto_update_patch to Service and retention fields to SiteConfiguration** - `11f1c92` (feat)
2. **Task 2: Add auto-update toggle to service CI tab** - `4578422` (feat)

## Files Created/Modified
- `core/models.py` - Added auto_update_patch on Service, version_retention_days and last_cleanup_at on SiteConfiguration
- `core/migrations/0028_service_auto_update_patch_and_more.py` - Migration for the three new fields
- `core/views/services.py` - Added ServiceAutoUpdateToggleView for HTMX toggle endpoint
- `core/urls.py` - Added URL pattern for auto-update toggle
- `core/templates/core/services/_auto_update_toggle.html` - Toggle partial with HTMX swap
- `core/templates/core/services/_ci_tab.html` - Included toggle in CI workflow card

## Decisions Made
- Used HTMX toggle partial pattern (dedicated template with id for outerHTML swap) instead of full page reload, matching existing project patterns
- Toggle partial is a separate included template for clean HTMX swap targeting
- Toggle is only visible when a CI workflow is assigned to the service

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Plan referenced `theme/templates/core/services/_ci_tab.html` but actual path is `core/templates/core/services/_ci_tab.html` -- adapted file paths accordingly

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Model fields are in place for Plan 02 (auto-update management command) and Plan 03 (version retention cleanup)
- Toggle UI ready for immediate use on service CI tabs

## Self-Check: PASSED

- All 7 files verified present on disk
- Both task commits (11f1c92, 4578422) verified in git log

---
*Phase: 06.7-version-lifecycle-automation*
*Completed: 2026-02-16*
