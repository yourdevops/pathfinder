---
phase: 06.7-version-lifecycle-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/models.py
  - core/migrations/NNNN_auto_update_and_retention.py
  - theme/templates/core/services/_ci_tab.html
autonomous: true
requirements: [GAP-11, GAP-12]

must_haves:
  truths:
    - "Service model has auto_update_patch boolean field defaulting to True"
    - "SiteConfiguration has version_retention_days integer field defaulting to 365"
    - "SiteConfiguration has last_cleanup_at nullable datetime field"
    - "Service CI tab shows auto-update toggle that can be switched on/off"
  artifacts:
    - path: "core/models.py"
      provides: "auto_update_patch on Service, retention fields on SiteConfiguration"
      contains: "auto_update_patch"
    - path: "core/migrations/"
      provides: "Database migration for new fields"
    - path: "theme/templates/core/services/_ci_tab.html"
      provides: "Auto-update toggle UI on service CI tab"
      contains: "auto_update_patch"
  key_links:
    - from: "theme/templates/core/services/_ci_tab.html"
      to: "core/models.py"
      via: "service.auto_update_patch field"
      pattern: "auto_update_patch"
---

<objective>
Add model fields for auto-update toggle and version retention settings, plus the service-level auto-update toggle UI.

Purpose: Establishes the data layer for version lifecycle automation -- the auto_update_patch boolean controls which services receive patch auto-updates, and retention fields control cleanup policy.
Output: Migration applied, Service has auto_update_patch, SiteConfiguration has retention settings, service CI tab has toggle.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.7-version-lifecycle-automation/06.7-RESEARCH.md
@core/models.py
@theme/templates/core/services/_ci_tab.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto_update_patch to Service and retention fields to SiteConfiguration</name>
  <files>core/models.py, core/migrations/</files>
  <action>
On the Service model (look for ci_manifest_status and ci_manifest_push_method fields), add:

```python
auto_update_patch = models.BooleanField(
    default=True,
    help_text="Automatically update CI manifest when a patch version is published",
)
```

On the SiteConfiguration model (singleton at the top of models.py), add two fields:

```python
version_retention_days = models.IntegerField(
    default=365,
    help_text="Days before manifest content is cleared from old versions",
)
last_cleanup_at = models.DateTimeField(
    null=True,
    blank=True,
    help_text="Timestamp of last version cleanup run",
)
```

Run `uv run python manage.py makemigrations core` and `uv run python manage.py migrate`.

Do NOT add any new choices to ci_manifest_status -- the "pending_pr" choice already exists on the Service model.
  </action>
  <verify>
Run `uv run python manage.py check` -- no errors.
Run `uv run python manage.py showmigrations core` -- new migration applied.
Confirm auto_update_patch, version_retention_days, and last_cleanup_at fields exist with correct defaults.
  </verify>
  <done>Service has auto_update_patch=True by default. SiteConfiguration has version_retention_days=365 and last_cleanup_at=None. Migration applied.</done>
</task>

<task type="auto">
  <name>Task 2: Add auto-update toggle to service CI tab</name>
  <files>theme/templates/core/services/_ci_tab.html, core/views/services.py</files>
  <action>
In the service CI tab template (`_ci_tab.html`), add an auto-update toggle in the "CI Configuration" section (near the existing push method and version pinning controls). Use an HTMX POST form that updates the setting inline.

Add a small card/section with:
- Label: "Auto-update on Patch Versions"
- Description text: "When enabled, this service automatically receives manifest updates when a patch version (same major.minor) is published."
- Toggle checkbox that auto-submits on change via HTMX

The toggle should POST to a new endpoint. In `core/views/services.py`, add a view `ServiceAutoUpdateToggleView` (LoginRequiredMixin, View) that:
1. Gets the service by project_name and service_name
2. Toggles `auto_update_patch` based on the checkbox value
3. Saves the service
4. Returns the updated CI tab partial via HTMX (or a small success response)

Add the URL pattern in `core/urls.py` under the services patterns. Use the existing pattern: `services/<dns:service_name>/auto-update-toggle/`.

Style the toggle consistently with other toggles in the project (e.g., the "Allow Drafts" toggle from project CI settings). Use a simple checkbox with `@change` or `hx-post` and `hx-swap` for immediate feedback.
  </action>
  <verify>
Run `uv run python manage.py check` -- no errors.
Visually verify the template has an auto-update toggle section in the CI tab with HTMX auto-submit behavior.
  </verify>
  <done>Service CI tab shows auto-update toggle. Toggle sends HTMX POST to update auto_update_patch field. Checkbox reflects current state.</done>
</task>

</tasks>

<verification>
- `uv run python manage.py check` passes
- `uv run python manage.py showmigrations core` shows new migration applied
- Service model has auto_update_patch BooleanField with default=True
- SiteConfiguration has version_retention_days IntegerField with default=365
- SiteConfiguration has last_cleanup_at DateTimeField (nullable)
- Service CI tab template includes auto-update toggle with HTMX POST
</verification>

<success_criteria>
Model fields exist with correct types and defaults. Migration is applied. Service CI tab has a working auto-update toggle that persists the setting via HTMX.
</success_criteria>

<output>
After completion, create `.planning/phases/06.7-version-lifecycle-automation/06.7-01-SUMMARY.md`
</output>
