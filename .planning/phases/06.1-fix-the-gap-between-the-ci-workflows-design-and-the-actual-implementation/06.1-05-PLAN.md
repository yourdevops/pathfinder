---
phase: 06.1-fix-the-gap-between-the-ci-workflows-design-and-the-actual-implementation
plan: 05
type: execute
wave: 4
depends_on: ["06.1-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All features from Phase 6.1 are visually correct and functionally working"
    - "Version lifecycle (draft -> publish -> revoke) works end-to-end"
    - "Build verification badges display correctly"
    - "Manifest generation includes version header"
---

<objective>
Human verification checkpoint for the complete Phase 6.1 implementation.

Purpose: Verify all features work correctly before marking the phase complete.
Output: Confirmation that all design doc features are implemented.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 6.1 CI Workflows design-implementation gap closure</name>
  <files></files>
  <action>
Complete CI Workflows design-implementation gap closure built in Plans 01-04:
1. CIWorkflowVersion model with draft/authorized/revoked lifecycle
2. Build verification via manifest hash comparison (verify_build task)
3. Plugin interface with 4 new methods (manifest_id, extract_manifest_id, get_manifest_id_pattern, fetch_manifest_content)
4. Deterministic manifest generation with version header
5. Version management UI (publish modal, version history tab, revoke flow)
6. Build verification status badges on builds tab
7. Fork workflow support
8. Service push method setting (PR vs direct)
9. Draft auto-creation on workflow edit

Human verification steps -- start the app with `make run`:

1. **Workflow Detail Page** -- Navigate to an existing CI Workflow detail page. Verify tabbed layout: Steps, Versions, Manifest, Services tabs. Click "Edit Workflow", make a small change, save. Return to detail page and verify draft banner appears showing "Unpublished changes".

2. **Publish Flow** -- Click "Publish Version" button on the draft banner. Verify modal opens with suggested version number (e.g., 1.0.0). Enter version number, optional changelog, click Publish. Verify version appears in Versions tab as "Authorized" with green badge. Verify Manifest tab shows the version header comment block.

3. **Version History** -- Click Versions tab. Verify published version shows: version number, status badge, author, publish date, build count.

4. **Revocation (if operator)** -- In Versions tab, click Revoke on the published version. Verify confirmation modal warns about impact. Confirm and verify version shows as "Revoked" with red badge.

5. **Fork Workflow** -- Click "Fork Workflow" button. Enter a new name, click Fork. Verify composer opens pre-populated with source workflow's steps. Save the forked workflow and verify it creates a new workflow with a draft version.

6. **Build Verification Badges** -- Navigate to a service with builds. Verify builds tab shows Verification column. Terminal builds should show "Pending" if verification hasn't run yet.

7. **Service Settings** -- Navigate to a service's Settings tab. Verify "CI Manifest Push Method" section exists with PR/Direct toggle.

8. **Generated Manifest Header** -- On workflow detail, check Manifest tab. Verify YAML starts with the header comment block containing "Managed by Pathfinder", workflow name, and version.

Type "approved" or describe any issues found.
  </action>
  <verify>User confirms all 8 verification steps pass.</verify>
  <done>All Phase 6.1 features verified as working: version lifecycle, build verification badges, manifest header, fork support, service settings.</done>
</task>

</tasks>

<verification>
Visual and functional verification by user.
</verification>

<success_criteria>
- All 9 features visually correct and functionally working
- No JavaScript errors in browser console
- No Django errors in server log
- Tabbed layout renders correctly
- Modals open/close properly
- Version lifecycle works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-fix-the-gap-between-the-ci-workflows-design-and-the-actual-implementation/06.1-05-SUMMARY.md`
</output>
