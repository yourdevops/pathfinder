---
phase: 06.1-fix-the-gap-between-the-ci-workflows-design-and-the-actual-implementation
plan: 04
type: execute
wave: 3
depends_on: ["06.1-02", "06.1-03"]
files_modified:
  - core/templates/core/services/_build_row.html
  - core/templates/core/services/_builds_tab.html
  - core/templates/core/services/_settings_tab.html
  - core/templates/core/services/_ci_tab.html
  - core/templates/core/projects/_ci_settings.html
  - core/views/services.py
  - core/views/projects.py
  - core/views/ci_workflows.py
  - core/urls.py
  - core/templates/core/ci_workflows/workflow_detail.html
autonomous: true

must_haves:
  truths:
    - "Build rows display verification status badge (Verified=green, Draft=amber, Unauthorized=red)"
    - "Builds with empty verification_status show 'Pending verification' when terminal"
    - "User can fork a workflow from the workflow detail page"
    - "Service settings include manifest push method (PR or direct)"
    - "Service CI tab has version pinning dropdown to select an authorized CIWorkflowVersion"
    - "Saving version pinning sets Service.ci_workflow_version FK"
    - "Project CI settings include 'Allow Drafts' toggle that sets ProjectCIConfig.allow_draft_workflows"
  artifacts:
    - path: "core/templates/core/services/_build_row.html"
      provides: "Verification status badge on build rows"
      contains: "verification_status"
    - path: "core/templates/core/services/_settings_tab.html"
      provides: "Manifest push method setting"
      contains: "ci_manifest_push_method"
    - path: "core/templates/core/services/_ci_tab.html"
      provides: "Version pinning dropdown and pinned version display"
      contains: "ci_workflow_version"
    - path: "core/templates/core/projects/_ci_settings.html"
      provides: "Allow Drafts toggle in Project CI settings"
      contains: "allow_draft_workflows"
    - path: "core/views/services.py"
      provides: "ServiceUpdatePushMethodView, ServicePinVersionView"
      contains: "class ServicePinVersionView"
    - path: "core/views/ci_workflows.py"
      provides: "ForkWorkflowView"
      contains: "class ForkWorkflowView"
  key_links:
    - from: "core/templates/core/services/_build_row.html"
      to: "core/models.py (Build.verification_status)"
      via: "template variable rendering"
      pattern: "build\\.verification_status"
    - from: "core/views/ci_workflows.py (ForkWorkflowView)"
      to: "core/views/ci_workflows.py (WorkflowComposerView)"
      via: "redirect to composer with pre-populated data"
      pattern: "workflow_composer"
    - from: "core/templates/core/services/_ci_tab.html"
      to: "core/views/services.py (ServicePinVersionView)"
      via: "form POST to pin version URL"
      pattern: "service_pin_version"
    - from: "core/views/services.py (ServicePinVersionView)"
      to: "core/models.py (Service.ci_workflow_version)"
      via: "sets FK from dropdown selection"
      pattern: "ci_workflow_version"
    - from: "core/templates/core/projects/_ci_settings.html"
      to: "core/views/projects.py"
      via: "form POST to update allow_draft_workflows"
      pattern: "allow_draft_workflows"
---

<objective>
UI integration layer: build verification badges, version pinning UI for services, "Allow Drafts" project setting, fork workflow support, service push method config.

Purpose: Makes the verification system visible to users, provides the version pinning UI that enables push_ci_manifest (Plan 02) to use pinned versions, and adds the project-level draft testing gate per versioning.md. This is the final functional plan before checkpoint.
Output: Updated build row template with verification badges, version pinning dropdown in service CI tab, Allow Drafts toggle in project CI settings, fork workflow view, service push method setting.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-fix-the-gap-between-the-ci-workflows-design-and-the-actual-implementation/06.1-01-SUMMARY.md
@.planning/phases/06.1-fix-the-gap-between-the-ci-workflows-design-and-the-actual-implementation/06.1-02-SUMMARY.md
@.planning/phases/06.1-fix-the-gap-between-the-ci-workflows-design-and-the-actual-implementation/06.1-03-SUMMARY.md
@docs/ci-workflows/build-authorization.md
@docs/ci-workflows/versioning.md
@core/templates/core/services/_build_row.html
@core/templates/core/services/_builds_tab.html
@core/templates/core/services/_settings_tab.html
@core/templates/core/services/_ci_tab.html
@core/templates/core/projects/detail.html
@core/views/services.py
@core/views/projects.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification badges + service push method + version pinning UI + Allow Drafts toggle</name>
  <files>core/templates/core/services/_build_row.html, core/templates/core/services/_builds_tab.html, core/templates/core/services/_settings_tab.html, core/templates/core/services/_ci_tab.html, core/templates/core/projects/_ci_settings.html, core/views/services.py, core/views/projects.py, core/urls.py</files>
  <action>
**Update _build_row.html** to add a verification status badge. In the Status column (`<td>` for status), add a second badge after the existing build status badge:

```html
<!-- Verification badge (after status badge) -->
{% if build.verification_status == "verified" %}
<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-green-500/20 text-green-300 ml-1.5" title="Manifest matches an authorized workflow version">
    <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/></svg>
    Verified
</span>
{% elif build.verification_status == "draft" %}
<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-amber-500/20 text-amber-300 ml-1.5" title="Manifest matches a draft workflow version">
    Draft
</span>
{% elif build.verification_status == "unauthorized" %}
<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-red-500/20 text-red-300 ml-1.5" title="Manifest does not match any authorized workflow version">
    Unauthorized
</span>
{% elif build.status in "success,failed,cancelled" and not build.verification_status %}
<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-gray-500/20 text-gray-400 ml-1.5" title="Verification pending">
    Pending
</span>
{% endif %}
```

Also check for revoked version warning: if `build.verification_status == "verified"` AND `build.workflow_version` AND `build.workflow_version.status == "revoked"`, show a small warning icon with tooltip "Built with revoked workflow version X.Y.Z". This is the pre-revocation build warning per design doc.

**Update _builds_tab.html** to add a "Verification" column header after the "Status" column:
```html
<th class="w-[12%] px-4 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Verification</th>
```
Adjust column widths to accommodate: Status 8%, Verification 10%, Commit 23%, Branch 16%, Author 13%, Started 18%, Duration 12%.

Move the verification badge from _build_row.html Status column to a new Verification `<td>` column instead (cleaner separation).

**Update _settings_tab.html** to add "CI Manifest Push Method" section:

After the "Webhook Configuration" section, add:
```html
<!-- CI Manifest Push Method -->
<div class="bg-dark-surface border border-dark-border rounded-lg">
    <div class="p-4 border-b border-dark-border flex items-center justify-between">
        <h2 class="text-lg font-semibold text-dark-text">CI Manifest Push Method</h2>
        <span class="px-2 py-1 text-xs rounded {% if service.ci_manifest_push_method == 'pr' %}bg-blue-500/20 text-blue-300{% else %}bg-amber-500/20 text-amber-300{% endif %}">
            {{ service.get_ci_manifest_push_method_display }}
        </span>
    </div>
    <div class="p-4">
        <p class="text-dark-muted mb-4">How Pathfinder pushes CI workflow manifests to this service's repository.</p>
        {% if can_edit %}
        <form method="post" action="{% url 'projects:service_update_push_method' project_name=project.name service_name=service.name %}">
            {% csrf_token %}
            <div class="flex items-center gap-4">
                <label class="flex items-center gap-2 cursor-pointer">
                    <input type="radio" name="push_method" value="pr" {% if service.ci_manifest_push_method == 'pr' %}checked{% endif %} class="text-dark-accent">
                    <span class="text-dark-text">Pull Request</span>
                    <span class="text-dark-muted text-sm">(recommended)</span>
                </label>
                <label class="flex items-center gap-2 cursor-pointer">
                    <input type="radio" name="push_method" value="direct" {% if service.ci_manifest_push_method == 'direct' %}checked{% endif %} class="text-dark-accent">
                    <span class="text-dark-text">Direct Push</span>
                </label>
                <button type="submit" class="px-3 py-1.5 text-sm bg-dark-accent hover:bg-dark-accent/80 text-white rounded transition-colors">
                    Save
                </button>
            </div>
        </form>
        {% else %}
        <p class="text-dark-muted">{{ service.get_ci_manifest_push_method_display }}</p>
        {% endif %}
    </div>
</div>
```

**Add ServiceUpdatePushMethodView** to core/views/services.py:
```python
class ServiceUpdatePushMethodView(LoginRequiredMixin, View):
    """Update service CI manifest push method."""

    def post(self, request, project_name, service_name):
        project = get_object_or_404(Project, name=project_name)
        service = get_object_or_404(Service, project=project, name=service_name)
        push_method = request.POST.get("push_method", "pr")
        if push_method in ("pr", "direct"):
            service.ci_manifest_push_method = push_method
            service.save(update_fields=["ci_manifest_push_method"])
        return redirect("projects:service_detail", project_name=project_name, service_name=service_name)
```

**Add ServicePinVersionView** to core/views/services.py -- this is the version pinning UI endpoint:
```python
class ServicePinVersionView(LoginRequiredMixin, View):
    """Pin a service to a specific CIWorkflowVersion."""

    def post(self, request, project_name, service_name):
        from core.models import CIWorkflowVersion

        project = get_object_or_404(Project, name=project_name)
        service = get_object_or_404(Service, project=project, name=service_name)

        version_id = request.POST.get("version_id", "")

        if version_id == "" or version_id == "none":
            # Unpin: clear the FK
            service.ci_workflow_version = None
            service.save(update_fields=["ci_workflow_version"])
            messages.success(request, "Version unpinned. Service will not use a specific version.")
        else:
            try:
                version = CIWorkflowVersion.objects.get(
                    id=int(version_id),
                    workflow=service.ci_workflow,  # Must belong to the service's assigned workflow
                    status__in=[CIWorkflowVersion.Status.AUTHORIZED, CIWorkflowVersion.Status.DRAFT],
                )
                # If draft, check project allows drafts
                if version.status == CIWorkflowVersion.Status.DRAFT:
                    try:
                        ci_config = project.ci_config
                        if not ci_config.allow_draft_workflows:
                            messages.error(request, "This project does not allow draft workflow versions. Enable 'Allow Drafts' in Project CI Settings first.")
                            return redirect("projects:service_detail", project_name=project_name, service_name=service_name)
                    except project.ci_config.RelatedObjectDoesNotExist:
                        messages.error(request, "This project does not allow draft workflow versions. Enable 'Allow Drafts' in Project CI Settings first.")
                        return redirect("projects:service_detail", project_name=project_name, service_name=service_name)

                service.ci_workflow_version = version
                service.save(update_fields=["ci_workflow_version"])
                messages.success(request, f"Service pinned to version {version.version or 'draft'}.")
            except (CIWorkflowVersion.DoesNotExist, ValueError):
                messages.error(request, "Invalid version selected.")

        return redirect("projects:service_detail", project_name=project_name, service_name=service_name)
```

**Update _ci_tab.html** to show version pinning UI. Add a "Workflow Version" section after the existing workflow assignment section:

```html
{% if service.ci_workflow %}
<!-- Version Pinning -->
<div class="bg-dark-surface border border-dark-border rounded-lg mt-4">
    <div class="p-4 border-b border-dark-border flex items-center justify-between">
        <h3 class="text-base font-semibold text-dark-text">Pinned Workflow Version</h3>
        {% if service.ci_workflow_version %}
        <span class="px-2 py-0.5 text-xs rounded bg-green-500/20 text-green-300">
            v{{ service.ci_workflow_version.version|default:"draft" }}
        </span>
        {% else %}
        <span class="px-2 py-0.5 text-xs rounded bg-gray-500/20 text-gray-400">Not pinned</span>
        {% endif %}
    </div>
    <div class="p-4">
        <p class="text-dark-muted text-sm mb-3">Select which workflow version to use when pushing manifests to this service's repository. Only authorized (and optionally draft) versions are available.</p>
        {% if can_edit %}
        <form method="post" action="{% url 'projects:service_pin_version' project_name=project.name service_name=service.name %}" class="flex items-center gap-3">
            {% csrf_token %}
            <select name="version_id" class="bg-dark-bg border border-dark-border rounded px-3 py-2 text-dark-text text-sm focus:outline-none focus:ring-2 focus:ring-dark-accent min-w-[200px]">
                <option value="none" {% if not service.ci_workflow_version %}selected{% endif %}>-- No version pinned --</option>
                {% for ver in available_versions %}
                <option value="{{ ver.id }}" {% if service.ci_workflow_version_id == ver.id %}selected{% endif %}>
                    {% if ver.status == "draft" %}Draft{% else %}v{{ ver.version }}{% endif %}
                    ({{ ver.status }})
                    {% if ver.status == "draft" %} - by {{ ver.author }}{% endif %}
                </option>
                {% endfor %}
            </select>
            <button type="submit" class="px-3 py-1.5 text-sm bg-dark-accent hover:bg-dark-accent/80 text-white rounded transition-colors">
                Save
            </button>
        </form>
        {% else %}
        <p class="text-dark-muted">{{ service.ci_workflow_version.version|default:"Not pinned" }}</p>
        {% endif %}
    </div>
</div>
{% endif %}
```

**Update the service detail view** (in core/views/services.py) to pass `available_versions` to the template context. In the view that renders the CI tab, add:
```python
# For version pinning dropdown
from core.models import CIWorkflowVersion
available_versions = []
if service.ci_workflow:
    available_versions = list(
        CIWorkflowVersion.objects.filter(
            workflow=service.ci_workflow,
            status__in=[CIWorkflowVersion.Status.AUTHORIZED, CIWorkflowVersion.Status.DRAFT],
        ).order_by("-published_at", "-created_at")
    )
    # Filter out drafts if project doesn't allow them
    try:
        ci_config = service.project.ci_config
        if not ci_config.allow_draft_workflows:
            available_versions = [v for v in available_versions if v.status != "draft"]
    except Exception:
        available_versions = [v for v in available_versions if v.status != "draft"]
```
Add `available_versions` to the context dict.

**Add "Allow Drafts" toggle to Project CI settings.**

Create or update `core/templates/core/projects/_ci_settings.html` partial (or add to existing project settings template). Add an "Allow Draft Workflows" toggle:
```html
<!-- Allow Draft Workflows -->
<div class="bg-dark-surface border border-dark-border rounded-lg">
    <div class="p-4 border-b border-dark-border flex items-center justify-between">
        <h3 class="text-base font-semibold text-dark-text">Draft Testing</h3>
    </div>
    <div class="p-4">
        <p class="text-dark-muted text-sm mb-3">When enabled, services in this project can be assigned draft workflow versions for pipeline testing. Draft builds are marked as "Draft" and blocked from production deployment (enforced in Phase 7).</p>
        <form method="post" action="{% url 'projects:update_ci_config' project_name=project.name %}">
            {% csrf_token %}
            <input type="hidden" name="setting" value="allow_draft_workflows">
            <label class="flex items-center gap-3 cursor-pointer">
                <input type="checkbox" name="allow_draft_workflows" value="true"
                       {% if ci_config.allow_draft_workflows %}checked{% endif %}
                       onchange="this.form.submit()"
                       class="w-4 h-4 rounded border-dark-border text-dark-accent focus:ring-dark-accent">
                <span class="text-dark-text">Allow Draft Workflows</span>
            </label>
        </form>
    </div>
</div>
```

**Add or update ProjectUpdateCIConfigView** in core/views/projects.py:
```python
class ProjectUpdateCIConfigView(LoginRequiredMixin, View):
    """Update project CI configuration settings."""

    def post(self, request, project_name):
        from core.models import ProjectCIConfig

        project = get_object_or_404(Project, name=project_name)
        ci_config, _ = ProjectCIConfig.objects.get_or_create(project=project)

        setting = request.POST.get("setting", "")
        if setting == "allow_draft_workflows":
            ci_config.allow_draft_workflows = request.POST.get("allow_draft_workflows") == "true"
            ci_config.save(update_fields=["allow_draft_workflows"])
            messages.success(request, "Draft workflows setting updated.")

        return redirect("projects:project_detail", project_name=project_name)
```

Include the `_ci_settings.html` partial in the appropriate project detail/settings template. Pass `ci_config` to the template context in the project detail view.

**Add URLs** for all new endpoints in core/urls.py:
```python
# In projects_patterns:
path(
    "<dns:project_name>/services/<dns:service_name>/ci/push-method/",
    ServiceUpdatePushMethodView.as_view(),
    name="service_update_push_method",
),
path(
    "<dns:project_name>/services/<dns:service_name>/ci/pin-version/",
    ServicePinVersionView.as_view(),
    name="service_pin_version",
),
path(
    "<dns:project_name>/ci-config/",
    ProjectUpdateCIConfigView.as_view(),
    name="update_ci_config",
),
```

Import all new views in core/urls.py.
  </action>
  <verify>
Run `make build` to verify no template/import errors.
Verify _build_row.html or _builds_tab.html contains `verification_status`.
Verify _settings_tab.html contains `ci_manifest_push_method`.
Verify _ci_tab.html contains `ci_workflow_version` and `service_pin_version`.
Verify the new URLs `service_update_push_method`, `service_pin_version`, `update_ci_config` all resolve.
Run `python manage.py shell -c "from core.views.services import ServicePinVersionView, ServiceUpdatePushMethodView; print('Views imported')"`.
Run `python manage.py shell -c "from core.views.projects import ProjectUpdateCIConfigView; print('View imported')"`.
  </verify>
  <done>Build rows show verification badge (Verified/Draft/Unauthorized/Pending) with correct colors. Revoked-version warning shown for pre-revocation Verified builds. Service settings have push method toggle (PR vs direct). Service CI tab has version pinning dropdown listing authorized (and optionally draft) versions, with Save button that sets Service.ci_workflow_version FK. Project CI settings have "Allow Drafts" toggle that sets ProjectCIConfig.allow_draft_workflows. Version pinning enforces draft gate -- draft versions only selectable when project allows drafts.</done>
</task>

<task type="auto">
  <name>Task 2: Fork workflow view + UI button</name>
  <files>core/views/ci_workflows.py, core/urls.py, core/templates/core/ci_workflows/workflow_detail.html</files>
  <action>
**Add ForkWorkflowView** to core/views/ci_workflows.py:

Per design doc (versioning.md): "A user can fork an existing CI Workflow with a new name/description. Versioning starts from a Draft with no version number." Forking is required when runtime set changes.

```python
class ForkWorkflowView(LoginRequiredMixin, View):
    """Fork an existing workflow -- redirects to composer pre-populated with existing steps."""

    def get(self, request, workflow_name):
        """Show fork form with name input."""
        workflow = get_object_or_404(CIWorkflow, name=workflow_name)
        return render(request, "core/ci_workflows/_fork_modal.html", {
            "workflow": workflow,
        })

    def post(self, request, workflow_name):
        """Redirect to composer with pre-populated data from source workflow."""
        from urllib.parse import urlencode

        workflow = get_object_or_404(CIWorkflow, name=workflow_name)
        new_name = request.POST.get("name", "").strip()

        if not new_name:
            messages.error(request, "Fork name is required.")
            return redirect("ci_workflows:workflow_detail", workflow_name=workflow.name)

        # Check uniqueness
        if CIWorkflow.objects.filter(name=new_name).exists():
            messages.error(request, f"A workflow named '{new_name}' already exists.")
            return redirect("ci_workflows:workflow_detail", workflow_name=workflow.name)

        # Redirect to composer in create mode with pre-populated params
        params = urlencode({
            "name": new_name,
            "description": f"Forked from {workflow.name}. {workflow.description}",
            "engine": workflow.workflow_steps.select_related("step").first().step.engine if workflow.workflow_steps.exists() else "github_actions",
            "runtime_family": workflow.runtime_family,
            "runtime_version": workflow.runtime_version,
            "fork_from": workflow.name,  # Composer will load steps from this workflow
        })
        return redirect(f"{reverse('ci_workflows:workflow_composer')}?{params}")
```

**Update WorkflowComposerView.get()** to handle `fork_from` query parameter:
In the create mode branch (where `workflow_name` is None), after reading name/description/runtime from query params, add:
```python
fork_from = request.GET.get("fork_from", "")
if fork_from and not workflow_uuid:
    # Load steps from source workflow for fork
    try:
        source_wf = CIWorkflow.objects.get(name=fork_from)
        workflow_steps = source_wf.workflow_steps.select_related("step").order_by("order")
        initial_steps_json = []
        for ws in workflow_steps:
            initial_steps_json.append({
                "id": str(ws.step.uuid),
                "name": ws.step.name,
                "phase": ws.step.phase,
                "description": ws.step.description[:80] if ws.step.description else "",
                "inputs_schema": ws.step.inputs_schema or {},
                "order": ws.order,
                "input_config": ws.input_config or {},
                "expanded": False,
            })
    except CIWorkflow.DoesNotExist:
        pass
```

**Add URL** in ci_workflows_patterns (before the dynamic workflow_name paths):
```python
path("<dns:workflow_name>/fork/", ForkWorkflowView.as_view(), name="workflow_fork"),
```

**Add Fork button to workflow_detail.html** header, next to "Edit Workflow":
```html
<!-- Fork form (shown inline or via Alpine toggle) -->
<div x-data="{ showFork: false }">
    <button @click="showFork = !showFork" class="px-4 py-2 bg-purple-500/20 hover:bg-purple-500/30 text-purple-400 rounded-lg transition-colors text-sm">
        Fork Workflow
    </button>
    <div x-show="showFork" x-cloak class="mt-2 p-4 bg-dark-surface border border-dark-border rounded-lg">
        <form method="post" action="{% url 'ci_workflows:workflow_fork' workflow_name=workflow.name %}" class="flex items-center gap-3">
            {% csrf_token %}
            <input type="text" name="name" placeholder="New workflow name" required
                   class="bg-dark-bg border border-dark-border rounded px-3 py-2 text-dark-text placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-dark-accent w-64">
            <button type="submit" class="px-4 py-2 bg-purple-500 hover:bg-purple-600 text-white rounded-lg transition-colors text-sm">
                Fork
            </button>
            <button type="button" @click="showFork = false" class="px-4 py-2 text-dark-muted hover:text-dark-text transition-colors text-sm">
                Cancel
            </button>
        </form>
    </div>
</div>
```

Since the fork flow goes through the composer, the user gets a pre-populated composer where they can modify steps (change runtimes, add/remove steps). When saved, the new workflow gets its own draft version automatically (from Plan 02's WorkflowComposerView.post).
  </action>
  <verify>
Run `make build` to verify no template/import errors.
Verify `ci_workflows:workflow_fork` URL resolves.
Verify ForkWorkflowView.post redirects to composer with fork_from parameter.
Verify WorkflowComposerView.get handles fork_from parameter to pre-populate steps.
  </verify>
  <done>Fork button on workflow detail page opens inline form. ForkWorkflowView redirects to composer pre-populated with source workflow's steps and runtime. WorkflowComposerView handles fork_from to load steps. New workflow created via composer gets its own draft version automatically.</done>
</task>

</tasks>

<verification>
1. `make build` -- no errors
2. Build rows show verification badges with correct colors
3. Revoked version warning shown on pre-revocation Verified builds
4. Service settings show push method toggle
5. Service CI tab shows version pinning dropdown with authorized versions
6. Saving version pinning sets Service.ci_workflow_version FK correctly
7. Project CI settings show "Allow Drafts" toggle
8. Draft versions only appear in pinning dropdown when project allows drafts
9. Fork button exists on workflow detail page
10. Fork flow pre-populates composer with source steps
11. All 3 new URLs resolve (push method, pin version, CI config)
</verification>

<success_criteria>
- Build verification badge displays correctly: Verified (green shield), Draft (amber), Unauthorized (red), Pending (gray for terminal builds without verification)
- Pre-revocation builds show "Built with revoked version" warning per design doc
- Service settings include manifest push method toggle (PR/direct)
- Service CI tab has version pinning dropdown listing authorized CIWorkflowVersions
- Saving version selection sets Service.ci_workflow_version FK (wiring complete for Plan 02's push_ci_manifest)
- Project CI settings include "Allow Drafts" toggle that controls ProjectCIConfig.allow_draft_workflows
- Draft versions only selectable when project allows drafts
- Fork workflow creates new workflow pre-populated with source steps
- Forked workflow gets its own draft version on save
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-fix-the-gap-between-the-ci-workflows-design-and-the-actual-implementation/06.1-04-SUMMARY.md`
</output>
