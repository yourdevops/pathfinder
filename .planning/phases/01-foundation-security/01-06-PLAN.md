---
phase: 01-foundation-security
plan: 06
type: execute
wave: 4
depends_on: ["01-04", "01-05"]
files_modified:
  - core/views/placeholders.py
  - core/views/__init__.py
  - core/templates/core/placeholders/blueprints.html
  - core/templates/core/placeholders/connections.html
  - core/urls.py
  - devssp/urls.py
autonomous: true
checkpoint: true

must_haves:
  truths:
    - "Authenticated users can view blueprints list page (empty for now)"
    - "Authenticated users can view connections list page (empty for now)"
    - "Navigation links to Blueprints and Connections work"
    - "Non-authenticated users are redirected to login"
  artifacts:
    - path: "core/views/placeholders.py"
      provides: "Placeholder views for future features"
      contains: "class BlueprintsListView"
    - path: "core/templates/core/placeholders/blueprints.html"
      provides: "Blueprints placeholder page"
      contains: "Blueprints"
    - path: "core/templates/core/placeholders/connections.html"
      provides: "Connections placeholder page"
      contains: "Connections"
  key_links:
    - from: "core/templates/core/components/nav.html"
      to: "blueprints:list"
      via: "URL link"
      pattern: "url.*blueprints:list"
    - from: "core/templates/core/components/nav.html"
      to: "connections:list"
      via: "URL link"
      pattern: "url.*connections:list"
---

<objective>
Create placeholder views for Blueprints and Connections to satisfy navigation requirements.

Purpose: Enable navigation links to work (FNDN-12, FNDN-13, UIUX-01). These are placeholder pages showing empty states that will be replaced in Phase 3-4.
Output: Working blueprints and connections list pages with empty states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-security/01-CONTEXT.md
@.planning/phases/01-foundation-security/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder views</name>
  <files>core/views/placeholders.py, core/views/__init__.py</files>
  <action>
1. Create `core/views/placeholders.py`:
```python
from django.shortcuts import render
from django.views import View
from django.contrib.auth.mixins import LoginRequiredMixin


class BlueprintsListView(LoginRequiredMixin, View):
    """Placeholder for blueprints list.

    Will be replaced by actual implementation in Phase 4.
    For now, shows empty state to satisfy FNDN-12 (authenticated
    users can view blueprints list).
    """
    template_name = 'core/placeholders/blueprints.html'

    def get(self, request):
        return render(request, self.template_name, {
            'blueprints': [],  # Empty for now
        })


class ConnectionsListView(LoginRequiredMixin, View):
    """Placeholder for connections list.

    Will be replaced by actual implementation in Phase 3.
    For now, shows empty state to satisfy FNDN-13 (authenticated
    users can view connections list).
    """
    template_name = 'core/placeholders/connections.html'

    def get(self, request):
        return render(request, self.template_name, {
            'connections': [],  # Empty for now
        })
```

2. Update `core/views/__init__.py`:
```python
from .setup import UnlockView, AdminRegistrationView
from .auth import LoginView, LogoutView
from .users import UserListView, UserCreateView, UserEditView, UserDeleteView
from .groups import (
    GroupListView, GroupDetailView, GroupCreateView, GroupEditView,
    GroupDeleteView, GroupAddMemberView, GroupRemoveMemberView,
)
from .audit import AuditLogView
from .placeholders import BlueprintsListView, ConnectionsListView

__all__ = [
    'UnlockView', 'AdminRegistrationView',
    'LoginView', 'LogoutView',
    'UserListView', 'UserCreateView', 'UserEditView', 'UserDeleteView',
    'GroupListView', 'GroupDetailView', 'GroupCreateView', 'GroupEditView',
    'GroupDeleteView', 'GroupAddMemberView', 'GroupRemoveMemberView',
    'AuditLogView',
    'BlueprintsListView', 'ConnectionsListView',
]
```
  </action>
  <verify>
  - `grep -q "class BlueprintsListView" core/views/placeholders.py` succeeds
  - `grep -q "class ConnectionsListView" core/views/placeholders.py` succeeds
  - `grep -q "BlueprintsListView" core/views/__init__.py` succeeds
  </verify>
  <done>Placeholder views created</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder templates and URLs</name>
  <files>core/templates/core/placeholders/blueprints.html, core/templates/core/placeholders/connections.html, core/urls.py, devssp/urls.py</files>
  <action>
1. Create template directory:
```bash
mkdir -p core/templates/core/placeholders
```

2. Create `core/templates/core/placeholders/blueprints.html`:
```html
{% extends "base.html" %}

{% block title %}Blueprints - DevSSP{% endblock %}

{% block content %}
<div class="p-8">
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold">Blueprints</h1>
    </div>

    <div class="card text-center py-16">
        <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-dark-border flex items-center justify-center">
            <svg class="w-8 h-8 text-dark-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6zM16 13a1 1 0 011-1h2a1 1 0 011 1v6a1 1 0 01-1 1h-2a1 1 0 01-1-1v-6z" />
            </svg>
        </div>
        <h2 class="text-xl font-semibold mb-2">No Blueprints Yet</h2>
        <p class="text-dark-muted max-w-md mx-auto">
            Blueprints are service templates that define how to scaffold and deploy new services.
            Platform engineers will register blueprints in a future update.
        </p>
    </div>
</div>
{% endblock %}
```

3. Create `core/templates/core/placeholders/connections.html`:
```html
{% extends "base.html" %}

{% block title %}Connections - DevSSP{% endblock %}

{% block content %}
<div class="p-8">
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold">Connections</h1>
    </div>

    <div class="card text-center py-16">
        <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-dark-border flex items-center justify-center">
            <svg class="w-8 h-8 text-dark-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1" />
            </svg>
        </div>
        <h2 class="text-xl font-semibold mb-2">No Connections Yet</h2>
        <p class="text-dark-muted max-w-md mx-auto">
            Connections link DevSSP to external services like GitHub and Docker.
            Platform engineers will configure integrations in a future update.
        </p>
    </div>
</div>
{% endblock %}
```

4. Update `core/urls.py` to add placeholder URL patterns:
```python
from django.urls import path
from .views import (
    UnlockView, AdminRegistrationView,
    LoginView, LogoutView,
    UserListView, UserCreateView, UserEditView, UserDeleteView,
    GroupListView, GroupDetailView, GroupCreateView, GroupEditView,
    GroupDeleteView, GroupAddMemberView, GroupRemoveMemberView,
    AuditLogView,
    BlueprintsListView, ConnectionsListView,
)

# Setup URLs
setup_patterns = [
    path('unlock/', UnlockView.as_view(), name='unlock'),
    path('register/', AdminRegistrationView.as_view(), name='register'),
]

# Auth URLs
auth_patterns = [
    path('login/', LoginView.as_view(), name='login'),
    path('logout/', LogoutView.as_view(), name='logout'),
]

# User management URLs
users_patterns = [
    path('', UserListView.as_view(), name='list'),
    path('create/', UserCreateView.as_view(), name='create'),
    path('<uuid:uuid>/edit/', UserEditView.as_view(), name='edit'),
    path('<uuid:uuid>/delete/', UserDeleteView.as_view(), name='delete'),
]

# Group management URLs
groups_patterns = [
    path('', GroupListView.as_view(), name='list'),
    path('create/', GroupCreateView.as_view(), name='create'),
    path('<uuid:uuid>/', GroupDetailView.as_view(), name='detail'),
    path('<uuid:uuid>/edit/', GroupEditView.as_view(), name='edit'),
    path('<uuid:uuid>/delete/', GroupDeleteView.as_view(), name='delete'),
    path('<uuid:uuid>/add-member/', GroupAddMemberView.as_view(), name='add_member'),
    path('<uuid:uuid>/remove-member/<uuid:user_uuid>/', GroupRemoveMemberView.as_view(), name='remove_member'),
]

# Audit log URLs
audit_patterns = [
    path('', AuditLogView.as_view(), name='list'),
]

# Placeholder URLs (to be replaced in Phase 3-4)
blueprints_patterns = [
    path('', BlueprintsListView.as_view(), name='list'),
]

connections_patterns = [
    path('', ConnectionsListView.as_view(), name='list'),
]
```

5. Update `devssp/urls.py` with all namespaces:
```python
from django.contrib import admin
from django.urls import path, include

from core.urls import (
    setup_patterns, auth_patterns, users_patterns,
    groups_patterns, audit_patterns,
    blueprints_patterns, connections_patterns,
)

urlpatterns = [
    path('admin/', admin.site.urls),
    path('setup/', include((setup_patterns, 'setup'), namespace='setup')),
    path('auth/', include((auth_patterns, 'auth'), namespace='auth')),
    path('users/', include((users_patterns, 'users'), namespace='users')),
    path('groups/', include((groups_patterns, 'groups'), namespace='groups')),
    path('audit/', include((audit_patterns, 'audit'), namespace='audit')),
    path('blueprints/', include((blueprints_patterns, 'blueprints'), namespace='blueprints')),
    path('connections/', include((connections_patterns, 'connections'), namespace='connections')),
]
```
  </action>
  <verify>
  - `core/templates/core/placeholders/blueprints.html` exists
  - `core/templates/core/placeholders/connections.html` exists
  - `grep -q "blueprints_patterns" core/urls.py` succeeds
  - `grep -q "connections_patterns" core/urls.py` succeeds
  - `grep -q "namespace='blueprints'" devssp/urls.py` succeeds
  - `grep -q "namespace='connections'" devssp/urls.py` succeeds
  </verify>
  <done>Placeholder templates and URLs created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete Phase 1: Foundation & Security implementation including:
  - Unlock flow with token verification and admin registration
  - Custom User and Group models with SystemRoles
  - Login/logout with session persistence
  - User management (list, create modal, edit with group assignment)
  - Group management (list, detail, create, edit, member management)
  - Audit log with human-readable entries
  - Navigation with permission-based visibility
  - Placeholder pages for Blueprints and Connections
  - Dark mode UI throughout
  </what-built>
  <how-to-verify>
  1. Start fresh (delete db.sqlite3 if exists):
     ```bash
     rm -f db.sqlite3
     source venv/bin/activate
     python manage.py migrate
     python manage.py tailwind build
     python manage.py runserver
     ```

  2. Navigate to http://localhost:8000/
     - Should redirect to /setup/unlock/
     - Should show dark-themed unlock page

  3. Get token: `cat secrets/initialUnlockToken`
     - Enter token, should redirect to /setup/register/

  4. Create admin account (e.g., admin / admin@test.com / AdminPass123!)
     - Should redirect to /users/
     - Unlock page should now redirect to login

  5. Check navigation sidebar:
     - Platform section: Blueprints, Connections (visible to all)
     - Admin section: Users, Groups, Audit Log (visible because you're admin)
     - Logout button at bottom

  6. Create a user via modal:
     - Click "Create User", fill form, submit
     - New user should appear in list

  7. Edit user and assign to a group:
     - Click Edit on new user
     - Check "admins" group checkbox
     - Save

  8. Navigate to Groups:
     - Should see "admins" group with 2 members
     - Create new group "operators" with operator role

  9. Navigate to Audit Log:
     - Should show entries like "admin created user alice"
     - Filter by action type

  10. Click Blueprints and Connections in nav:
      - Should show empty state pages

  11. Logout and login again:
      - Session should persist on refresh
      - Logout should redirect to login
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. All navigation links work without errors
2. Blueprints page shows empty state with explanation
3. Connections page shows empty state with explanation
4. Non-authenticated users are redirected to login
5. Full end-to-end flow from unlock to user management works
</verification>

<success_criteria>
- Navigation Blueprints link goes to /blueprints/ (empty list)
- Navigation Connections link goes to /connections/ (empty list)
- Both pages require authentication
- Phase 1 success criteria all met:
  1. Fresh install shows unlock page, token allows admin creation
  2. Admin can manage users, groups, assign SystemRoles
  3. Login works, session persists, logout works
  4. Authenticated user sees nav with Blueprints and Connections
  5. All entity changes appear in audit log
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security/01-06-SUMMARY.md`
</output>
