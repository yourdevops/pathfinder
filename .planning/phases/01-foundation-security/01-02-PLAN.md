---
phase: 01-foundation-security
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - devssp/settings.py
  - theme/apps.py
  - theme/templates/base.html
  - theme/static_src/src/styles.css
  - theme/tailwind.config.js
  - core/context_processors.py
  - core/templates/core/components/nav.html
autonomous: true

must_haves:
  truths:
    - "Base template uses dark mode Tailwind styling"
    - "Navigation shows Blueprints and Connections links for authenticated users"
    - "Navigation shows Admin section for users with admin SystemRole"
    - "Tailwind CSS compiles and hot reloads in development"
  artifacts:
    - path: "theme/templates/base.html"
      provides: "Base HTML template with dark mode"
      contains: "class=\"dark\""
    - path: "theme/tailwind.config.js"
      provides: "Tailwind configuration"
      contains: "darkMode: 'class'"
    - path: "core/context_processors.py"
      provides: "User roles context processor"
      contains: "def user_roles"
    - path: "core/templates/core/components/nav.html"
      provides: "Navigation component"
      contains: "Blueprints"
  key_links:
    - from: "theme/templates/base.html"
      to: "core/templates/core/components/nav.html"
      via: "include tag"
      pattern: "include.*nav\\.html"
    - from: "devssp/settings.py"
      to: "core.context_processors.user_roles"
      via: "TEMPLATES context_processors"
      pattern: "core\\.context_processors\\.user_roles"
---

<objective>
Set up django-tailwind theme with dark mode and create navigation component with permission-based visibility.

Purpose: Establish consistent dark mode UI foundation and navigation that shows appropriate sections based on user roles.
Output: Theme app with Tailwind, base template, and navigation component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
@.planning/phases/01-foundation-security/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize django-tailwind theme app</name>
  <files>devssp/settings.py, theme/apps.py, theme/tailwind.config.js, theme/static_src/src/styles.css</files>
  <action>
1. Activate venv and initialize tailwind:
```bash
source venv/bin/activate
python manage.py tailwind init
```
When prompted, use app name: `theme`

2. Update `devssp/settings.py` - add to INSTALLED_APPS:
```python
INSTALLED_APPS = [
    'core.apps.CoreConfig',
    'auditlog',
    'tailwind',
    'theme',  # django-tailwind theme app
    'django.contrib.admin',
    # ... rest unchanged
]
```

Add after INSTALLED_APPS:
```python
# Tailwind CSS
TAILWIND_APP_NAME = 'theme'
INTERNAL_IPS = ['127.0.0.1']  # For django-browser-reload if used
```

Add NPM_BIN_PATH if node is in non-standard location (usually not needed):
```python
# NPM_BIN_PATH = '/usr/local/bin/npm'  # Uncomment if needed
```

3. Install Tailwind dependencies:
```bash
python manage.py tailwind install
```

4. Update `theme/tailwind.config.js`:
```javascript
module.exports = {
  darkMode: 'class',
  content: [
    '../templates/**/*.html',
    '../../**/templates/**/*.html',
    '../../core/templates/**/*.html',
  ],
  theme: {
    extend: {
      colors: {
        'dark-bg': '#0f172a',        // slate-900
        'dark-surface': '#1e293b',   // slate-800
        'dark-border': '#334155',    // slate-700
        'dark-text': '#f1f5f9',      // slate-100
        'dark-muted': '#94a3b8',     // slate-400
        'dark-accent': '#3b82f6',    // blue-500
        'dark-accent-hover': '#2563eb', // blue-600
      }
    }
  },
  plugins: [
    require('@tailwindcss/forms'),
  ],
}
```

5. Update `theme/static_src/src/styles.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Custom component classes */
@layer components {
  .btn-primary {
    @apply bg-dark-accent hover:bg-dark-accent-hover text-white font-medium py-2 px-4 rounded-lg transition-colors;
  }

  .btn-secondary {
    @apply bg-dark-surface hover:bg-dark-border text-dark-text font-medium py-2 px-4 rounded-lg border border-dark-border transition-colors;
  }

  .card {
    @apply bg-dark-surface border border-dark-border rounded-lg p-4;
  }

  .input-field {
    @apply bg-dark-bg border border-dark-border rounded-lg px-3 py-2 text-dark-text placeholder-dark-muted focus:outline-none focus:ring-2 focus:ring-dark-accent focus:border-transparent;
  }

  .table-row {
    @apply border-b border-dark-border hover:bg-dark-surface/50;
  }
}
```
  </action>
  <verify>
  - Directory `theme/` exists
  - `theme/tailwind.config.js` contains `darkMode: 'class'`
  - `npm --prefix theme/static_src list @tailwindcss/forms` shows installed
  </verify>
  <done>Tailwind theme app initialized with dark mode configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create base template and user roles context processor</name>
  <files>theme/templates/base.html, core/context_processors.py, devssp/settings.py</files>
  <action>
1. Create `core/context_processors.py`:
```python
from core.models import GroupMembership


def user_roles(request):
    """Add user's system roles to template context."""
    if not request.user.is_authenticated:
        return {'user_roles': [], 'is_admin': False, 'is_operator': False, 'is_auditor': False}

    roles = set()
    memberships = GroupMembership.objects.filter(
        user=request.user,
        group__status='active'
    ).select_related('group')

    for membership in memberships:
        roles.update(membership.group.system_roles)

    return {
        'user_roles': list(roles),
        'is_admin': 'admin' in roles,
        'is_operator': 'operator' in roles,
        'is_auditor': 'auditor' in roles,
    }
```

2. Update `devssp/settings.py` TEMPLATES context_processors:
```python
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'core.context_processors.user_roles',  # Add this
            ],
        },
    },
]
```

3. Create `theme/templates/base.html`:
```html
{% load static tailwind_tags %}
<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}DevSSP{% endblock %}</title>
    {% tailwind_css %}
    <script>
        // Ensure dark mode class is set before render to prevent flash
        document.documentElement.classList.add('dark');
    </script>
</head>
<body class="bg-dark-bg text-dark-text min-h-screen">
    {% if user.is_authenticated %}
    {% include "core/components/nav.html" %}
    {% endif %}

    <main class="{% if user.is_authenticated %}ml-64{% endif %} min-h-screen">
        {% if messages %}
        <div class="p-4">
            {% for message in messages %}
            <div class="card mb-2 {% if message.tags %}{{ message.tags }}{% endif %}">
                {{ message }}
            </div>
            {% endfor %}
        </div>
        {% endif %}

        {% block content %}{% endblock %}
    </main>

    {% block scripts %}{% endblock %}
</body>
</html>
```
  </action>
  <verify>
  - `grep -q "user_roles" core/context_processors.py` succeeds
  - `grep -q "core.context_processors.user_roles" devssp/settings.py` succeeds
  - `theme/templates/base.html` exists and contains `class="dark"`
  </verify>
  <done>Base template and context processor created</done>
</task>

<task type="auto">
  <name>Task 3: Create navigation component</name>
  <files>core/templates/core/components/nav.html</files>
  <action>
1. Create directory structure:
```bash
mkdir -p core/templates/core/components
```

2. Create `core/templates/core/components/nav.html`:
```html
<!-- Sidebar Navigation -->
<aside class="fixed left-0 top-0 h-full w-64 bg-dark-surface border-r border-dark-border flex flex-col">
    <!-- Logo / Brand -->
    <div class="p-4 border-b border-dark-border">
        <h1 class="text-xl font-bold text-dark-text">DevSSP</h1>
    </div>

    <!-- Main Navigation -->
    <nav class="flex-1 p-4 space-y-1">
        <!-- Platform Discovery Section (all authenticated users) -->
        <div class="mb-6">
            <h2 class="text-xs font-semibold text-dark-muted uppercase tracking-wider mb-2">Platform</h2>

            <a href="{% url 'blueprints:list' %}" class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors {% if request.resolver_match.url_name == 'blueprints_list' %}bg-dark-border{% endif %}">
                <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6zM16 13a1 1 0 011-1h2a1 1 0 011 1v6a1 1 0 01-1 1h-2a1 1 0 01-1-1v-6z" />
                </svg>
                Blueprints
            </a>

            <a href="{% url 'connections:list' %}" class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors {% if request.resolver_match.url_name == 'connections_list' %}bg-dark-border{% endif %}">
                <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1" />
                </svg>
                Connections
            </a>
        </div>

        <!-- Admin Section (admin role only) -->
        {% if is_admin %}
        <div class="mb-6">
            <h2 class="text-xs font-semibold text-dark-muted uppercase tracking-wider mb-2">Admin</h2>

            <a href="{% url 'users:list' %}" class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors {% if 'users' in request.path %}bg-dark-border{% endif %}">
                <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z" />
                </svg>
                Users
            </a>

            <a href="{% url 'groups:list' %}" class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors {% if 'groups' in request.path %}bg-dark-border{% endif %}">
                <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                </svg>
                Groups
            </a>

            <a href="{% url 'audit:list' %}" class="flex items-center px-3 py-2 rounded-lg text-dark-text hover:bg-dark-border/50 transition-colors {% if 'audit' in request.path %}bg-dark-border{% endif %}">
                <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                </svg>
                Audit Log
            </a>
        </div>
        {% endif %}
    </nav>

    <!-- User Section -->
    <div class="p-4 border-t border-dark-border">
        <div class="flex items-center justify-between">
            <div class="flex items-center">
                <div class="w-8 h-8 rounded-full bg-dark-accent flex items-center justify-center text-white font-medium">
                    {{ user.username|slice:":1"|upper }}
                </div>
                <div class="ml-3">
                    <p class="text-sm font-medium text-dark-text">{{ user.username }}</p>
                    <p class="text-xs text-dark-muted">{{ user.email }}</p>
                </div>
            </div>
            <a href="{% url 'auth:logout' %}" class="text-dark-muted hover:text-dark-text transition-colors" title="Logout">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                </svg>
            </a>
        </div>
    </div>
</aside>
```

Note: The URL names (blueprints:list, connections:list, etc.) will fail until those views exist. This is expected - they will be created in later plans and phases. The navigation is designed to be complete per UIUX-01.
  </action>
  <verify>
  - `core/templates/core/components/nav.html` exists
  - `grep -q "Blueprints" core/templates/core/components/nav.html` succeeds
  - `grep -q "is_admin" core/templates/core/components/nav.html` succeeds
  </verify>
  <done>Navigation component created with permission-based sections</done>
</task>

</tasks>

<verification>
1. `python manage.py tailwind build` completes without errors
2. Base template includes dark mode class on html element
3. Navigation component shows Platform section (Blueprints, Connections)
4. Navigation component conditionally shows Admin section based on is_admin
5. Context processor provides is_admin, is_operator, is_auditor booleans
</verification>

<success_criteria>
- Tailwind CSS builds successfully with dark mode
- Base template has consistent dark theme styling
- Navigation shows Blueprints and Connections for all authenticated users
- Admin section (Users, Groups, Audit) only shows for users with admin role
- Logout link visible in navigation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security/01-02-SUMMARY.md`
</output>
