---
phase: 05.1-ci-workflows-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/models.py
  - core/views/blueprints.py
  - core/views/services.py
  - core/views/__init__.py
  - core/forms/services.py
  - core/urls.py
  - core/templates/core/components/nav.html
  - core/templates/core/services/list.html
  - core/templates/core/services/wizard/step_blueprint.html
  - core/templates/core/services/wizard/step_review.html
  - core/templates/core/services/_details_tab.html
  - core/templates/core/projects/_services_tab.html
  - core/templates/core/dashboard/home.html
autonomous: true

must_haves:
  truths:
    - "Application starts without errors after Blueprint/BlueprintVersion models are removed"
    - "Service model no longer has blueprint or blueprint_version foreign keys"
    - "Sidebar shows CI Workflows section instead of Blueprints"
    - "New CI Workflow models exist in database (StepsRepository, RuntimeFamily, CIStep, CIWorkflow, CIWorkflowStep)"
    - "Service list and detail pages render without blueprint references"
  artifacts:
    - path: "core/models.py"
      provides: "New CI Workflow models, Service without Blueprint FK"
      contains: "class StepsRepository"
    - path: "core/urls.py"
      provides: "ci_workflows URL patterns replacing blueprints"
      contains: "ci_workflows_patterns"
    - path: "core/templates/core/components/nav.html"
      provides: "CI Workflows sidebar section"
      contains: "CI Workflows"
  key_links:
    - from: "core/urls.py"
      to: "core/views/__init__.py"
      via: "URL imports"
      pattern: "ci_workflows_patterns"
    - from: "core/models.py"
      to: "core/views/services.py"
      via: "Service model without blueprint FK"
      pattern: "class Service"
---

<objective>
Remove Blueprint and BlueprintVersion models, add new CI Workflow domain models, update Service model to remove blueprint FK, clean all blueprint references from views/templates/URLs, and update sidebar navigation.

Purpose: This is the critical migration that replaces the Blueprints concept with CI Workflows. Everything else in Phase 5.1 depends on these models and clean URLs existing.
Output: Working application with new CI Workflow models, no Blueprint references, updated sidebar navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ci-workflows-builder/05.1-RESEARCH.md
@core/models.py
@core/urls.py
@core/views/__init__.py
@core/views/blueprints.py
@core/views/services.py
@core/forms/services.py
@core/templates/core/components/nav.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Blueprint models and references, update Service model</name>
  <files>
    core/models.py
    core/views/blueprints.py
    core/views/services.py
    core/views/__init__.py
    core/forms/services.py
    core/urls.py
    core/templates/core/services/list.html
    core/templates/core/services/wizard/step_blueprint.html
    core/templates/core/services/wizard/step_review.html
    core/templates/core/services/_details_tab.html
    core/templates/core/projects/_services_tab.html
    core/templates/core/dashboard/home.html
    core/templates/core/placeholders/blueprints.html
  </files>
  <action>
    **In core/models.py:**
    1. Remove the `Blueprint` class entirely (lines ~368-472)
    2. Remove the `BlueprintVersion` class entirely (lines ~475-512)
    3. On the `Service` model: remove `blueprint` ForeignKey field and `blueprint_version` ForeignKey field entirely (no nullability -- just remove them since no backwards compatibility is needed per CLAUDE.md)
    4. Remove any import or reference to Blueprint/BlueprintVersion in the file

    **In core/views/blueprints.py:**
    - Delete the entire file content or replace with empty module docstring. This file will be repurposed as `core/views/ci_workflows.py` in Plan 02.

    **In core/views/services.py:**
    - Remove import of `BlueprintVersion` from `core.models`
    - Remove `BlueprintVersionsView` class entirely
    - In `ServiceListView.get_queryset()`: remove `.select_related('blueprint', 'blueprint_version')` -- just use `.select_related('project')`
    - In `ServiceCreateWizard`: The wizard currently has a blueprint selection step. For now, comment out or temporarily disable the wizard (redirect to a "Service creation temporarily unavailable" message) OR simplify it to skip the blueprint step. Simplest approach: make the wizard only 3 steps (repository, config, review) by removing the blueprint step. Remove `BlueprintStepForm` from the wizard forms list. Update `done()` method to not set `blueprint` or `blueprint_version` on Service creation.
    - Remove `BlueprintVersionsView` from the file

    **In core/views/__init__.py:**
    - Remove all Blueprint view imports (`BlueprintsListView`, `BlueprintListView`, `BlueprintDetailView`, `BlueprintRegisterView`, `BlueprintPreviewView`, `BlueprintSyncView`, `BlueprintSyncStatusView`, `BlueprintDeleteView`)
    - Remove Blueprint-related imports from `views/services.py` exports (`BlueprintVersionsView`)

    **In core/forms/services.py:**
    - Remove `BlueprintStepForm` class entirely
    - Remove any import of Blueprint/BlueprintVersion models

    **In core/urls.py:**
    - Remove the entire `blueprints_patterns` URL list
    - Remove Blueprint view imports from the top of the file
    - Remove `BlueprintVersionsView` from services imports
    - Remove the `blueprint-versions` URL from `services_patterns`
    - Update the main URL conf in the project's urls.py if `blueprints_patterns` is included there (check `ssp/urls.py`)

    **In templates:**
    - `services/list.html`: Remove any reference to `service.blueprint` or `service.blueprint_version` -- show service name, project, status only
    - `services/wizard/step_blueprint.html`: Delete this template file (no longer needed)
    - `services/wizard/step_review.html`: Remove blueprint/version display from review summary
    - `services/_details_tab.html`: Remove blueprint reference display
    - `projects/_services_tab.html`: Remove any blueprint column/reference
    - `dashboard/home.html`: Remove any blueprint reference in dashboard stats/activity
    - `placeholders/blueprints.html`: Delete or repurpose (no longer needed)

    **Run migrations:**
    ```bash
    source venv/bin/activate
    python manage.py makemigrations core
    python manage.py migrate
    ```

    **IMPORTANT:** The migration will DROP the blueprint and blueprint_version tables and remove FK columns from Service. This is fine per CLAUDE.md "No Backwards Compatibility Required". If existing Service records exist with blueprint FKs, the migration needs to handle removal of those columns. Django will auto-generate the migration to remove fields and drop tables.
  </action>
  <verify>
    ```bash
    source venv/bin/activate
    python manage.py check --deploy 2>&1 | head -20
    python manage.py showmigrations core | tail -5
    python -c "import django; django.setup()" 2>&1
    ```
    No import errors, no migration issues, `python manage.py check` passes.
  </verify>
  <done>Blueprint and BlueprintVersion models removed. Service model has no blueprint FK. All views, forms, URLs, templates compile without blueprint references. Migrations applied successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Add CI Workflow models and update navigation</name>
  <files>
    core/models.py
    core/urls.py
    core/templates/core/components/nav.html
  </files>
  <action>
    **In core/models.py, add these new models** (after existing models, before Service or at the end):

    1. `StepsRepository` model:
       - `id = BigAutoField(primary_key=True)`
       - `uuid = UUIDField(default=uuid.uuid4, editable=False, unique=True, db_index=True)`
       - `name = CharField(max_length=63, unique=True, validators=[dns_label_validator])`
       - `git_url = URLField(max_length=500, unique=True)`
       - `default_branch = CharField(max_length=100, default='main')`
       - `connection = ForeignKey(IntegrationConnection, on_delete=SET_NULL, null=True, blank=True, related_name='steps_repositories')`
       - `scan_status = CharField(max_length=20, choices=[('pending','Pending'),('scanning','Scanning'),('scanned','Scanned'),('error','Error')], default='pending')`
       - `scan_error = TextField(blank=True)`
       - `last_scanned_at = DateTimeField(null=True, blank=True)`
       - `created_by = CharField(max_length=150, blank=True)`
       - `created_at = DateTimeField(auto_now_add=True)`
       - `updated_at = DateTimeField(auto_now=True)`
       - `Meta: db_table = 'core_steps_repository'`
       - `__str__` returns `self.name`

    2. `RuntimeFamily` model:
       - `id = BigAutoField(primary_key=True)`
       - `repository = ForeignKey(StepsRepository, on_delete=CASCADE, related_name='runtimes')`
       - `name = CharField(max_length=63)` (e.g., 'python', 'node')
       - `display_name = CharField(max_length=100, blank=True)` (e.g., 'Python', 'Node.js')
       - `versions = JSONField(default=list)` (e.g., ["3.11", "3.12", "3.13"])
       - `Meta: db_table = 'core_runtime_family', unique_together = [['repository', 'name']]`
       - `__str__` returns `f"{self.name} ({self.repository.name})"`

    3. `CIStep` model:
       - `id = BigAutoField(primary_key=True)`
       - `uuid = UUIDField(default=uuid.uuid4, editable=False, unique=True, db_index=True)`
       - `repository = ForeignKey(StepsRepository, on_delete=CASCADE, related_name='steps')`
       - `directory_name = CharField(max_length=255)` (e.g., 'setup-python')
       - `name = CharField(max_length=255)` (from action.yml 'name')
       - `description = TextField(blank=True)` (from action.yml 'description')
       - `phase = CharField(max_length=20, choices=[('setup','Setup'),('build','Build'),('test','Test'),('package','Package')], blank=True)`
       - `runtime_constraints = JSONField(default=dict)` (e.g., {"python": ">=3.10"})
       - `tags = JSONField(default=list)`
       - `produces = JSONField(null=True, blank=True)` (e.g., {"type": "container-image"})
       - `inputs_schema = JSONField(default=dict)` (full inputs from action.yml)
       - `commit_sha = CharField(max_length=40, blank=True)`
       - `raw_metadata = JSONField(default=dict)` (full parsed action.yml)
       - `created_at = DateTimeField(auto_now_add=True)`
       - `updated_at = DateTimeField(auto_now=True)`
       - `Meta: db_table = 'core_ci_step', unique_together = [['repository', 'directory_name']]`
       - `__str__` returns `self.name`

    4. `CIWorkflow` model:
       - `id = BigAutoField(primary_key=True)`
       - `uuid = UUIDField(default=uuid.uuid4, editable=False, unique=True, db_index=True)`
       - `name = CharField(max_length=63, unique=True, validators=[dns_label_validator])`
       - `description = TextField(blank=True)`
       - `runtime_family = CharField(max_length=63)` (e.g., 'python')
       - `runtime_version = CharField(max_length=20)` (e.g., '3.12')
       - `artifact_type = CharField(max_length=50, blank=True)` (derived from last package step)
       - `created_by = CharField(max_length=150, blank=True)`
       - `created_at = DateTimeField(auto_now_add=True)`
       - `updated_at = DateTimeField(auto_now=True)`
       - `Meta: db_table = 'core_ci_workflow'`
       - `__str__` returns `self.name`

    5. `CIWorkflowStep` model:
       - `id = BigAutoField(primary_key=True)`
       - `workflow = ForeignKey(CIWorkflow, on_delete=CASCADE, related_name='workflow_steps')`
       - `step = ForeignKey(CIStep, on_delete=PROTECT, related_name='workflow_usages')`
       - `order = IntegerField()`
       - `input_config = JSONField(default=dict)` (per-step overrides)
       - `created_at = DateTimeField(auto_now_add=True)`
       - `Meta: db_table = 'core_ci_workflow_step', unique_together = [['workflow', 'order']], ordering = ['order']`
       - `__str__` returns `f"{self.workflow.name} - Step {self.order}: {self.step.name}"`

    **In core/urls.py:**
    - Add placeholder `ci_workflows_patterns` (empty list for now, will be populated in Plan 02):
      ```python
      ci_workflows_patterns = [
          # Populated in Plan 02
      ]
      ```
    - Ensure the main URL conf (`ssp/urls.py`) replaces the `blueprints` namespace with `ci_workflows`:
      - Change `path('blueprints/', include((..., 'blueprints')))` to `path('ci-workflows/', include((ci_workflows_patterns, 'ci_workflows')))`

    **In core/templates/core/components/nav.html:**
    - Replace the "Blueprints" nav section with "CI Workflows":
      - Rename Alpine.js data property from `blueprintsOpen` to `ciWorkflowsOpen` with `$persist(false).as('nav_ciWorkflows')`
      - Change section label from "Blueprints" to "CI Workflows"
      - Use a workflow/pipeline icon (e.g., a gear/cog with arrows or a list-check icon)
      - Sub-items: "Workflows" (links to `ci_workflows:workflow_list`), "Steps" (links to `ci_workflows:steps_catalog`), "Runtimes" (links to `ci_workflows:runtimes`), "Repositories" (links to `ci_workflows:repo_list`)
      - For now, use `#` as href for all sub-items since URL names don't exist yet. Use comments to mark them for Plan 02.
      - Update path detection: `{% if 'ci-workflows' in request.path %}` instead of `{% if 'blueprints' in request.path %}`
    - Keep the Resources link under a separate section or remove it (it was under Blueprints but is unrelated to CI Workflows). Move it to a standalone nav item or keep as placeholder.

    **Run migrations:**
    ```bash
    source venv/bin/activate
    python manage.py makemigrations core
    python manage.py migrate
    ```
  </action>
  <verify>
    ```bash
    source venv/bin/activate
    python manage.py check --deploy 2>&1 | head -20
    python manage.py showmigrations core | tail -5
    python -c "
import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'ssp.settings')
import django; django.setup()
from core.models import StepsRepository, RuntimeFamily, CIStep, CIWorkflow, CIWorkflowStep
print('All CI models imported successfully')
print(f'StepsRepository fields: {[f.name for f in StepsRepository._meta.get_fields()]}')
print(f'CIWorkflow fields: {[f.name for f in CIWorkflow._meta.get_fields()]}')
"
    ```
    All models import correctly, migrations applied, `python manage.py check` passes.
  </verify>
  <done>Five new CI Workflow models exist in database. Sidebar navigation shows "CI Workflows" with Workflows, Steps, Runtimes, Repositories sub-items. URL namespace changed from blueprints to ci_workflows. Application runs without errors.</done>
</task>

</tasks>

<verification>
1. `python manage.py check` passes with no errors
2. `python manage.py migrate` shows no pending migrations
3. Application starts: `make run` (or `python manage.py runserver`) loads without errors
4. Navigate to home page -- sidebar shows "CI Workflows" instead of "Blueprints"
5. Navigate to `/services/` -- service list renders without blueprint columns
6. No Python import errors related to Blueprint or BlueprintVersion anywhere
7. `python -c "from core.models import StepsRepository, CIStep, CIWorkflow"` works
</verification>

<success_criteria>
- Blueprint and BlueprintVersion models fully removed from codebase
- Service model has no blueprint or blueprint_version fields
- Five new CI Workflow models exist and are migrated
- Sidebar navigation shows CI Workflows section
- All existing pages (services, projects, dashboard) render without errors
- No references to Blueprint/BlueprintVersion in any view, form, template, or URL
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-ci-workflows-builder/05.1-01-SUMMARY.md`
</output>
