---
phase: 06.11-templates-documentation-folder
plan: "03"
type: execute
wave: 3
depends_on:
  - "06.11-01"
  - "06.11-02"
files_modified:
  - docs/templates/scaffolding.md
  - docs/templates/examples.md
autonomous: true
requirements:
  - BPRT-06

must_haves:
  truths:
    - "docs/templates/scaffolding.md documents the full wizard flow from template selection to running service"
    - "scaffolding.md documents the manifest transformation (kind: template to kind: service)"
    - "scaffolding.md documents that the CI Workflow recommendation uses runtimes from the manifest"
    - "scaffolding.md documents the post-scaffolding repo state with pathfinder.yaml present"
    - "docs/templates/examples.md provides complete pathfinder.yaml samples for at least three distinct stacks"
    - "examples.md has both kind: template and kind: service variants for each stack"
  artifacts:
    - path: "docs/templates/scaffolding.md"
      provides: "Full scaffolding flow from wizard to post-scaffold service repo state"
      min_lines: 100
    - path: "docs/templates/examples.md"
      provides: "Complete pathfinder.yaml samples for Python FastAPI, Node.js, and at least one more stack"
      min_lines: 80
  key_links:
    - from: "docs/templates/scaffolding.md"
      to: "docs/templates/manifest-schema.md"
      via: "cross-reference link"
      pattern: "manifest-schema\\.md"
    - from: "docs/templates/scaffolding.md"
      to: "docs/templates/variable-lifecycle.md"
      via: "cross-reference link"
      pattern: "variable-lifecycle\\.md"
    - from: "docs/templates/examples.md"
      to: "docs/templates/manifest-schema.md"
      via: "cross-reference link"
      pattern: "manifest-schema\\.md"
---

<objective>
Author docs/templates/scaffolding.md and docs/templates/examples.md.

Purpose: scaffolding.md is the full redesign of the service creation wizard — from template selection through to the post-scaffolding repo state including the manifest transformation. examples.md gives template authors concrete, copy-paste-ready pathfinder.yaml samples.

Output:
- docs/templates/scaffolding.md — complete wizard flow, manifest transformation, post-scaffolding state
- docs/templates/examples.md — complete pathfinder.yaml samples for Python FastAPI, Node.js, Go, and minimal service
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Foundation docs already authored — reference for consistent field names and cross-links
@docs/templates/manifest-schema.md
@docs/templates/variable-lifecycle.md
@docs/templates/template-registration.md

# Style references
@docs/ci-workflows/build-lifecycle.md
@docs/ci-workflows/README.md

# Research with detailed scaffolding flow and examples
@.planning/phases/06.11-templates-documentation-folder/06.11-RESEARCH.md
@.planning/phases/06.11-templates-documentation-folder/06.11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/templates/scaffolding.md</name>
  <files>docs/templates/scaffolding.md</files>
  <action>
Write docs/templates/scaffolding.md. Mirror the heading hierarchy from docs/ci-workflows/build-lifecycle.md (H1, H2, H3). Use ASCII flow diagrams — no Mermaid. This covers the full redesign of the service creation wizard and post-scaffold repo state.

**H1: Scaffolding**

Opening paragraph (2-3 sentences): Scaffolding creates a new service repository from a registered template. Pathfinder walks the operator through a multi-step wizard that collects service details, configures required variables, and produces a service-ready repository with pathfinder.yaml transformed from kind: template to kind: service.

**H2: Wizard Flow**

Document each wizard page as a numbered flow with ASCII diagram:

```
Wizard Page 1: Template Selection
  - Select template from registered list
  - Select version (git tag) from available tags
  - Enter service name (becomes the "name" in kind: service manifest)
  - Select Project

        |
        v

Wizard Page 2: Repository Setup
  - New repository: enter repo name, default branch
  - OR existing repository: enter repo URL, target branch
  - Configure branch protection settings

        |
        v

Wizard Page 3: Configure Required Variables
  - Pathfinder reads required_vars from template manifest
    at the selected tag's commit SHA
  - Each variable shown with:
      - Variable name (e.g., DATABASE_URL)
      - Description from manifest as helper text
      - Input field for initial value
  - Operator provides initial value for each variable
  - Variables with no value cannot proceed

        |
        v

Wizard Page 4: CI Workflow Recommendation
  - Pathfinder reads runtimes from template manifest
  - Displays compatible CI Workflows based on runtime match
  - Operator confirms or changes the recommendation
  - Selected CI Workflow is associated with the service

        |
        v

Wizard Page 5: Review & Confirm
  - Summary of all selections
  - Operator confirms to begin scaffolding
```

**H2: Scaffolding Execution**

Document what happens after the operator confirms. Use a numbered ASCII flow:

```
Scaffolding begins
        |
        v
1. Fetch template repository at selected tag's commit SHA
        |
        v
2. Copy file tree to target repository
   (new repo: direct to default branch)
   (existing repo: new branch for PR)
        |
        v
3. Transform pathfinder.yaml in target repo:
   - Change kind: "template" to kind: "service"
   - Set name to the service name entered in wizard
   - All other fields (description, runtimes,
     required_vars) are preserved unchanged
        |
        v
4. Commit and push scaffolded files
   (new repo: commit directly to main)
   (existing repo: open Pull Request)
        |
        v
5. Create Service record in Pathfinder
        |
        v
6. Store initial required_var values entered
   in wizard as env_vars in Pathfinder
   (stored at environment level)
        |
        v
Scaffolding complete
```

**H2: Manifest Transformation**

Dedicated section documenting the kind: template → kind: service transition. This is the mechanism by which every service repo self-describes its requirements.

Show a before/after example:

Template manifest (before, at selected tag):
```yaml
kind: template
name: python-fastapi
description: "FastAPI service with async PostgreSQL"
runtimes:
  - python: ">=3.11"
required_vars:
  DATABASE_URL: "PostgreSQL connection string"
  SECRET_KEY: "Application secret key"
```

Service manifest (after, in scaffolded repo):
```yaml
kind: service
name: order-service          # Set to wizard-entered service name
description: "FastAPI service with async PostgreSQL"
runtimes:
  - python: ">=3.11"         # Preserved — informational only post-scaffolding
required_vars:
  DATABASE_URL: "PostgreSQL connection string"
  SECRET_KEY: "Application secret key"
```

State clearly: "The required_vars section is preserved verbatim from the template. The transformation only changes kind and name. This ensures the service repo's manifest accurately declares the same variables the template required."

Cross-reference: "For the pathfinder.yaml field format, see [Manifest Schema](manifest-schema.md)."

**H2: Post-Scaffolding State**

Document what exists after scaffolding completes:

Service repo state:
- Contains all files from the template at the selected tag
- pathfinder.yaml present in repo root with kind: service
- name field matches the service name in Pathfinder

Pathfinder state:
- Service record created and linked to the Project
- Initial required_var values stored in env_vars (at service/environment level)
- CI Workflow associated with the service (from wizard selection)
- Template and version recorded (for audit/display purposes)

**H2: First Build**

Document the first build webhook behavior after scaffolding. This connects scaffolding to the variable lifecycle:

```
Developer pushes first commit to service repo
        |
        v
Build webhook received
        |
        v
Pathfinder reads pathfinder.yaml at commit SHA
Finds kind: service with required_vars
        |
        v
Compares against stored required_vars
(initial values already in env_vars from wizard)
        |
        v
All vars satisfied (wizard captured initial values)
        |
        v
Deployment gate: allowed
```

State: "Because the scaffolding wizard collected initial values for all required variables, the first build should find all variables satisfied. If the operator skipped or left values empty during the wizard, the first build may find unsatisfied variables and block deployment."

Cross-reference: "For full variable lifecycle behavior, see [Variable Lifecycle](variable-lifecycle.md)."

**H2: Non-Scaffolded Services**

Brief section (4-5 sentences): Existing service repositories can be onboarded to Pathfinder without scaffolding. These repos do not automatically get a pathfinder.yaml. An operator can add pathfinder.yaml manually to an existing service repo to enable variable discovery and enforcement. The file must use kind: service. Once present, Pathfinder reads it at build time like any scaffolded service.

IMPORTANT constraints from CONTEXT.md (locked):
- Scaffolded repos ALWAYS have pathfinder.yaml — scaffolding creates it (never optional)
- kind transforms from template to service; name changes to service name; all other fields preserved
- runtimes in kind: service are informational only after scaffolding
- Wizard page 3 shows descriptions from the manifest as helper text (confirmed in RESEARCH.md open questions)
- Wizard page 4 reads runtimes for CI Workflow recommendation — runtimes do NOT automatically assign a workflow
- DO NOT reference old wizard.md flow, old Blueprints, or Jinja2 template substitution
  </action>
  <verify>
    - ls docs/templates/scaffolding.md
    - Confirm H2 sections: "Wizard Flow", "Scaffolding Execution", "Manifest Transformation", "Post-Scaffolding State", "First Build", "Non-Scaffolded Services"
    - Confirm ASCII wizard flow (5 pages) is present
    - Confirm ASCII scaffolding execution flow (6 steps) is present
    - Confirm before/after YAML example for manifest transformation is present
    - Confirm cross-references to manifest-schema.md and variable-lifecycle.md are present
    - Confirm no reference to old Blueprints system or Jinja2
  </verify>
  <done>
    docs/templates/scaffolding.md exists with complete wizard flow, scaffolding execution flow, manifest transformation before/after example, post-scaffolding state description, first build behavior, and non-scaffolded service handling. BPRT-06 is satisfied: scaffolded repos keep pathfinder.yaml with kind: service.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author docs/templates/examples.md</name>
  <files>docs/templates/examples.md</files>
  <action>
Write docs/templates/examples.md. This is a reference document of complete, copy-paste-ready pathfinder.yaml samples. Keep it concise — each example is a complete YAML block with a short preamble explaining what it demonstrates.

**H1: Examples**

Opening paragraph (1-2 sentences): This page provides complete pathfinder.yaml examples for common service templates and scaffolded service repos. All examples are minimal and valid against the schema documented in [Manifest Schema](manifest-schema.md).

**H2: Template Examples**

Sub-heading note: "The following examples are for template repositories (kind: template). These files live in template repos and are read by Pathfinder during registration and when the template is selected in the service creation wizard."

**H3: Python FastAPI Service**

Brief context (1 sentence): A Python FastAPI service with PostgreSQL and Redis.

```yaml
kind: template
name: python-fastapi
description: "FastAPI service with async PostgreSQL and Redis"
runtimes:
  - python: ">=3.11"
required_vars:
  DATABASE_URL: "PostgreSQL connection string (asyncpg format)"
  SECRET_KEY: "Application secret key for JWT signing"
  REDIS_URL: "Redis connection string for caching"
```

**H3: Node.js Express API**

Brief context (1 sentence): A Node.js Express API with TypeScript.

```yaml
kind: template
name: node-express-api
description: "Node.js Express REST API with TypeScript"
runtimes:
  - node: ">=20"
required_vars:
  DATABASE_URL: "PostgreSQL connection string"
  JWT_SECRET: "Secret for JWT token signing"
  PORT: "Port the service listens on (e.g. 8080)"
```

**H3: Go HTTP Service**

Brief context (1 sentence): A minimal Go HTTP service.

```yaml
kind: template
name: go-http-service
description: "Minimal Go HTTP service with Chi router"
runtimes:
  - go: ">=1.22"
required_vars:
  DATABASE_URL: "PostgreSQL connection string"
  LOG_LEVEL: "Log verbosity: debug, info, warn, error"
```

**H3: React Frontend (SPA)**

Brief context (1 sentence): A React single-page application. No required_vars because the frontend is served as static assets with no server-side secrets.

```yaml
kind: template
name: react-spa
description: "React single-page application with TypeScript and Vite"
runtimes:
  - node: ">=20"
```

Note: "No required_vars — this is a valid template. If no runtime-level secrets are needed, the required_vars section can be omitted entirely."

**H3: Multi-Runtime Service**

Brief context (1 sentence): A service that builds Python with Node.js tooling (e.g., a Django app with a compiled frontend).

```yaml
kind: template
name: django-with-frontend
description: "Django web app with compiled React frontend"
runtimes:
  - python: ">=3.11"
  - node: ">=20"
required_vars:
  DATABASE_URL: "PostgreSQL connection string"
  SECRET_KEY: "Django secret key"
  ALLOWED_HOSTS: "Comma-separated list of allowed hostnames"
```

**H2: Service Examples**

Sub-heading note: "The following examples show pathfinder.yaml files as they appear in scaffolded service repos (kind: service). These are produced by scaffolding — the template's kind: template becomes kind: service with name set to the service handler name."

**H3: Scaffolded Python FastAPI Service**

Brief context (1 sentence): The python-fastapi template after scaffolding into a service named order-service.

```yaml
kind: service
name: order-service
description: "FastAPI service with async PostgreSQL and Redis"
runtimes:
  - python: ">=3.11"
required_vars:
  DATABASE_URL: "PostgreSQL connection string (asyncpg format)"
  SECRET_KEY: "Application secret key for JWT signing"
  REDIS_URL: "Redis connection string for caching"
```

**H3: Minimal Service (No Required Variables)**

Brief context (1 sentence): A service that requires no configurable variables.

```yaml
kind: service
name: frontend-static
description: "React SPA frontend"
runtimes:
  - node: ">=20"
```

Note: "No required_vars — no variable enforcement applies. The service deploys without any manifest gate."

**H3: Manually Added Manifest (Non-Scaffolded Service)**

Brief context (2 sentences): An existing service repo that was onboarded to Pathfinder without scaffolding. An operator added pathfinder.yaml manually to enable variable discovery.

```yaml
kind: service
name: legacy-api
description: "Legacy API service onboarded to Pathfinder"
required_vars:
  DATABASE_URL: "Primary database connection string"
  API_KEY: "Third-party API key for payment processing"
```

Note: "runtimes is optional. Omitting it means no CI Workflow recommendation is available for this service, but variable enforcement still applies."

Cross-references: after all examples, add footer: "For the full pathfinder.yaml field reference, see [Manifest Schema](manifest-schema.md). For how required_vars are enforced at deployment time, see [Variable Lifecycle](variable-lifecycle.md)."
  </action>
  <verify>
    - ls docs/templates/examples.md
    - Confirm H2 sections: "Template Examples" and "Service Examples"
    - Confirm at least 5 kind: template examples are present
    - Confirm at least 3 kind: service examples are present
    - Confirm multi-runtime example (python + node) is present
    - Confirm minimal example with no required_vars is present
    - Confirm cross-references to manifest-schema.md and variable-lifecycle.md are present
    - Confirm no YAML example contains variable values in required_vars (only descriptions)
  </verify>
  <done>
    docs/templates/examples.md exists with template examples (Python FastAPI, Node.js, Go, React SPA, multi-runtime) and service examples (scaffolded, minimal, manually-added). All required_vars entries contain only descriptions — no values. Cross-references to manifest-schema.md are present.
  </done>
</task>

</tasks>

<verification>
- docs/templates/scaffolding.md exists and covers: 5-page wizard flow, scaffolding execution steps, manifest transformation before/after, post-scaffolding state, first build behavior, non-scaffolded services
- docs/templates/examples.md exists with examples for at least 3 template stacks and 2 service variants
- Both files use ASCII diagrams — no Mermaid
- No example in examples.md contains a value in required_vars (only descriptions)
- scaffolding.md documents the hard constraint: scaffolded repos ALWAYS have pathfinder.yaml
- scaffolding.md documents runtimes inform CI Workflow recommendation — they do not auto-assign
- Both files cross-reference manifest-schema.md
</verification>

<success_criteria>
A template author reading examples.md can copy any example, modify the name and required_vars, and have a valid pathfinder.yaml in under 5 minutes. An operator reading scaffolding.md understands every step from wizard start to first deployment without needing to consult implementation code.
</success_criteria>

<output>
After completion, create .planning/phases/06.11-templates-documentation-folder/06.11-03-SUMMARY.md
</output>
