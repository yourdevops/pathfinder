---
phase: 06-builds
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - core/views/services.py
  - core/templates/core/services/_builds_tab.html
  - core/templates/core/services/_build_row.html
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can view build history table on service Builds tab"
    - "Build table shows status, commit, branch, author, started, duration, links"
    - "User can filter builds by status (all, success, failed, running)"
    - "Build list is paginated with 20 items per page"
    - "In-progress builds auto-refresh via HTMX polling"
  artifacts:
    - path: "core/views/services.py"
      provides: "Builds tab context with filtering and pagination"
      contains: "builds = Build.objects.filter"
    - path: "core/templates/core/services/_builds_tab.html"
      provides: "Builds tab template with table and filters"
      contains: "table"
    - path: "core/templates/core/services/_build_row.html"
      provides: "Individual build row component for HTMX updates"
      contains: "hx-trigger"
  key_links:
    - from: "core/views/services.py"
      to: "core/models.py"
      via: "Build.objects.filter query"
      pattern: "Build\\.objects\\.filter"
    - from: "core/templates/core/services/_builds_tab.html"
      to: "core/templates/core/services/_build_row.html"
      via: "template include"
      pattern: "include.*_build_row"
---

<objective>
Implement build history UI for service detail page.

Purpose: Enable users to view, filter, and monitor builds for their services with real-time updates for in-progress builds.

Output: Builds tab with table layout showing build details, status filtering, pagination, and HTMX polling for live updates.
</objective>

<execution_context>
@/Users/fandruhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fandruhin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-builds/06-CONTEXT.md
@.planning/phases/06-builds/06-RESEARCH.md
@.planning/phases/06-builds/06-01-SUMMARY.md
@core/views/services.py
@core/templates/core/services/detail.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ServiceDetailView builds tab with real data</name>
  <files>core/views/services.py</files>
  <action>
Update the ServiceDetailView.get_context_data method to populate the builds tab with real data:

1. Import at top of file:
   - from django.core.paginator import Paginator
   - from core.models import Build (add to existing imports)

2. In the "elif tab == 'builds':" section, replace placeholder with:

   # Get builds for this service
   builds_qs = Build.objects.filter(service=self.service).order_by('-created_at')

   # Apply status filter if provided
   status_filter = self.request.GET.get('status', 'all')
   if status_filter and status_filter != 'all':
       builds_qs = builds_qs.filter(status=status_filter)

   # Paginate (20 per page per CONTEXT.md)
   paginator = Paginator(builds_qs, 20)
   page_number = self.request.GET.get('page', 1)
   page_obj = paginator.get_page(page_number)

   context['builds'] = page_obj
   context['page_obj'] = page_obj
   context['status_filter'] = status_filter
   context['status_choices'] = [
       ('all', 'All'),
       ('running', 'Running'),
       ('success', 'Success'),
       ('failed', 'Failed'),
   ]

   # Check if any builds are in progress (for auto-refresh)
   context['has_running_builds'] = builds_qs.filter(status__in=['pending', 'running']).exists()

3. Also add context for empty state:
   context['has_any_builds'] = Build.objects.filter(service=self.service).exists()
  </action>
  <verify>
Run: `uv run python manage.py check` - no errors
Verify: grep for "Paginator" and "Build.objects.filter" in core/views/services.py
  </verify>
  <done>
ServiceDetailView builds tab returns paginated, filtered Build queryset with status choices and running build detection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create builds tab template with table layout</name>
  <files>core/templates/core/services/_builds_tab.html</files>
  <action>
Replace the existing placeholder _builds_tab.html with a full implementation:

1. Header section with title and filter dropdown:
   - "Builds" heading
   - Status filter dropdown (HTMX-powered, pushes URL)
   - Filter options: All, Running, Success, Failed

2. Empty state (when has_any_builds is False):
   - Icon (inbox or similar)
   - "No builds yet" message
   - "Builds will appear here when your CI workflow runs" helper text

3. Build table with columns (per CONTEXT.md):
   - Status icon (colored dot or badge)
   - Commit SHA (first 7 chars) + commit message preview (truncated)
   - Branch name with branch icon
   - Author avatar (small) + username
   - Started timestamp (relative, e.g., "5 min ago")
   - Duration (e.g., "2m 34s" or "-" if still running)
   - CI Job link (external link icon, opens GitHub Actions)

4. Table styling (dark theme, matching existing UI):
   - bg-gray-800 header
   - bg-gray-900 rows with hover:bg-gray-800
   - Proper cell padding and text colors

5. Pagination controls at bottom:
   - Previous/Next buttons
   - Page X of Y indicator
   - Use HTMX for navigation (hx-get, hx-target, hx-push-url)

6. Auto-refresh for running builds:
   - If has_running_builds is True, add hx-trigger="every 5s" to the table container
   - Target the entire tab content for refresh

7. HTMX attributes:
   - Filter and pagination use hx-get with updated query params
   - hx-target="#builds-tab-content"
   - hx-push-url="true" to update browser URL
   - hx-swap="innerHTML"
  </action>
  <verify>
File exists: core/templates/core/services/_builds_tab.html
Contains: table, hx-trigger, status_filter
  </verify>
  <done>
_builds_tab.html displays build history in table format with filtering, pagination, and auto-refresh for running builds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create build row component for HTMX updates</name>
  <files>core/templates/core/services/_build_row.html</files>
  <action>
Create a reusable build row component for the builds table:

1. Create core/templates/core/services/_build_row.html

2. Expects 'build' object in context

3. Row structure (single <tr>):

   a) Status cell:
      - Colored status badge/dot based on build.status
      - pending/running: blue with pulse animation
      - success: green
      - failed: red
      - cancelled: gray

   b) Commit cell:
      - Commit SHA (first 7 chars): `{{ build.commit_sha|slice:":7" }}`
      - Styled as monospace, text-blue-400 for link appearance
      - Commit message preview (first 50 chars): `{{ build.commit_message|truncatechars:50 }}`
      - Show "..." if truncated

   c) Branch cell:
      - Branch icon (SVG)
      - Branch name: `{{ build.branch }}`

   d) Author cell:
      - Small avatar (16x16 or 20x20): `<img src="{{ build.author_avatar_url }}" class="w-5 h-5 rounded-full">`
      - Fallback to initials if no avatar
      - Username: `{{ build.author }}`

   e) Started cell:
      - Relative time using Django's timesince: `{{ build.started_at|timesince }} ago`
      - Title attribute with full datetime for hover

   f) Duration cell:
      - If build.duration_seconds: format as "Xm Ys" or "Xh Ym"
      - If still running: show "-" or "running..."
      - Template filter or inline logic for formatting

   g) Actions cell:
      - Link to GitHub Actions: `<a href="{{ build.ci_job_url }}" target="_blank">` with external link icon
      - Only show if ci_job_url is set

4. For running builds (status in pending, running):
   - Add id="build-row-{{ build.id }}"
   - Could add individual row polling, but table-level refresh is simpler

5. Styling:
   - text-gray-300 for primary text
   - text-gray-500 for secondary text (timestamp, duration)
   - hover:bg-gray-800 on row
  </action>
  <verify>
File exists: core/templates/core/services/_build_row.html
Contains: build.commit_sha, build.status, build.ci_job_url
  </verify>
  <done>
_build_row.html renders individual build data with status indicator, commit info, author, timing, and CI link.
  </done>
</task>

</tasks>

<verification>
1. Run checks: `uv run python manage.py check`
2. Start server: `make run`
3. Navigate to a service detail page and click Builds tab
4. Verify table renders (may be empty if no builds)
5. Verify filter dropdown works (URL updates, table refreshes)
6. If builds exist, verify pagination works
</verification>

<success_criteria>
- Builds tab shows table with proper columns (or empty state if no builds)
- Status filter changes URL and filters results
- Pagination shows 20 items per page with navigation
- In-progress builds trigger auto-refresh (every 5s)
- All links work (CI job opens in new tab)
- Dark theme styling consistent with rest of app
</success_criteria>

<output>
After completion, create `.planning/phases/06-builds/06-02-SUMMARY.md`
</output>
