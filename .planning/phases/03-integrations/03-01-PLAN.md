---
phase: 03-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/__init__.py
  - plugins/base.py
  - core/models.py
  - core/encryption.py
  - core/migrations/0003_integrationconnection.py
  - pathfinder/settings.py
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "Plugin registry discovers and loads plugins at Django startup"
    - "IntegrationConnection model stores connection config with encrypted sensitive fields"
    - "Sensitive fields cannot be decrypted without correct encryption key"
  artifacts:
    - path: "plugins/__init__.py"
      provides: "Plugin autodiscover function"
      contains: "autodiscover"
    - path: "plugins/base.py"
      provides: "BasePlugin abstract class and PluginRegistry"
      contains: "class BasePlugin"
    - path: "core/models.py"
      provides: "IntegrationConnection model"
      contains: "class IntegrationConnection"
    - path: "core/encryption.py"
      provides: "Fernet encryption utilities"
      contains: "encrypt_config"
  key_links:
    - from: "plugins/__init__.py"
      to: "plugins/base.py"
      via: "import registry"
      pattern: "from plugins.base import registry"
    - from: "core/models.py"
      to: "core/encryption.py"
      via: "encrypt_config, decrypt_config"
      pattern: "from core.encryption import"
---

<objective>
Create the plugin framework foundation and IntegrationConnection model with Fernet encryption for sensitive credentials.

Purpose: This is the foundation for all integration plugins. Without this, neither GitHub nor Docker plugins can be implemented.
Output: Working plugin registry with auto-discovery, IntegrationConnection model with encrypted config support, and encryption utilities.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-integrations/03-CONTEXT.md
@.planning/phases/03-integrations/03-RESEARCH.md
@core/models.py
@pathfinder/settings.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create plugin package structure</name>
  <files>
    requirements.txt
    plugins/__init__.py
    plugins/base.py
    pathfinder/settings.py
  </files>
  <action>
1. Add new dependencies to requirements.txt:
   - cryptography>=44.0.0
   - PyGithub>=2.5.0
   - docker>=7.1.0
   - django-tasks[database]>=0.4.0
   - django-formtools>=2.5.0

2. Create plugins/ directory at project root (same level as core/).

3. Create plugins/__init__.py with autodiscover function:
   - Use pkgutil.iter_modules to scan plugins directory
   - Import each plugin package (triggers registration)
   - Handle ImportError gracefully with logging

4. Create plugins/base.py with:
   - PluginRegistry class (singleton pattern with class methods)
     - _plugins: Dict[str, BasePlugin]
     - register(plugin): adds plugin to registry
     - get(name): returns plugin by name
     - all(): returns all plugins
     - by_category(category): returns plugins filtered by category ('scm', 'ci', 'deploy')
   - BasePlugin abstract class with:
     - name: str (unique identifier like 'github', 'docker')
     - display_name: str (human-readable)
     - category: str ('scm', 'ci', 'deploy')
     - capabilities: List[str]
     - icon: str (icon class name)
     - sensitive_field_patterns: List[str] = ['password', 'token', 'secret', 'private_key', 'api_key', 'client_secret']
     - Abstract methods: get_config_schema(), get_wizard_forms(), health_check(config), get_urlpatterns()
     - is_sensitive_field(field_name): checks if field should be encrypted

5. Update pathfinder/settings.py:
   - Add 'django_tasks' and 'django_tasks.backends.database' to INSTALLED_APPS
   - Add 'formtools' to INSTALLED_APPS
   - Add TASKS configuration for database backend
   - Add HEALTH_CHECK_INTERVAL = 900 (15 minutes)

6. Run pip install to install new dependencies in venv.
  </action>
  <verify>
    python -c "from plugins.base import BasePlugin, registry; print('Plugin base OK')"
    python -c "from django_tasks import task; print('django-tasks OK')"
    python -c "from cryptography.fernet import Fernet; print('cryptography OK')"
  </verify>
  <done>
    - plugins/ directory exists with __init__.py and base.py
    - BasePlugin and PluginRegistry classes defined
    - All new dependencies installed and importable
    - Django settings updated for django-tasks and formtools
  </done>
</task>

<task type="auto">
  <name>Task 2: Create encryption utilities</name>
  <files>
    core/encryption.py
    secrets/.gitkeep
  </files>
  <action>
1. Create core/encryption.py with Fernet encryption functions:

   get_encryption_key() -> bytes:
   - Check PTF_ENCRYPTION_KEY environment variable first
   - If not set, check secrets/encryption.key file
   - If file doesn't exist, generate new key with Fernet.generate_key()
   - Save new key to secrets/encryption.key with chmod 0o600
   - Return the key bytes

   get_fernet() -> Fernet:
   - Return Fernet instance using get_encryption_key()

   encrypt_config(config: dict) -> bytes:
   - Serialize config to JSON bytes
   - Encrypt with Fernet
   - Return encrypted bytes

   decrypt_config(encrypted: bytes) -> dict:
   - Decrypt bytes with Fernet
   - Parse JSON and return dict

2. Ensure secrets/ directory exists (already should from Phase 1).

3. Add secrets/encryption.key to .gitignore if not already present.

Note: The encryption key is auto-generated on first use if not provided via environment variable. This is acceptable for development; production deployments should use PTF_ENCRYPTION_KEY env var.
  </action>
  <verify>
    python -c "
from core.encryption import encrypt_config, decrypt_config
test_data = {'api_key': 'secret123', 'value': 'test'}
encrypted = encrypt_config(test_data)
decrypted = decrypt_config(encrypted)
assert decrypted == test_data, 'Encryption roundtrip failed'
print('Encryption roundtrip OK')
"
  </verify>
  <done>
    - core/encryption.py exists with encrypt_config and decrypt_config functions
    - Encryption key is auto-generated or loaded from env/file
    - Roundtrip test passes (encrypt then decrypt returns original data)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create IntegrationConnection model</name>
  <files>
    core/models.py
    core/migrations/0003_integrationconnection.py
  </files>
  <action>
1. Add IntegrationConnection model to core/models.py:

   class IntegrationConnection(models.Model):
       HEALTH_STATUS_CHOICES = [
           ('healthy', 'Healthy'),
           ('unhealthy', 'Unhealthy'),
           ('unknown', 'Unknown'),
       ]
       STATUS_CHOICES = [
           ('active', 'Active'),
           ('disabled', 'Disabled'),
       ]

       id = models.BigAutoField(primary_key=True)
       uuid = models.UUIDField(default=uuid.uuid4, editable=False, unique=True, db_index=True)
       name = models.CharField(max_length=63, unique=True)  # DNS-compatible
       description = models.TextField(blank=True)
       plugin_name = models.CharField(max_length=63)  # References plugin by name

       # Configuration storage
       config = models.JSONField(default=dict)  # Non-sensitive config
       config_encrypted = models.BinaryField(null=True, blank=True)  # Encrypted sensitive fields

       # Status fields
       status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='active')
       health_status = models.CharField(max_length=20, choices=HEALTH_STATUS_CHOICES, default='unknown')
       last_health_check = models.DateTimeField(null=True, blank=True)
       last_health_message = models.TextField(blank=True)

       # Audit fields
       created_by = models.CharField(max_length=150, blank=True)
       created_at = models.DateTimeField(auto_now_add=True)
       updated_at = models.DateTimeField(auto_now=True)

       class Meta:
           db_table = 'core_integration_connection'
           ordering = ['name']

       def __str__(self):
           return f"{self.name} ({self.plugin_name})"

       def set_config(self, full_config: dict):
           """Separate and store config, encrypting sensitive fields."""
           from plugins.base import registry
           plugin = registry.get(self.plugin_name)

           sensitive = {}
           non_sensitive = {}

           for key, value in full_config.items():
               if value and plugin and plugin.is_sensitive_field(key):
                   sensitive[key] = value
               else:
                   non_sensitive[key] = value

           self.config = non_sensitive
           if sensitive:
               from core.encryption import encrypt_config
               self.config_encrypted = encrypt_config(sensitive)
           else:
               self.config_encrypted = None

       def get_config(self) -> dict:
           """Return merged config with decrypted sensitive fields."""
           result = dict(self.config)
           if self.config_encrypted:
               from core.encryption import decrypt_config
               decrypted = decrypt_config(self.config_encrypted)
               result.update(decrypted)
           return result

       def get_plugin(self):
           """Return the plugin instance for this connection."""
           from plugins.base import registry
           return registry.get(self.plugin_name)

       @property
       def plugin_missing(self) -> bool:
           """Check if plugin is no longer available."""
           return self.get_plugin() is None

2. Register IntegrationConnection with auditlog at the bottom of models.py.

3. Run makemigrations and migrate:
   python manage.py makemigrations core
   python manage.py migrate
  </action>
  <verify>
    python manage.py check
    python -c "
from core.models import IntegrationConnection
# Test model exists and can be instantiated
conn = IntegrationConnection(name='test', plugin_name='github')
print('IntegrationConnection model OK')
"
  </verify>
  <done>
    - IntegrationConnection model added to core/models.py
    - Migration created and applied
    - Model supports set_config/get_config for encrypted sensitive fields
    - Model registered with auditlog
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. python manage.py check - no errors
2. Import plugins.base.BasePlugin and registry - works
3. Import core.encryption functions - works
4. IntegrationConnection can store and retrieve encrypted config
5. Plugin registry can register and retrieve plugins (empty registry is OK at this point)
</verification>

<success_criteria>
- [ ] plugins/ package exists with base.py defining BasePlugin and PluginRegistry
- [ ] core/encryption.py provides Fernet-based encrypt/decrypt functions
- [ ] IntegrationConnection model exists with set_config/get_config methods
- [ ] Sensitive fields are encrypted when stored, decrypted when retrieved
- [ ] django-tasks and formtools are installed and configured
- [ ] All imports work without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-integrations/03-01-SUMMARY.md`
</output>
