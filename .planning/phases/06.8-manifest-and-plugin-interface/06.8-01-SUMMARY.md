---
phase: 06.8-manifest-and-plugin-interface
plan: 01
subsystem: plugins
tags: [plugin-interface, ci, manifest, artifact-resolution, github-packages]

# Dependency graph
requires:
  - phase: 06.1-ci-workflows-gap
    provides: manifest_id() method and CICapableMixin interface
provides:
  - Clean plugin interface without dead manifest_path method
  - resolve_artifact_ref() on CICapableMixin for container image discovery
  - GitHubPlugin artifact resolution via GitHub Packages API
  - Automatic artifact ref resolution in poll_build_details for successful builds
affects: [06.8-02, 06.8-03, 06.8-04, deployments]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Non-blocking artifact resolution: failures logged as warnings, never block build processing"
    - "Dual endpoint fallback: try org packages first, then user packages for GHCR lookup"

key-files:
  created: []
  modified:
    - plugins/base.py
    - plugins/github/plugin.py
    - core/git_utils.py
    - core/tasks.py

key-decisions:
  - "manifest_path fully removed; scaffolding uses manifest_id(workflow) for CI manifest file paths"
  - "resolve_artifact_ref returns empty string on failure (non-blocking design)"

patterns-established:
  - "Artifact resolution via CI plugin API rather than trusting webhook-extracted refs"

requirements-completed: [GAP-19, GAP-13]

# Metrics
duration: 2min
completed: 2026-02-17
---

# Phase 06.8 Plan 01: Manifest and Plugin Interface Summary

**Removed dead manifest_path from plugin interface, added resolve_artifact_ref for API-based container image discovery via GitHub Packages**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-17T15:39:28Z
- **Completed:** 2026-02-17T15:42:18Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Eliminated confusing dead `manifest_path` method from CICapableMixin and GitHubPlugin
- Updated both scaffolding functions to use `manifest_id(workflow)` for correct workflow-name-based file paths
- Added `resolve_artifact_ref()` to plugin interface for API-resolved container image references
- Integrated artifact resolution into `poll_build_details` for successful builds (non-blocking)

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove manifest_path and update scaffolding to use manifest_id** - `1ec7aba` (refactor)
2. **Task 2: Add resolve_artifact_ref to plugin interface and update poll_build_details** - `751a4fd` (feat)

## Files Created/Modified
- `plugins/base.py` - Removed manifest_path(), added resolve_artifact_ref() to CICapableMixin
- `plugins/github/plugin.py` - Removed manifest_path(), implemented resolve_artifact_ref() querying GitHub Packages API
- `core/git_utils.py` - Updated scaffold_new_repository and scaffold_existing_repository to use manifest_id(workflow)
- `core/tasks.py` - Updated comment, added artifact resolution block in poll_build_details

## Decisions Made
None - followed plan as specified

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plugin interface is clean: no dead methods, artifact resolution available
- Ready for Plan 02 (further plugin interface refinements)

## Self-Check: PASSED

All files exist. All commits verified.

---
*Phase: 06.8-manifest-and-plugin-interface*
*Completed: 2026-02-17*
